---
phase: 08-live-state-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/domain/stack/stack-status-badge.tsx
  - client/test/unit/components/domain/stack/stack-status-badge.test.tsx
  - .planning/todos/completed/2026-08-28-config-error-ui-indication-missing.md
  - .planning/todos/completed/2026-08-28-env-file-changes-dont-flag-config-changed.md
  - .planning/todos/completed/2026-08-28-manual-actions-dont-broadcast-sse.md
  - .planning/STATE.md
autonomous: true
requirements:
  - "n/a — phase scoped by three todo files, not by REQUIREMENTS.md IDs: .planning/todos/pending/2026-08-28-config-error-ui-indication-missing.md, .planning/todos/pending/2026-08-28-env-file-changes-dont-flag-config-changed.md, .planning/todos/pending/2026-08-28-manual-actions-dont-broadcast-sse.md"

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A stack whose status is BACKING_UP, RESTORING, or MIGRATING renders a badge that visibly pulses, using the same motion cue DEPLOYING and UPDATING already use, while keeping its existing outline variant and gaining no background, text, or border color."
    - "DEPLOYING and UPDATING keep their exact existing blue-plus-pulse treatment, and no status outside the five in-progress states gains motion."
    - "An unrecognized status string still renders as its own raw label in an outline badge with no color and no motion."
    - "The three Phase-08-scoped todo files are no longer filed under .planning/todos/pending/, each carries completed/status frontmatter plus a resolution naming the plans that closed its code gap, and no unrelated pending todo was disturbed."
    - "STATE.md no longer lists the three closed items as pending, and its stale Phase-02 config_error blocker entry is annotated as resolved and points at the todo's new location."
    - "The six previously-unexecutable human-judgment items from 05.1-02 (D6), 05.1-04 (D7), and 05.1-06 (D9) exist as one concrete, self-contained verification script that names, per item, the exact observable it asserts and the D-item it closes."
    - "Whether this execution session could run those live checks itself is settled by a recorded reachability probe result rather than by assumption, and the branch taken is stated in the summary."
  artifacts:
    - "client/src/components/domain/stack/stack-status-badge.tsx — statusColors gains BACKING_UP, RESTORING, and MIGRATING entries whose entire value is the single utility animate-pulse"
    - "client/test/unit/components/domain/stack/stack-status-badge.test.tsx — new file, one it() per status and per regression lock"
    - ".planning/todos/completed/2026-08-28-config-error-ui-indication-missing.md — moved, stamped, resolution added"
    - ".planning/todos/completed/2026-08-28-env-file-changes-dont-flag-config-changed.md — moved, stamped, resolution added"
    - ".planning/todos/completed/2026-08-28-manual-actions-dont-broadcast-sse.md — moved, stamped, resolution added"
    - ".planning/STATE.md — three bullets removed from Pending Todos, Phase-02 config_error blocker entry annotated"
    - ".planning/phases/08-live-state-consistency/08-01-SUMMARY.md — probe evidence, branch taken, and the six restated verification items"
  key_links:
    - "statusConfig (label plus shadcn variant) and statusColors (Tailwind override className) stay two independently-keyed maps — the fix adds only to statusColors, and the outline variant for the three maintenance states keeps coming from statusConfig"
    - "StackStatusBadge -> Badge's className prop -> cn(badgeVariants({variant}), className) on a span carrying data-slot=badge and data-variant — the only channel through which animate-pulse reaches the DOM"
    - "stack_status SSE -> useStack / useStacks -> StackStatusBadge re-render — shipped in 05.1-02 and 05.1-04; the pulse is the terminal presentation of that already-working chain, not a new link in it"
    - "gsd_run query todo complete <filename> is the single mechanism that both moves a todo and stamps its completed/status frontmatter — a hand-rolled git mv would move the file without the stamp"
---

<objective>
Close Phase 08 by shipping the one real code gap its own scope actually has, and by converting six stranded verification items into something a human can execute in one sitting.

Direct inspection (08-RESEARCH.md, with file:line citations) proves all three of this phase's scoped todos are already code-complete and unit-tested — shipped across plans 05.1-02, 05.1-04, and 05.1-06. What is genuinely open is: a missing motion cue on the three maintenance statuses, six live checks that no prior session could run, and three todo files that were never closed out even though their code gaps were.

Purpose: a stack sitting at "Backing Up" with a static gray badge reads as stuck, not as working — which defeats the whole point of a phase whose goal is that correct server state shows up live. And a project whose STATE.md still advertises three fixed bugs as open cannot tell a reader what is actually left.

Output: three entries added to `statusColors`, a new unit test file that regression-locks the whole status palette, a recorded reachability verdict for this environment, a complete six-item live-verification script handed to the phase's UAT, and three todos closed with a pointer back to the plans that actually fixed them.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-live-state-consistency/08-RESEARCH.md
@.planning/phases/08-live-state-consistency/08-PATTERNS.md
@.planning/phases/08-live-state-consistency/08-UI-SPEC.md
@client/src/components/domain/stack/stack-status-badge.tsx
@client/test/unit/components/domain/stack/cert-status-badge.test.tsx
</context>

<planning_notes>
**No CONTEXT.md exists** — the user deliberately skipped `/gsd-discuss-phase` because `08-UI-SPEC.md` (status: approved, 2026-09-13) already carries this phase's locked design decisions. `08-UI-SPEC.md` is therefore treated as the decision source of record: its Color section locks the fix to `animate-pulse` only, with no color or variant change.

**No REQUIREMENTS.md IDs** — `phase_req_ids` is literally the string "n/a — this phase is scoped by the todo list below". The `must_haves.truths` above are derived from the ROADMAP Phase 8 goal, the UI-SPEC Color contract, and 08-RESEARCH.md's Phase Requirements table, which together function as this phase's acceptance-criteria spec.

Planner decisions, recorded so the executor does not re-litigate them:

- **PD-1 (tracer choice).** The `animate-pulse` fix plus its new test is the phase's tracer: it is the only genuinely new deliverable, it touches the single layer this phase modifies end to end (status value -> lookup map -> `Badge` className -> rendered DOM class), and it carries a real runnable end-to-end verify. Tasks 2 and 3 are not expansion of that slice — they are a parallel verification-and-closure track that is ordered after it only so the human doing the live pass sees the pulse while checking the backup round trip (V4).

- **PD-2 (reachability is measured, not assumed).** 08-RESEARCH.md Open Question 1 asked whether the execution session could run the live checks itself. This planning session measured it: `docker info` succeeds and `docker exec docktor-db-dev psql -U docktor -d docktor -c 'select 1'` returns a row, but a Postgres SSLRequest payload written to `127.0.0.1:5432` gets no response within 10s, and the same payload to the container's own bridge address `172.19.0.2:5432` also gets no response. The database is healthy; the agent's network namespace cannot complete a TCP payload exchange with it. That is the same limitation STATE.md records for 05.1-01, 05.1-05, 05.1-06, 06-01, and 06-07 — so `yarn dev`'s server cannot boot here and no page can be loaded. Task 2 still re-probes rather than trusting this measurement, because the execution session is a separate invocation.

- **PD-3 (no bespoke live-SSE harness).** A three-container harness (built image plus Postgres plus a curl client on a private Docker network) could prove server-side SSE liveness without the blocked path, since container-to-container traffic is unaffected. It is deliberately not planned. 08-RESEARCH.md Assumption A2 and Pitfall 2 already evaluated and rejected building a real-backend test tier for this phase; the work (image build, setup-wizard auth, stack creation, DooD stacks-path wiring, SSE observation, teardown on a shared host) plausibly exceeds a single executor's context on its own; and it would still not answer the actual open question, which is whether the *browser* updates without a reload. This is the documented escalation path if UAT surfaces a real failure — not a silent omission.

- **PD-4 (`BLOCKED_STATES` stays unfixed, by evidence).** `StackActions`' `BLOCKED_STATES` omits `UPDATING`/`MIGRATING`, so Deploy stays clickable during those. 08-UI-SPEC.md marks this row "⚠ unresolved" and says to fix it only if the phase's own live-verification pass surfaces it as user-visible confusion; 08-RESEARCH.md Pitfall 4 warns against a silent drive-by fix and Assumption A1 logs it. Since the live pass cannot run in this session, there is no evidence either way — so it is surfaced as an explicit judgement question (V8) in Task 2's human-check instead of being fixed or dropped. A "yes, it confused me" answer promotes it to a follow-up todo.

- **PD-5 (todos close now, not after UAT).** The three todo files describe code gaps that are provably closed (08-RESEARCH.md cites the call sites). Holding them open until the live pass would leave STATE.md advertising fixed bugs as open indefinitely. Each closure records that live confirmation is tracked as this phase's UAT items, so a failed UAT reopens them through normal gap closure rather than leaving a falsely-closed record.

**Prohibitions carried from 08-RESEARCH.md's Anti-Patterns and Common Pitfalls** — a task doing any of these is a misread of the scope:
- Do not add, duplicate, or "fix" any SSE broadcast wiring. `StackService.transitionStatus()`, `BackupService`'s equivalent, `FileWatcher.handleFileChange()`/`handleEnvChange()`, and the client `stack_status`/`config_changed`/`config_error` branches all exist and are unit-tested.
- Do not add polling or a second broadcast mechanism to paper over an SSE gap — CLAUDE.md forbids polling for state available via SSE.
- Do not recolor the three maintenance states to blue; UI-SPEC reserves blue for user-initiated deploy/update actions.
- Do not touch `stack-list.tsx`'s hand-rolled config pills, and do not re-litigate whether `configError` and `configChanged` are independent (05.1-06 resolved that deliberately).
- Do not install any package. 08-RESEARCH.md's Package Legitimacy Audit records zero additions for this phase.
</planning_notes>

<source_audit>
## Multi-Source Coverage Audit

**GOAL** — ROADMAP Phase 8: "State that is already correct on the server shows up live in the UI, without the user needing to refresh"

| Item | Status | Where |
|---|---|---|
| GOAL-1 Correct server state is visible live in the UI | COVERED | Task 1 closes the last presentation gap (maintenance badges read as stuck); Task 2 produces the live proof that the shipped server-to-UI chain works |

**REQ** — `phase_req_ids` is the literal string "n/a — this phase is scoped by the todo list below, not by REQUIREMENTS.md IDs". No REQUIREMENTS.md IDs to cover. The ROADMAP's three scoped items are audited in their place:

| Item | Status | Where |
|---|---|---|
| R-1 `config_error` has no client-side UI indicator | COVERED | Code shipped 05.1-06; Task 2 item V6 proves it live; Task 3 closes the todo |
| R-2 Env file changes never set the config-changed badge | COVERED | Code shipped 05.1-02 and 05.1-06; Task 2 items V2 and V5; Task 3 closes the todo |
| R-3 Manual actions never broadcast SSE status updates | COVERED | Code shipped 05.1-02 and 05.1-04; Task 2 items V1, V3, V4; Task 3 closes the todo |

**RESEARCH** — `08-RESEARCH.md`

| Item | Status | Where |
|---|---|---|
| RES-1 Add `animate-pulse` to BACKING_UP/RESTORING/MIGRATING | COVERED | Task 1 |
| RES-2 New `stack-status-badge.test.tsx` (declared Wave 0 gap) | COVERED | Task 1 |
| RES-3 Close the six recorded `human_judgment: true` items | COVERED | Task 2 human-check V1-V6 |
| RES-4 Close the three todo files and update STATE.md's pending list | COVERED | Task 3 |
| RES-5 Re-probe reachability at execution time; live check if reachable, human fallback if not | COVERED | Task 2 action steps 1-3 |
| RES-6 `BLOCKED_STATES` gap | DEFERRED with source blessing | PD-4; RESEARCH Assumption A1 and Pitfall 4 and UI-SPEC's own "⚠ unresolved" all scope it out unless the live pass surfaces it; surfaced as V8 |
| RES-7 Anti-patterns (no new broadcast wiring, no polling, no recolor, no third pill style, no flag re-litigation) | COVERED | `<planning_notes>` prohibitions, restated in Task 1's action |
| RES-8 Zero package installs | COVERED | Threat T-08-SC |

**CONTEXT** — no CONTEXT.md (discuss-phase deliberately skipped). `08-UI-SPEC.md` (status: approved) is the decision source of record:

| Item | Status | Where |
|---|---|---|
| UI-1 `animate-pulse` only; keep gray/outline; never blue | COVERED | Task 1 action and acceptance criteria |
| UI-2 Zero new copy | COVERED | Task 1 changes no label and no message |
| UI-3 Lock the existing visual language; reuse shipped components, do not redesign | COVERED | `<planning_notes>` prohibitions |
| UI-4 Deploy not disabled during UPDATING/MIGRATING ("⚠ unresolved") | DEFERRED with source blessing | PD-4; surfaced as V8 |
| UI-5 Config-error pill shows a fixed label, not the message ("⚠ unresolved", acceptable as-is) | DEFERRED with source blessing | UI-SPEC states a hover-tooltip enhancement is explicitly not a Phase 8 requirement |

**Result: no unplanned items.** Every deferral above is deferred by the source artifact's own instruction, not by planner judgement.
</source_audit>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "a maintenance-state badge visibly signals work in progress" — one path only</name>

  <files>client/src/components/domain/stack/stack-status-badge.tsx, client/test/unit/components/domain/stack/stack-status-badge.test.tsx</files>

  <read_first>
    - client/src/components/domain/stack/stack-status-badge.tsx (all ~43 lines — both maps and the two fallbacks; this is the file being modified)
    - client/src/components/ui/badge.tsx (lines 29-46 — `Badge` merges its `className` prop through `cn()` onto a `<span>` carrying `data-slot="badge"` and `data-variant={variant}`; that span is what Testing Library's `getByText` resolves to, and `data-variant` is the stable handle for asserting the variant did not change)
    - client/test/unit/components/domain/stack/cert-status-badge.test.tsx (all 50 lines — the analog: same directory, same presentational status-to-badge shape, no mocks, no fixtures, one `it` per case; line 42's `messageEl.className).toContain(...)` is the class-assertion idiom to copy)
    - .planning/phases/08-live-state-consistency/08-PATTERNS.md (both file sections plus "Shared Patterns")
    - .planning/phases/08-live-state-consistency/08-UI-SPEC.md ("Color" section — the locked status-color table and the rationale for keeping the maintenance trio gray)
  </read_first>

  <precondition>The host is not under the load and memory pressure STATE.md records for [Phase 06-05], where an unrelated SonarQube/Immich/MySQL/Postgres workload mix at load average ~85 on 6 cores produced 17 spurious vitest failures in files that plan did not touch. Assert with `uptime` and `free -m` before the full-suite command in `<verify>`: if the 1-minute load average exceeds roughly four times the core count, or swap is nearly exhausted, halt and report rather than attributing unrelated red tests to this task. The narrow single-file test command is unaffected and may still be run.</precondition>

  <behavior>
    - Every key in `statusConfig` renders its mapped label: DRAFT renders "Draft", DEPLOYING "Deploying", RUNNING "Running", HEALTHY "Healthy", UNHEALTHY "Unhealthy", STOPPED "Stopped", ERROR "Error", UPDATING "Updating", BACKING_UP "Backing Up", RESTORING "Restoring", MIGRATING "Migrating".
    - The element returned by `getByText("Backing Up")` carries `data-slot="badge"` — proving the query resolves to the Badge element itself and not to some wrapper, so every class assertion below is made against the right node.
    - BACKING_UP: the badge's class list contains `animate-pulse`, its `data-variant` is `outline`, and its class list contains neither `bg-blue-500` nor `text-blue-700` — the motion cue is added without promoting it into the blue action bucket.
    - RESTORING: same three assertions, label "Restoring".
    - MIGRATING: same three assertions, label "Migrating".
    - DEPLOYING (regression lock): class list contains both `bg-blue-500/15` and `animate-pulse`, and `data-variant` is `default`.
    - UPDATING (regression lock): same as DEPLOYING with label "Updating".
    - RUNNING and HEALTHY: class list contains `bg-green-500/15` and does not contain `animate-pulse`.
    - ERROR and UNHEALTHY: class list contains `bg-red-500/15`, does not contain `animate-pulse`, and `data-variant` is `destructive`.
    - STOPPED and DRAFT: class list contains `bg-gray-500/15` and does not contain `animate-pulse`.
    - An unrecognized status renders the raw status string as its label, with `data-variant` `outline` and no `animate-pulse`.
  </behavior>

  <action>
In `client/src/components/domain/stack/stack-status-badge.tsx`:

1. Add exactly three entries to the `statusColors` map, after the existing `DRAFT` entry: `BACKING_UP`, `RESTORING`, and `MIGRATING`, each whose entire value is the single class string `animate-pulse` and nothing else. Per the UI-SPEC Color table these three keep the `outline` variant they already get from `statusConfig`, so no background, text, or border utility may appear in their value — adding one would visually promote a maintenance operation into the blue bucket the spec reserves for user-initiated deploy and update actions.
2. Change nothing else in this file. Leave `statusConfig` exactly as it is — it already carries the correct labels and the `outline` variant for all three. Leave the two existing blue entries byte-identical. Leave the component body, both `??` fallbacks, and the import untouched. Do not merge the two maps: 08-PATTERNS.md records the split as deliberate, and the split is precisely what this fix extends.
3. Add no comment. The three new entries are self-describing next to the two existing pulsing entries, and the file carries no comments today.

Create `client/test/unit/components/domain/stack/stack-status-badge.test.tsx`:

4. Follow `cert-status-badge.test.tsx` exactly for shape: the same three imports (`describe`/`expect`/`it` from vitest, `render`/`screen` from `@testing-library/react`, the component via the `@/` alias), one `describe` block, one `it` per bullet in this task's `<behavior>`, no mocks, no fixtures, no `beforeEach`. The component has zero external dependencies, so a bare `render()` plus `screen.getByText()` is sufficient.
5. Assert Tailwind utilities with `expect(badge.className).toContain(...)` and `.not.toContain(...)` rather than snapshotting the whole class string, so the tests survive unrelated class reordering by `tailwind-merge`. Assert the variant with `expect(badge).toHaveAttribute("data-variant", ...)` rather than by looking for variant-specific utility classes — the `outline` variant's own class string contains a hover background utility, so a broad background-class assertion would give a false reading.
6. Give the labels-render cases a single `it` per status or a small table-driven `it.each`, whichever reads better; give each of the three maintenance states, each of the two blue states, and the unknown-status fallback its own named `it` so a failure names the exact status that broke.
7. Name the tests in this file's house style, starting with "renders", "applies", or "keeps" — for example one that applies the motion cue to a maintenance state without changing its color, and one that keeps the two action states blue and pulsing as a regression lock.
  </action>

  <verify>
    <automated>yarn workspace @docktor/client test test/unit/components/domain/stack/stack-status-badge.test.tsx</automated>
    <fails_when>non-zero exit, or the summary line reports anything other than "Test Files  1 passed", or the output contains "No test files found" — which would mean the new file landed outside client/vitest.config.ts's `test/unit/**/*.test.{ts,tsx}` include pattern</fails_when>

    <automated>yarn workspace @docktor/client test</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 23 passed test files, or any non-zero "failed" count — the measured baseline immediately before this task is "Test Files 22 passed (22)" with exit 0</fails_when>

    <automated>yarn typecheck</automated>
    <fails_when>non-zero exit, or any line containing "error TS" — the measured baseline immediately before this task is a silent exit 0 with no output</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -c '"animate-pulse"' client/src/components/domain/stack/stack-status-badge.tsx` prints 3 — one per maintenance state, each of whose entire value is that single utility.
    - `grep -c 'BACKING_UP: "animate-pulse"' client/src/components/domain/stack/stack-status-badge.tsx` prints 1, and the same command with `RESTORING` and with `MIGRATING` each prints 1.
    - `grep -c 'bg-blue-500/15 text-blue-700 border-blue-500/25 animate-pulse' client/src/components/domain/stack/stack-status-badge.tsx` prints 2 — DEPLOYING and UPDATING keep their exact existing strings.
    - `grep -c 'statusConfig' client/src/components/domain/stack/stack-status-badge.tsx` prints 2 — the declaration and its one use inside the component; the label/variant map was not restructured.
    - `grep -c 'data-variant' client/test/unit/components/domain/stack/stack-status-badge.test.tsx` prints at least 6 — the three maintenance states, the two blue states, and the unknown-status fallback each assert their variant.
    - `git status --porcelain` lists exactly `client/src/components/domain/stack/stack-status-badge.tsx` and `client/test/unit/components/domain/stack/stack-status-badge.test.tsx`, and no other path.
    - Every bullet in this task's `<behavior>` exists as a passing assertion in the new test file.
  </acceptance_criteria>

  <reversibility rating="reversible">Three entries in an existing lookup map plus one new test file; deleting both restores today's behavior exactly, and nothing else in the codebase reads the added keys.</reversibility>

  <done>A badge rendered for BACKING_UP, RESTORING, or MIGRATING carries `animate-pulse` and still reports `data-variant="outline"` with no blue utility, DEPLOYING and UPDATING are unchanged, an unknown status still falls through to a plain outline badge, and all three are proven by a runnable single-file test command with the full client suite and the monorepo typecheck both green.</done>
</task>

<task type="auto">
  <name>Task 2: Settle this session's live reachability, then hand the six stranded verification items to the phase's UAT</name>

  <files>.planning/phases/08-live-state-consistency/08-01-SUMMARY.md</files>

  <read_first>
    - .planning/phases/05.1-stabilization-fix-blockers-and-majors-surfaced-during-testin/05.1-02-SUMMARY.md (the D6 entry — its `description` and `rationale` are the source wording for verification items V1 and V2)
    - .planning/phases/05.1-stabilization-fix-blockers-and-majors-surfaced-during-testin/05.1-04-SUMMARY.md (the D7 entry — source wording for V3 and V4)
    - .planning/phases/05.1-stabilization-fix-blockers-and-majors-surfaced-during-testin/05.1-06-SUMMARY.md (the D9 entry — source wording for V5 and V6)
    - .planning/STATE.md (`### Blockers/Concerns` — the repeated TCP-to-Docker-published-port entries for 05.1-01/05/06 and 06-01/06-07, and the [Phase 05.1-03] shared-host incident that constrains every docker verb this task may run)
    - .planning/phases/08-live-state-consistency/08-RESEARCH.md ("Common Pitfalls" 2, "Open Questions" 1, "Environment Availability")
    - docker-compose.dev.yml (the dev database service, its container name, published port, and the credentials that are already public in this tracked file)
  </read_first>

  <precondition>The Docker daemon answers through its socket and the dev database container is up, so a non-response from the probe below can only mean the payload block and not a stopped database. Assert with `docker info` (expect exit 0) and `docker ps --filter name=docktor-db-dev --format '{{.Names}}'` (expect the container name). If the container is not running, start it with `docker compose -f docker-compose.dev.yml up -d` — that file declares only a bare Postgres, is its own Compose project, and touches nothing else on this host — then re-assert before probing. If the daemon itself is unreachable, halt and report: the probe's verdict would be unattributable.</precondition>

  <action>
This task settles one question and then writes down six others. It writes no source code.

1. Probe the daemon: run `docker info`, capture the exit code and whether the output names a server version. Record both verbatim.

2. Probe the payload path at both addresses — the published port on loopback first, then the container's own bridge address from `docker inspect` — because a reachable published port and a reachable bridge address fail for different reasons and only both together identify the limitation. Use the exact command in this task's `<verify>`: it opens a TCP connection to port 5432, writes the 8-byte Postgres SSLRequest frame, and prints one line per address carrying that address, the exit code, and how many bytes came back. Three readings are meaningful and no others: `rc=0 bytes=1` means the database answered; `rc=124 bytes=0` means the connection opened but the payload was never answered within the 10-second cap; a non-zero rc other than 124 with `bytes=0` means the connection itself was refused or errored. Record both lines verbatim — they are the evidence, not a pass/fail gate.

3. Branch on the verdict.

   Branch A — either address printed `rc=0 bytes=1`. The limitation STATE.md records has lifted for this session. Apply the schema with `yarn db:push`, start `yarn dev` in the background, wait for the client dev server to answer, and then drive as much of the verification below as is achievable without a browser. The highest-value headless substitute, in priority order: hold two concurrent `GET /api/events` SSE connections and confirm both receive the same `stack_status` frame after one deploy is triggered (this is the transport-level half of V1 — it proves the broadcast reaches every subscriber, not only the acting one); then edit a watched `.env` on disk and confirm a `config_changed` frame arrives on both; then write a YAML syntax error into a watched compose file and confirm a `config_error` frame arrives. Cap the attempt hard: if any step needs more than one retry, stop, shut down the background processes you started, and record exactly where it stopped. Do not debug the environment inside this task, and do not let a headless success cause you to drop the corresponding human item — the DOM half is still unproven.

   Branch B — neither address printed `rc=0 bytes=1`. This is the expected outcome and the same limitation STATE.md records for 05.1-01, 05.1-05, 05.1-06, 06-01, and 06-07. Do not attempt the live pass at all: the server cannot reach Postgres, so `yarn dev` cannot boot and no page can be loaded. Record the two probe tokens as the evidence that the limitation still holds, and leave every item below to the human.

4. Obey these prohibitions in either branch. STATE.md's [Phase 05.1-03] entry records a real incident on this exact host where a live `docker compose up --remove-orphans` stopped and removed unrelated running production containers before the project-name collision was noticed. So: never run `docker compose up` against this repository's own `docker-compose.yml`, never pass `--remove-orphans` to anything, never create or remove a container beyond the dev database service the precondition may start, and never use a plain or example-derived project name. The read-only verbs plus `docker exec` into the already-running dev database container are the entire allowed surface. Separately, do not read or quote `.env.development`: workspace permission settings deny it (STATE.md [Phase 05.1-10]) and the dev credentials are already public in the tracked `docker-compose.dev.yml`, so nothing needs it.

5. Do not build a new automated test tier for any of this. 08-RESEARCH.md Pitfall 2 establishes with file:line citations that `client/test/integration/fixtures.ts` aborts every unstubbed `/api/**` request and stubs `/api/events` with an empty `text/event-stream` body, and that Playwright's `webServer` config starts only the Vite dev server and never the Fastify API. A spec in that hermetic tier cannot observe a real SSE round trip, and replacing the tier is out of proportion for a phase whose only code change is a CSS class. Planner decision PD-3 records this as the escalation path if UAT fails, not as work to attempt here.

6. Write into `08-01-SUMMARY.md`: the two probe tokens verbatim, the branch taken and why, any evidence captured in branch A, and the six verification items restated with the 05.1 D-item each one closes — so the phase verifier harvests a complete, self-contained script rather than a pointer to three other summaries.
  </action>

  <verify>
    <automated>timeout 20 docker info > /tmp/docktor-08-daemon.log 2>&1; echo "docker_rc=$?"; grep -c 'Server Version' /tmp/docktor-08-daemon.log</automated>
    <fails_when>the docker_rc line prints anything other than 0, or the grep -c line prints 0 — the daemon is unreachable, so a silent payload probe could not be attributed to the documented block rather than to a stopped database</fails_when>

    <automated>DB_IP=$(timeout 20 docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}} {{end}}' docktor-db-dev | awk '{print $1}'); for ADDR in 127.0.0.1 "$DB_IP"; do timeout 10 bash -c "exec 3<>/dev/tcp/$ADDR/5432; printf '\x00\x00\x00\x08\x04\xd2\x16\x2f' >&3; head -c 1 <&3" > /tmp/docktor-08-pg-$ADDR.out 2>/dev/null; echo "$ADDR rc=$? bytes=$(wc -c < /tmp/docktor-08-pg-$ADDR.out)"; done</automated>
    <fails_when>either address line prints neither `rc=0 bytes=1` (the database answered the SSLRequest frame, so branch A applies) nor `rc=124 bytes=0` (the connection opened but never answered, so branch B applies) nor a non-zero rc with `bytes=0` from a refused connection — any other combination leaves the reachability verdict unsettled, which is the single outcome this probe exists to prevent. Both addresses printed `rc=124 bytes=0` when measured during planning.</fails_when>

    <automated>grep -c -E 'DB_PAYLOAD|rc=124|rc=0 bytes=1' .planning/phases/08-live-state-consistency/08-01-SUMMARY.md</automated>
    <fails_when>prints 0 — the summary does not record the probe's own output, so a later reader cannot tell whether the live checks were skipped by evidence or by assumption</fails_when>

    <human-check>
**Why this is a human step:** during planning, `docker info` succeeded and `docker exec docktor-db-dev psql -U docktor -d docktor -c 'select 1'` returned a row, but a Postgres SSLRequest frame written to `127.0.0.1:5432` and to the container's own bridge address `172.19.0.2:5432` both went unanswered for 10 seconds. The database is healthy; the agent's network namespace cannot complete a TCP payload exchange with it. The dev server therefore cannot boot here and no page can be loaded. This is the same limitation STATE.md records for 05.1-01, 05.1-05, 05.1-06, 06-01, and 06-07. Task 2's `<verify>` re-measures it; if it reports `rc=0 bytes=1` the executor will have already run the headless half and said so in the summary.

**Setup (about five minutes, on your own terminal where the block does not apply):**

S1. Start the dev database: `docker compose -f docker-compose.dev.yml up -d` — this file declares only a bare Postgres 18 as container `docktor-db-dev` in its own Compose project, and runs no Docktor container. If it is already running, nothing to do.

S2. Apply the schema: `yarn db:push`. STATE.md [Phase 05.1-06] records that `Stack.configError` and `Stack.lastEnvHash` could never be pushed to this dev database from an agent session — until this runs, V5 and V6 below will silently do nothing.

S3. Start the app: `yarn dev`, then open http://localhost:5173 (Vite proxies `/api` to the Fastify server on 3000). Note the stacks directory the server logs at boot — that is where V5 and V6 edit files.

S4. Have one stack deployed and running, and a backup repository configured in Settings. V3 and V4 need both.

**The six items, each closing a verification the 05.1 sessions recorded but could not execute:**

V1 — closes 05.1-02 D6, first half. Open the same stack's detail page in two browser tabs. Press Deploy in tab A. Expected: both tabs show the Deploying badge while the deploy runs, with no manual refresh anywhere in tab B, and both return to the running state when it finishes.

V2 — closes 05.1-02 D6, second half. With both tabs still open, edit and save the compose text in tab A's Compose tab, then do the same on the Env tab. Expected: the yellow "Configuration has changed since last deployment. Re-deploy to apply changes." banner appears in tab B without a reload, for both the compose save and the env save.

V3 — closes 05.1-04 D7, first half. In Settings, clear the backup repository configuration. On a running stack, press Backup Now. Expected: an error toast that names the missing repository, the stack's status badge does not change, and the Backups tab gains no new row.

V4 — closes 05.1-04 D7, second half, and exercises this plan's Task 1. Restore the backup repository configuration and run a backup. Expected: the status badge moves to "Backing Up" and back to its previous status live with no page reload — and while it reads "Backing Up" the badge visibly pulses, fading in and out exactly the way "Deploying" does, while staying gray/outline rather than turning blue.

V5 — closes 05.1-06 D9, first half. With the stack running, edit its `.env` file directly on disk in the stacks directory from S3, outside the app. Expected: within the file watcher's detection window the config-changed indicator appears with no reload — both the yellow pill in the stack list and the yellow banner on the detail page.

V6 — closes 05.1-06 D9, second half. Introduce a YAML syntax error into that stack's `docker-compose.yml` on disk. Expected: a red indicator appears with no reload — a "config error" pill in the stack list and a red banner on the detail page carrying the parser's own message — and fixing the file clears both.

**Two judgement calls this pass exists to settle:**

V7 — Does the pulsing "Backing Up" badge read as "work in progress" rather than "stuck"? And is keeping it gray, rather than blue like "Deploying", the right distinction between a maintenance operation and a user-initiated action — or would you rather they look the same? "Leave it as shipped" is a valid answer and is recorded as a decision.

V8 — While a stack is Updating or Migrating, the Deploy button stays clickable; the server rejects the action and you get an error toast, so nothing unsafe happens. Did that actually confuse you during the passes above? 08-UI-SPEC.md marks this row "⚠ unresolved" and says to fix it only if this pass surfaces it. Answering "no, leave it" closes that row; answering "yes" promotes it to a follow-up todo rather than a silent drive-by fix.

Report per item: pass or fail, and what you saw.
    </human-check>
  </verify>

  <acceptance_criteria>
    - `08-01-SUMMARY.md` quotes both probe lines verbatim and states which branch was taken and why, in the branch's own terms rather than by citing this plan.
    - `08-01-SUMMARY.md` restates all six verification items V1-V6 with the 05.1 D-item each closes, plus V7 and V8, so the script is self-contained for the phase verifier.
    - If branch A was taken, the summary records the headless evidence captured and states explicitly which parts of V1-V6 remain unproven because no browser DOM was observed.
    - If branch B was taken, the summary states that no live attempt was made and names the STATE.md entries documenting the same limitation.
    - `git status --porcelain` lists no file under `client/` or `server/` as changed by this task.
    - No container was created or removed beyond the dev database service, and no `docker compose` command in the transcript targets this repository's own `docker-compose.yml` or passes `--remove-orphans`.
  </acceptance_criteria>

  <reversibility rating="reversible">Read-only probes plus a written record; no source file, schema, or running service is mutated that a `docker compose -f docker-compose.dev.yml down` would not undo.</reversibility>

  <done>The question 08-RESEARCH.md left open — can this session run the live checks itself — is answered by a recorded measurement rather than an assumption, and all six previously-stranded human-judgment items plus two judgement calls exist as one self-contained script the phase verifier can harvest into UAT.</done>
</task>

<task type="auto">
  <name>Task 3: Close the three stale todos and make STATE.md tell the truth about them</name>

  <files>.planning/todos/completed/2026-08-28-config-error-ui-indication-missing.md, .planning/todos/completed/2026-08-28-env-file-changes-dont-flag-config-changed.md, .planning/todos/completed/2026-08-28-manual-actions-dont-broadcast-sse.md, .planning/STATE.md</files>

  <read_first>
    - .planning/todos/pending/2026-08-28-config-error-ui-indication-missing.md (all of it — the Problem section describes the pre-05.1 state and is what the resolution must answer)
    - .planning/todos/pending/2026-08-28-env-file-changes-dont-flag-config-changed.md (all of it — two separately-described gaps, both now closed)
    - .planning/todos/pending/2026-08-28-manual-actions-dont-broadcast-sse.md (all of it)
    - .planning/todos/completed/2026-08-28-backup-without-config-wedges-stack.md (frontmatter lines 1-11 — the shape a completed todo carries after closure: `completed:` plus `status: completed` appended to the existing block)
    - .planning/STATE.md (the `### Pending Todos` bullets naming these three files, and the `- [Phase 02]:` entry under `### Blockers/Concerns` that still describes config_error as open; note the existing "RESOLVED <date> (commit <sha>)" annotation convention used by the Phase 02 entry immediately above it)
    - .planning/phases/08-live-state-consistency/08-RESEARCH.md ("Phase Requirements" table — the file:line citations each resolution must name)
  </read_first>

  <precondition>The GSD tools shim resolves and all three todos are still filed as pending, so the closure mechanism has something to act on. Assert with `gsd_run query todo complete 2026-08-28-config-error-ui-indication-missing.md --dry-run` and expect a payload carrying `dry_run: true` and a `would_move` target under `.planning/todos/completed/`. If the shim does not resolve or the dry run reports the file missing, halt and report rather than hand-moving the files — a plain `git mv` would relocate them without the `completed`/`status` frontmatter stamp that every other closed todo in this project carries.</precondition>

  <action>
1. Close each of the three todos with `gsd_run query todo complete <filename>`, one call per file, using the bare filenames (no directory prefix): the config-error UI indication file, the env-file config-changed file, and the manual-actions SSE broadcast file, all dated 2026-08-28. This verb is the project's single closure mechanism: it moves the file into `.planning/todos/completed/` and stamps `completed: <today>` plus `status: completed` into the existing frontmatter block. Do not hand-move any of them and do not hand-edit the frontmatter.

2. Append a `## Resolution` section to each of the three now-completed files, after their existing content, in the same plain prose register the files already use. Each resolution must name three things: the plan that closed the code gap, the specific call sites that close it (take the file:line citations from 08-RESEARCH.md's Phase Requirements table rather than re-deriving them), and the fact that live confirmation is tracked as this phase's UAT items rather than being claimed here. Per file: the config-error one points at 05.1-06 and the `FileWatcher.handleFileChange()` catch branch plus the client `use-stack`/`use-stacks` handling and the detail-page alert and list pill, and names V6; the env-file one points at 05.1-02 and 05.1-06 for both of its two described gaps — the app-driven write path in `StackService.updateStack()`'s env branch and the external-edit path via `FileWatcher.handleEnvChange()` with its own `lastEnvHash` — and names V5 and V2; the manual-actions one points at 05.1-02's `StackService.transitionStatus()` single-choke-point wrapper and 05.1-04's equivalent in `BackupService`, notes that the addendum gap about the detail page not refetching after a compose or env save is also closed, and names V1, V3, and V4. Each resolution must also state, in one sentence, that a failing UAT reopens the item through normal gap closure — the closure is a claim about the code, not about the live behavior.

3. In `.planning/STATE.md`'s `### Pending Todos` list, remove the three bullets naming these files and nothing else. The list currently carries 26 bullets against 22 files on disk, so it is already loose — do not attempt to reconcile the other stale entries here, that is outside this phase's scope and would make the diff unreviewable.

4. In `.planning/STATE.md`'s `### Blockers/Concerns`, the `- [Phase 02]:` entry describing config_error as still having no client-side indicator is now false and points at a path that no longer exists. Annotate it in place using the convention the neighbouring Phase 02 entry already uses — an inline "RESOLVED" clause naming when and where — recording that the client handling and both indicators shipped in 05.1-06, that the todo was closed in Phase 08, and updating its trailing path reference to the file's new location under `.planning/todos/completed/`. Annotate; do not delete the entry. This ledger's value is that it shows what was believed when.

5. Change nothing else in STATE.md. Its frontmatter, `## Current Position`, `## Performance Metrics`, and `### Decisions` are written by other GSD machinery and must not be touched by this task.
  </action>

  <verify>
    <automated>ls .planning/todos/pending/*.md | wc -l</automated>
    <fails_when>prints anything other than 19 — the measured count before this task is 22, so any other value means either fewer than three todos were closed or an unrelated pending todo was collaterally removed</fails_when>

    <automated>ls .planning/todos/completed/ | grep -E '2026-08-28-(config-error-ui-indication-missing|env-file-changes-dont-flag-config-changed|manual-actions-dont-broadcast-sse)\.md' | wc -l</automated>
    <fails_when>prints anything other than 3 — at least one of the three did not land in the completed directory</fails_when>

    <automated>grep -l 'status: completed' .planning/todos/completed/2026-08-28-config-error-ui-indication-missing.md .planning/todos/completed/2026-08-28-env-file-changes-dont-flag-config-changed.md .planning/todos/completed/2026-08-28-manual-actions-dont-broadcast-sse.md | wc -l</automated>
    <fails_when>prints anything other than 3 — a file was moved without the frontmatter stamp, which is what hand-moving instead of using the closure verb produces</fails_when>

    <automated>grep -l '^## Resolution' .planning/todos/completed/2026-08-28-config-error-ui-indication-missing.md .planning/todos/completed/2026-08-28-env-file-changes-dont-flag-config-changed.md .planning/todos/completed/2026-08-28-manual-actions-dont-broadcast-sse.md | wc -l</automated>
    <fails_when>prints anything other than 3 — a todo was closed with no record of what closed it</fails_when>

    <automated>awk '/^### Pending Todos/{f=1;next} /^### /{f=0} f&&/^- \[/{c++} END{print c+0}' .planning/STATE.md</automated>
    <fails_when>prints anything other than 23 — the measured count before this task is 26, so any other value means either fewer than three bullets were removed or an unrelated bullet was lost</fails_when>

    <automated>grep -c 'todos/completed/2026-08-28-config-error-ui-indication-missing.md' .planning/STATE.md</automated>
    <fails_when>prints 0 — the Phase 02 blocker entry still points at the todo's old location, so STATE.md carries a dangling path</fails_when>

    <automated>yarn workspace @docktor/client test test/unit/components/domain/stack/stack-status-badge.test.tsx</automated>
    <fails_when>non-zero exit, or the summary line reports anything other than "Test Files  1 passed" — this task must not have disturbed Task 1's result</fails_when>
  </verify>

  <acceptance_criteria>
    - `.planning/todos/pending/` no longer holds any of the three files, and `.planning/todos/completed/` holds all three with both `completed:` and `status: completed` in their frontmatter.
    - Each of the three completed files carries a `## Resolution` section naming the closing plan, the specific call sites, the UAT items that will confirm the live behavior, and the sentence stating a failing UAT reopens the item.
    - The `### Pending Todos` list lost exactly three bullets and nothing else; `git diff .planning/STATE.md` shows no change outside `### Pending Todos` and the one annotated `- [Phase 02]:` blocker line.
    - The annotated Phase 02 blocker entry still contains its original text describing what was believed at the time, with the resolution added rather than substituted.
    - `git status --porcelain` lists no file under `client/` or `server/` as changed by this task.
  </acceptance_criteria>

  <reversibility rating="reversible">File moves and documentation edits, all inside git; `git revert` restores both the pending files and the STATE.md text exactly.</reversibility>

  <done>None of the three todos is filed as pending, each records in its own file which plan closed its code gap and which UAT item will confirm the live behavior, and STATE.md neither advertises them as open nor points at a path that no longer exists.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

| Symbol / artifact | Kind | Location |
|---|---|---|
| `statusColors.BACKING_UP`, `statusColors.RESTORING`, `statusColors.MIGRATING` | three entries in an existing module-private map, each valued exactly `animate-pulse` | `client/src/components/domain/stack/stack-status-badge.tsx` |
| `describe("StackStatusBadge")` | new unit test block covering every status, both blue regression locks, and the unknown-status fallback | `client/test/unit/components/domain/stack/stack-status-badge.test.tsx` |
| `/tmp/docktor-08-daemon.log`, `/tmp/docktor-08-pg-<addr>.out` | captured probe evidence, ephemeral, quoted into the summary | not committed |
| Reachability verdict and branch taken | execution-environment evidence | `.planning/phases/08-live-state-consistency/08-01-SUMMARY.md` |
| Verification script V1-V6 plus judgement calls V7 and V8 | human-check block harvested into the phase UAT | `08-01-SUMMARY.md` and this plan's Task 2 `<verify>` |
| Three closed todos with `## Resolution` sections | project bookkeeping | `.planning/todos/completed/2026-08-28-*.md` |
| Corrected Pending Todos list and annotated Phase 02 blocker entry | project state | `.planning/STATE.md` |

This plan introduces no server symbol, no route, no schema field, and no package.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server status enum -> browser DOM | `stack.status` arrives over REST and SSE and is used as a lookup key into two module-private maps; the resulting class string is rendered into a `class` attribute. |
| agent process -> shared Docker host | Task 2's probes run on a host that also runs unrelated real production workloads (STATE.md [Phase 05.1-03] records a live incident there). |
| probe output -> committed summary | Captured command output is quoted into a file that is committed and read later. |
| planning metadata -> future readers | Task 3 closes bug records whose live behavior is not yet human-confirmed. |

## Applicable ASVS L1 Categories

| ASVS Category | Applies | Reason |
|---------------|---------|--------|
| V2 Authentication | No | No auth code path is read or written; the SSE endpoint's `requireAuth` gate is pre-existing and untouched. |
| V3 Session Management | No | No session, cookie, or token handling is involved. |
| V4 Access Control | No | No route is added or modified; no authorization decision changes. |
| V5 Input Validation | No | No new user input surface. The one value crossing into render is a server-controlled status enum used as a map key; an unrecognized value falls through to an empty class string and a React-escaped text label, so it can neither inject an attribute nor select an unintended class. |
| V6 Cryptography | No | Nothing is encrypted, signed, hashed, or stored. |

Rationale: this plan changes one presentational lookup map, runs read-only probes, and edits planning metadata. There is no new attack surface to analyze, which is why the register below is dominated by operational rather than request-time threats.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Denial of Service | Task 2 probes on a shared Docker host | high | mitigate | STATE.md [Phase 05.1-03] records a live `docker compose up --remove-orphans` on this exact host stopping and removing unrelated running production containers. Task 2's action step 4 restricts the entire allowed surface to `docker info`, `docker ps`, `docker inspect`, `docker exec` into the already-running dev database, and at most `docker compose -f docker-compose.dev.yml up -d` (its own project, one bare Postgres); `--remove-orphans`, this repository's own `docker-compose.yml`, and plain project names are forbidden outright, and the acceptance criteria require confirming no container was created or removed beyond that service. |
| T-08-02 | Information Disclosure | probe output quoted into a committed summary | medium | mitigate | `.env.development` must not be read or quoted — workspace permission settings deny it (STATE.md [Phase 05.1-10]) and the dev credentials are already public in the tracked `docker-compose.dev.yml`, so nothing in this plan needs it. The summary records only the probe's own tokens, exit codes, byte counts, the port, and the container's bridge address. |
| T-08-03 | Tampering | `animate-pulse` reaching the DOM via `Badge`'s `className` pass-through | low | accept | The class string is selected by an exact key lookup into a module-private literal map, never concatenated from input; an unrecognized status yields an empty string and renders the raw value as React-escaped text. No attacker-controlled substring can reach the `class` attribute. |
| T-08-04 | Repudiation | closing three bug records whose live behavior is not yet human-confirmed | medium | mitigate | Each `## Resolution` names the plan and call sites that closed the code gap, states that live confirmation is tracked as this phase's UAT items V1-V6, and states that a failing UAT reopens the item through normal gap closure — so the record cannot later be read as a claim nobody made. Planner decision PD-5 records why they close now rather than after UAT. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero packages are installed: 08-RESEARCH.md's Package Legitimacy Audit records the gate as not applicable with zero additions, and every library touched is already declared and version-pinned. Any package install appearing during execution is out of plan — stop and require a blocking human legitimacy checkpoint before installing. |
</threat_model>

<verification>
Plan-level checks, run from the repository root after all three tasks:

1. `yarn workspace @docktor/client test` — 23 test files passed, exit 0. The measured baseline immediately before this plan was 22 files passed, exit 0, so the delta is exactly the new file.
2. `yarn typecheck` — silent exit 0, matching the measured pre-plan baseline.
3. `git diff --name-only` plus `git status --porcelain` together list exactly: the badge component, the new badge test, three renames into `.planning/todos/completed/`, `.planning/STATE.md`, and `08-01-SUMMARY.md`. Nothing under `server/`, nothing under `client/src/routes/`, nothing under `client/test/integration/`.
4. `ls .planning/todos/pending/*.md | wc -l` prints 19 (was 22).

Known environment limitation, measured during planning and re-measured by Task 2: a TCP payload to the dev database gets no response from this agent's network namespace at either `127.0.0.1:5432` or the container's bridge address, while `docker info` and `docker exec ... psql` both succeed. The database is healthy and the Docker socket works; only TCP payload exchange is blocked. This is the same limitation STATE.md records for 05.1-01, 05.1-05, 05.1-06, 06-01, and 06-07. The dev server therefore cannot boot here, so the six live items are handed to the phase's UAT rather than proven in-session — matching 05.1's own precedent, whose four human_verification items were confirmed by the user on 2026-09-11.

Escalation path if UAT fails on any of V1-V6: do not patch inside this plan. Report the observed behavior, reopen the corresponding todo through gap closure, and reconsider planner decision PD-3 — a container-internal SSE harness becomes proportionate once there is evidence of a real failure, which is exactly the condition under which its cost is justified.
</verification>

<success_criteria>
- BACKING_UP, RESTORING, and MIGRATING badges carry `animate-pulse`, still report `data-variant="outline"`, and carry no blue utility.
- DEPLOYING and UPDATING keep their exact existing blue-plus-pulse strings; no other status gains motion; an unknown status still renders its raw label in a plain outline badge.
- A new unit test file regression-locks the whole status palette, and both it and the full client suite pass with the monorepo typecheck green.
- This session's ability to run the live checks is settled by a recorded probe result, and the branch taken is stated in the summary.
- All six previously-stranded human-judgment items from 05.1-02 D6, 05.1-04 D7, and 05.1-06 D9 exist as one self-contained script, each naming the D-item it closes, plus two judgement calls covering the new pulse and the deferred `BLOCKED_STATES` row.
- The three scoped todos are closed with stamped frontmatter and a resolution naming what closed them; STATE.md neither lists them as pending nor points at their old paths.
- No package was installed, no SSE wiring was added or duplicated, no polling was introduced, and `stack-list.tsx`, `stack-actions.tsx`, and every server file are untouched.
</success_criteria>

<output>
Create `.planning/phases/08-live-state-consistency/08-01-SUMMARY.md` when done.
</output>