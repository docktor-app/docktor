---
phase: 08-live-state-consistency
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/domain/stack/stack-status-badge.tsx
  - client/test/unit/components/domain/stack/stack-status-badge.test.tsx
autonomous: true
gap_closure: true
gap_ids: [G-08-7]
requirements:
  - "Closes UAT gap G-08-7 (cosmetic, judgement call V7) diagnosed in 08-UAT.md: the user's live answer to plan 08-01's own judgement question was \"change it also to blue,\" overriding 08-UI-SPEC.md's earlier gray-only lock for BACKING_UP specifically."

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "A stack whose status is BACKING_UP renders the exact same blue-plus-pulse badge treatment as DEPLOYING and UPDATING, per the user's live UAT answer to V7."
    - "RESTORING and MIGRATING keep their existing gray-plus-pulse treatment, unchanged — the UAT gap and its root_cause explicitly scope the fix to BACKING_UP only."
  artifacts:
    - "client/src/components/domain/stack/stack-status-badge.tsx — statusConfig.BACKING_UP.variant changes from \"outline\" to \"default\"; statusColors.BACKING_UP changes from \"animate-pulse\" to the exact DEPLOYING/UPDATING blue-plus-pulse string"
    - "client/test/unit/components/domain/stack/stack-status-badge.test.tsx — the BACKING_UP-specific test (written in plan 08-01) now asserts the blue treatment and default variant instead of the gray/outline one it locked down before this UAT answer existed"
  key_links:
    - "statusConfig[status].variant + statusColors[status] -> Badge's className prop -> cn(badgeVariants({variant}), className) on the rendered span -> data-variant and class attributes a live user sees — same mechanism plan 08-01 already shipped and tested, only the two BACKING_UP map entries change"
---

<objective>
Close UAT gap G-08-7 (cosmetic): plan 08-01 shipped `BACKING_UP`'s `animate-pulse` motion cue while deliberately keeping it gray/`outline`, per 08-UI-SPEC.md's locked color table reserving blue for user-initiated `DEPLOYING`/`UPDATING` actions. When the phase's own live UAT pass asked the judgement question this decision anticipated (V7: "is gray, not blue, the right distinction?"), the user's answer was explicit: "change it also to blue." This plan implements that live answer, superseding the UI-SPEC's original lock for `BACKING_UP` specifically — `RESTORING` and `MIGRATING` are unaffected; the gap's own root_cause and missing items name only `BACKING_UP`.

Purpose: the badge a user actually watches during a live backup should read using the same visual language as every other in-progress action, per their own stated preference, rather than the color scheme a pre-launch spec guessed at.

Output: `BACKING_UP` renders with the identical `default` variant and `bg-blue-500/15 text-blue-700 border-blue-500/25 animate-pulse` treatment `DEPLOYING` and `UPDATING` already use; `RESTORING` and `MIGRATING` are untouched.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-live-state-consistency/08-UAT.md
@client/src/components/domain/stack/stack-status-badge.tsx
@client/test/unit/components/domain/stack/stack-status-badge.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote BACKING_UP to the blue-plus-pulse treatment, per the user's live V7 answer</name>

  <files>client/src/components/domain/stack/stack-status-badge.tsx, client/test/unit/components/domain/stack/stack-status-badge.test.tsx</files>

  <read_first>
    - client/src/components/domain/stack/stack-status-badge.tsx (all ~46 lines — both maps in full)
    - client/test/unit/components/domain/stack/stack-status-badge.test.tsx (all of it — this is plan 08-01's own test file; the "applies animate-pulse to BACKING_UP without promoting it to the blue action color" test at lines 31-38 is the one this task must replace, since it currently locks down the exact opposite of what the user asked for)
  </read_first>

  <behavior>
    - `BACKING_UP` renders with `data-variant="default"`, and its class list contains both `bg-blue-500/15` and `animate-pulse` — identical to `DEPLOYING`'s and `UPDATING`'s existing regression-lock assertions.
    - `RESTORING` still renders with `data-variant="outline"`, its class list still contains `animate-pulse` and still does not contain `bg-blue-500` or `text-blue-700` — unchanged from plan 08-01.
    - `MIGRATING` still renders with `data-variant="outline"`, same unchanged assertions as `RESTORING`.
    - `DEPLOYING` and `UPDATING` remain exactly as plan 08-01 left them (regression lock, unchanged).
    - The unknown-status fallback, the green/red/gray groups, and the label-rendering table are all unchanged from plan 08-01.
  </behavior>

  <action>
1. In `client/src/components/domain/stack/stack-status-badge.tsx`'s `statusConfig` map, change the `BACKING_UP` entry's `variant` from `"outline"` to `"default"` — the label `"Backing Up"` stays exactly as-is. Do not touch `RESTORING` or `MIGRATING`'s entries in this map; both keep `variant: "outline"`.

2. In the same file's `statusColors` map, change the `BACKING_UP` entry from `"animate-pulse"` to the exact string `"bg-blue-500/15 text-blue-700 border-blue-500/25 animate-pulse"` — byte-identical to `DEPLOYING`'s and `UPDATING`'s existing values in the same map, so a future reader sees three identical strings for the three blue-pulsing statuses rather than a near-duplicate. Do not touch `RESTORING` or `MIGRATING`'s entries; both keep the single-utility value `"animate-pulse"`.

3. Change nothing else in this file — the component body, both `??` fallbacks, and the import are untouched.

4. In `client/test/unit/components/domain/stack/stack-status-badge.test.tsx`, replace the test `"applies animate-pulse to BACKING_UP without promoting it to the blue action color"` (the one whose assertions are now factually wrong) with a new test asserting the opposite, in the same style as the existing `"keeps DEPLOYING blue and pulsing (regression lock)"` test immediately below it: render `status="BACKING_UP"`, get the badge by its `"Backing Up"` text, and assert its class list contains `bg-blue-500/15` and `animate-pulse`, and its `data-variant` attribute is `"default"`. Name it something like `"applies the blue action treatment to BACKING_UP, matching DEPLOYING (G-08-7)"`.
   Leave the `"applies animate-pulse to RESTORING..."` and `"applies animate-pulse to MIGRATING..."` tests exactly as they are (still asserting `outline` + no blue) — the gap does not touch those two statuses. Leave every other test in the file (labels table, "resolves the Backing Up query to the Badge element itself", the DEPLOYING/UPDATING regression locks, green/red/gray groups, the unknown-status fallback) exactly as they are.
  </action>

  <verify>
    <automated>yarn workspace @docktor/client test test/unit/components/domain/stack/stack-status-badge.test.tsx</automated>
    <automated>yarn workspace @docktor/client test:unit</automated>
    <automated>yarn typecheck</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'BACKING_UP: {label: "Backing Up", variant: "default"}' client/src/components/domain/stack/stack-status-badge.tsx` prints 1.
    - `grep -c 'BACKING_UP: "bg-blue-500/15 text-blue-700 border-blue-500/25 animate-pulse"' client/src/components/domain/stack/stack-status-badge.tsx` prints 1.
    - `grep -c 'bg-blue-500/15 text-blue-700 border-blue-500/25 animate-pulse' client/src/components/domain/stack/stack-status-badge.tsx` prints 3 (DEPLOYING, UPDATING, and now BACKING_UP — all byte-identical).
    - `grep -c 'RESTORING: "animate-pulse"' client/src/components/domain/stack/stack-status-badge.tsx` prints 1, and `grep -c 'MIGRATING: "animate-pulse"' client/src/components/domain/stack/stack-status-badge.tsx` prints 1 — both untouched.
    - `git status --porcelain` lists exactly the two files in this plan's `files_modified`.
    - Every bullet in this task's `<behavior>` exists as a passing assertion in the test file.
  </acceptance_criteria>

  <reversibility rating="reversible">Two map-entry value changes plus one replaced test assertion; reverting restores plan 08-01's gray/outline BACKING_UP treatment exactly.</reversibility>

  <done>BACKING_UP renders identically to DEPLOYING and UPDATING (default variant, blue-plus-pulse); RESTORING and MIGRATING are untouched — proven by a passing single-file test run, the full client unit suite, and the monorepo typecheck, all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server status enum -> browser DOM | Unchanged from plan 08-01: `stack.status` is used as a lookup key into two module-private maps; the resulting class string is rendered into a `class` attribute. |

## Applicable ASVS L1 Categories

| ASVS Category | Applies | Reason |
|---------------|---------|--------|
| V5 Input Validation | No | No new input surface — this plan changes two literal values in an existing lookup-by-key map; an unrecognized status still falls through to the pre-existing empty-string/outline fallback, unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04-01 | Tampering | `animate-pulse`/blue utility classes reaching the DOM via `Badge`'s `className` pass-through | low | accept | Selected by exact key lookup into a module-private literal map — same mechanism plan 08-01 already shipped and threat-modeled (T-08-03 there); this plan only changes two literal values, not the lookup mechanism. No attacker-controlled substring can reach the `class` attribute. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero packages installed — this is a two-literal-value change in an existing, already-imported component. |
</threat_model>

<verification>
1. `yarn workspace @docktor/client test:unit` — full client unit suite green, exit 0.
2. `yarn typecheck` — silent exit 0.
3. `git status --porcelain` lists exactly the two files in this plan's `files_modified`.
</verification>

<success_criteria>
- BACKING_UP renders with the default variant and the exact blue-plus-pulse class string DEPLOYING/UPDATING use.
- RESTORING and MIGRATING are provably unchanged (still outline/gray-plus-pulse).
- Full client unit suite and monorepo typecheck stay green; no package installed; no file outside this plan's two touched.
</success_criteria>

<output>
Create `.planning/phases/08-live-state-consistency/08-04-SUMMARY.md` when done.
</output>
