---
phase: 11-ui-rework
plan: 13
type: execute
wave: 5
depends_on: ["11-02", "11-03", "11-04", "11-06", "11-07", "11-08", "11-09", "11-10", "11-11", "11-12"]
files_modified:
  - client/playwright.config.ts
  - client/test/integration/mobile.spec.ts
  - client/src/components/common/layout/page.tsx
  - client/src/components/common/data/table/data-table.tsx
  - client/src/components/common/data/table/table-content.tsx
  - client/src/components/common/data/table/table-pagination.tsx
  - client/src/components/app-sidebar.tsx
  - client/src/routes/app/stacks/components/stack-actions.tsx
  - client/src/routes/app/stacks/index.tsx
  - client/src/routes/app/stacks/create.tsx
  - client/src/components/domain/stack/brownfield-import.tsx
  - CLAUDE.md
  - .planning/todos/pending/2026-08-28-frontend-refactor-audit.md
  - .planning/todos/pending/2026-08-28-redesign-ui-ux-service-colors-mobile.md
  - .planning/todos/pending/2026-08-28-redesign-dashboard-statistics.md
  - .planning/todos/pending/2026-08-28-update-available-badge-missing-at-stack-level.md
  - .planning/todos/pending/2026-08-28-update-badge-shown-with-null-latest-tag.md
  - .planning/todos/pending/2026-08-28-add-yaml-env-editor.md
  - .planning/todos/completed/2026-08-28-frontend-refactor-audit.md
  - .planning/todos/completed/2026-08-28-redesign-ui-ux-service-colors-mobile.md
  - .planning/todos/completed/2026-08-28-redesign-dashboard-statistics.md
  - .planning/todos/completed/2026-08-28-update-available-badge-missing-at-stack-level.md
  - .planning/todos/completed/2026-08-28-update-badge-shown-with-null-latest-tag.md
  - .planning/todos/completed/2026-08-28-add-yaml-env-editor.md
autonomous: true
requirements:
  - "GH-15 (docktor-app/docktor#15) — this plan implements D-17 (full component-by-component responsive audit with fix-now vs follow-up-issue triage), verifies D-03/D-08 phase-wide, updates CLAUDE.md's Known Refactoring Targets and UI guidance for the reworked patterns, and closes the six folded todos."

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-17: A Playwright 'mobile-chromium' project (Pixel 7 viewport) runs a mobile spec in which the dashboard, stacks list, create stack, stack detail (all five tabs), backup detail and settings pages show no page-level horizontal overflow and keep their primary controls visible and clickable."
    - "D-17: The core flows work at phone width — deploy from the stack header, view the activity timeline and change its filter, view logs, edit and save compose and environment variables, open the Assign Domain and Edit Schedule dialogs (which fit the viewport), and read status badges."
    - "D-17 / UI-SPEC Discretion Decision 5: every page and shared component is audited at 390px in light and dark mode; findings that break a core flow are fixed in this plan, cosmetic findings are filed as GitHub issues (Feature/Chore type, area:client label) and listed in the SUMMARY."
    - "UI-SPEC StatCards overflow row: the six-card dashboard row wraps without horizontal overflow at phone width."
    - "D-08 phase gate: no hand-rolled pill markup remains anywhere in client/src outside components/ui — every status/config/update pill renders through ToneBadge/Badge."
    - "D-03 phase gate: the stack-management surfaces (stack detail page and its components, backup detail page, dashboard page file) contain no Card usage; Cards remain only for genuinely distinct groupings (StatCard, settings groups, auth/setup forms, the create/import form cards)."
    - "Page-size gate: [id].tsx ≤ 90, [backupId].tsx ≤ 90, dashboard.tsx ≤ 80, settings.tsx ≤ 80 lines."
    - "CLAUDE.md's Known Refactoring Targets table no longer lists [id].tsx, settings.tsx or dashboard.tsx, records any residual targets found in this phase, and the Frontend UI guidance names the new shared patterns so Phases 12/14/15 build on them."
    - "The six folded todos are moved to .planning/todos/completed/ with a Resolution section naming the plans that closed them."
  prohibitions:
    - "No new features in the audit — fixes are responsive/layout/legibility corrections only; anything larger becomes a GitHub issue."
    - "Deferred ideas stay deferred: no stack-level colors (D-12), category colors (D-13) or topology view are introduced or filed as part of this phase's work."
  artifacts:
    - "client/playwright.config.ts — mobile-chromium project"
    - "client/test/integration/mobile.spec.ts — phone-width regression coverage for core flows"
    - "CLAUDE.md — updated Known Refactoring Targets and Frontend UI pattern guidance"
    - ".planning/todos/completed/2026-08-28-*.md — six folded todos with Resolution sections"
  key_links:
    - "The mobile project must use a Chromium-based device descriptor (Pixel 7): only chromium browsers are installed for Playwright in this environment."
    - "mobile.spec.ts must stub every /api request it triggers (fixtures.ts guard), including **/api/settings/backup-defaults on the dashboard."
    - "The desktop 'chromium' project ignores mobile.spec.ts and the mobile project matches only it, so the existing suite's runtime and assertions are unchanged."
---

**Implements:** [docktor-app/docktor#15](https://github.com/docktor-app/docktor/issues/15) — decision D-17; phase-wide verification of D-03/D-08; phase close-out.

<objective>
Close the phase: run the full component-by-component mobile audit D-17 asks for on the reworked UI (earlier plans already built their surfaces responsive; this plan proves it and fixes what is left), lock phone-width behaviour in with a Playwright mobile project, run the phase-wide D-03/D-08/page-size gates, and leave the repository's guidance accurate — CLAUDE.md's refactoring-target table and UI patterns, plus the six folded todos.

Purpose: D-17; guarantee the "single consistent badge scheme" and "less Card wrapping" hold across the whole client, not per plan; make Phases 12/14/15 build on the new patterns (the reason this phase was sequenced first).
Output: mobile Playwright project + spec, audit fixes, filed follow-up issues, updated CLAUDE.md, closed todos.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-ui-rework/11-CONTEXT.md
@.planning/phases/11-ui-rework/11-RESEARCH.md
@.planning/phases/11-ui-rework/11-UI-SPEC.md
@CLAUDE.md
</context>

## Planning notes

- Spec-less probe fallback skipped (no REQUIREMENTS.md-backed IDs); truths derive from D-17 and UI-SPEC Discretion Decision 5.
- The audit runs last because auditing layouts that earlier waves rewrite would be wasted work; each earlier plan carries its own responsive truths (scrolling tab lists, wrapping header rows, 44px icon targets, stacking timeline rows, responsive stat grid and retention grid).
- `files_modified` lists the shared layout/table/action/sidebar/form files the audit is most likely to touch. If a fix-now finding lives elsewhere, fix it (a broken core flow is in scope) and record the extra file under Deviations in the SUMMARY.

## Artifacts this phase produces

| Artifact | Kind | Status |
|----------|------|--------|
| `mobile-chromium` | Playwright project in `client/playwright.config.ts` (Pixel 7, matches `mobile.spec.ts` only) | new |
| `client/test/integration/mobile.spec.ts` | Playwright spec | new |
| `expectNoHorizontalOverflow(page)` | helper inside mobile.spec.ts | new |
| CLAUDE.md Known Refactoring Targets / Frontend UI sections | documentation | modified |
| six `.planning/todos/completed/2026-08-28-*.md` files | moved todos with Resolution sections | moved |

<tasks>

<task type="auto">
  <name>Task 1: Phone-width regression coverage — Playwright mobile project and core-flow mobile spec</name>
  <files>client/playwright.config.ts, client/test/integration/mobile.spec.ts</files>
  <read_first>
    - client/playwright.config.ts — projects array and PLAYWRIGHT_PORT handling (11-01)
    - client/test/integration/fixtures.ts — the unstubbed-API guard and the central /api/events stub
    - client/test/integration/stacks.spec.ts, proxy.spec.ts, backups.spec.ts, theme.spec.ts, config-unsaved-changes.spec.ts — current stubs and selectors for every page the spec visits
    - .planning/phases/11-ui-rework/11-UI-SPEC.md §Spacing (44px exception) and §Discretion Decisions item 5
  </read_first>
  <action>
    Per D-17:

    1. playwright.config.ts: add a project `{name: "mobile-chromium", use: {...devices["Pixel 7"]}, testMatch: /mobile\.spec\.ts/}` and give the existing `chromium` project `testIgnore: /mobile\.spec\.ts/`. Nothing else changes.
    2. mobile.spec.ts (self-sufficient stubs for every request): a helper `expectNoHorizontalOverflow(page)` that polls `document.documentElement.scrollWidth - document.documentElement.clientWidth` and expects ≤ 0. Tests, each ending with the overflow assertion:
       - dashboard: six stat cards visible, recent stacks list visible, "Toggle theme" visible;
       - sidebar: the sidebar trigger opens the mobile navigation and "Stacks" navigates;
       - stack detail Overview: Deploy button clickable, status badge visible, timeline filter changes to "Deployments";
       - all five tabs reachable (scroll the tab list and click each);
       - Config: edit compose and click "Save compose file" (PUT observed); add an env variable and save (PUT observed);
       - Logs: service select and terminal visible;
       - Backups: "Edit Schedule" opens a dialog whose bounding box fits inside the viewport width; "Backup Now" visible;
       - Proxy: "Assign Domain" opens a dialog that fits the viewport;
       - backup detail page: metadata and output visible;
       - settings: each of the four tabs reachable;
       - create stack: compose editor and env editor usable, "Create Stack" visible.
       Run it now; failures here are the input list for Task 2 (do not weaken assertions to make them pass).
  </action>
  <verify>
    <automated>PLAYWRIGHT_PORT=5182 yarn workspace @docktor/client test:integration --project=mobile-chromium --list</automated>
    <fails_when>non-zero exit, or the listing contains no tests from mobile.spec.ts, or lists tests from any other spec file under the mobile project</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'mobile-chromium' client/playwright.config.ts` prints 1 and `grep -c 'Pixel 7' client/playwright.config.ts` prints 1
    - `grep -c 'expectNoHorizontalOverflow' client/test/integration/mobile.spec.ts` prints ≥ 10
    - The first mobile run's failures (if any) are recorded verbatim in the SUMMARY as the audit's starting findings
  </acceptance_criteria>
  <done>Phone-width behaviour for every core flow is covered by an automated Playwright project that the desktop suite does not run.</done>
</task>

<task type="auto">
  <name>Task 2: Component-by-component mobile and dark-mode audit — fix core-flow breaks, file cosmetic follow-ups</name>
  <files>client/src/components/common/layout/page.tsx, client/src/components/common/data/table/data-table.tsx, client/src/components/common/data/table/table-content.tsx, client/src/components/common/data/table/table-pagination.tsx, client/src/components/app-sidebar.tsx, client/src/routes/app/stacks/components/stack-actions.tsx, client/src/routes/app/stacks/index.tsx, client/src/routes/app/stacks/create.tsx, client/src/components/domain/stack/brownfield-import.tsx, client/test/integration/mobile.spec.ts</files>
  <read_first>
    - Task 1's mobile run output
    - every file in the audit checklist below, read before judging it
    - CLAUDE.md §Issue Tracking — gh issue create format, Issue Type via GraphQL, area:client label, duplicate search first
    - .planning/phases/11-ui-rework/11-UI-SPEC.md §Spacing, §Typography, §Color
  </read_first>
  <action>
    Per D-17 and UI-SPEC Discretion Decision 5. Start the dev server (`yarn workspace @docktor/client exec vite --port 5183 --strictPort` in the background with the E2E stubs, or run the mobile spec headed) and audit at 390x844 in both light and dark theme, component by component, recording each as pass / fix-now / issue in a table in the SUMMARY:

    Checklist — login, signup, setup wizard (every step), AppSidebar (mobile sheet), PageHeader (breadcrumb + toggle row, title/actions row), dashboard (StatCard grid, recent stacks table), stacks list (DataTable + pagination), create stack (compose editor, env editor, buttons), import page (brownfield import card), stack detail header (title, badges, Deploy + actions menu), tab list, Overview (services table, activity timeline + filter), Config (compose editor, env table/raw toggle, save buttons), Logs (toolbar wrap, terminal), Backups (summary, schedule dialog, history table, snapshots table, restore dialog), Proxy (domains table, assign/edit dialog, remove dialog), service upgrade dialog, backup detail (metadata row, output), Settings (tab list, each card, certificates card, timezone combobox), toasts.

    Triage rule: fix-now = content or a control is cut off, unreachable, overlapping, illegible (including dark-mode contrast from a light-only color utility), or forces page-level horizontal scroll on a core flow (deploy, view timeline/status, view logs, view/edit config, backups, proxy). Fix with responsive utilities only (flex-wrap, min-w-0, overflow-x-auto containers, stacked layouts below sm/md, dark: variants, 44px icon targets). Everything else (spacing polish, alignment, nice-to-haves) → one GitHub issue per finding via `gh issue create --repo docktor-app/docktor` with the CLAUDE.md body format (Context / Acceptance criteria / Out of scope / Notes), label `area:client`, Issue Type set via the GraphQL mutation, after searching open issues for duplicates. List every created issue number in the SUMMARY. Add a mobile.spec.ts assertion for each fix-now item that the Task 1 spec did not already cover.

    Also run the dark-mode sweep on changed files: `grep -rnE '(text|bg|border)-(gray|green|red|blue|yellow|orange|amber)-[0-9]{3}' client/src --include=*.tsx | grep -v 'dark:' | grep -v components/ui` and add a dark: counterpart to each hit that renders on a light/dark surface (skip hits inside the always-black log terminal).
  </action>
  <verify>
    <automated>PLAYWRIGHT_PORT=5182 yarn workspace @docktor/client test:integration --project=mobile-chromium</automated>
    <fails_when>non-zero exit, or a "failed" count in the Playwright summary</fails_when>
    <automated>PLAYWRIGHT_PORT=5182 yarn workspace @docktor/client test:integration --project=chromium</automated>
    <fails_when>non-zero exit, or a "failed" count in the Playwright summary — an audit fix regressed a desktop flow</fails_when>
    <human-check>At 390px width in both light and dark mode, walk dashboard → stack detail (all five tabs, both dialogs) → backup detail → settings; confirm nothing is cut off or illegible and that the filed follow-up issues match what you see.</human-check>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains the full checklist table with a pass / fix-now / issue verdict for every listed component
    - Every fix-now finding has a corresponding passing mobile.spec.ts assertion
    - Every issue-verdict finding lists a GitHub issue number (or a linked pre-existing duplicate)
    - The dark-mode sweep command's remaining hits are each justified in the SUMMARY (terminal-only or intentionally theme-independent)
  </acceptance_criteria>
  <done>Every component has an audit verdict; core flows work at phone width in both themes; cosmetic follow-ups are tracked as GitHub issues.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-wide gates, CLAUDE.md update and folded-todo closure</name>
  <files>CLAUDE.md, .planning/todos/pending/2026-08-28-frontend-refactor-audit.md, .planning/todos/pending/2026-08-28-redesign-ui-ux-service-colors-mobile.md, .planning/todos/pending/2026-08-28-redesign-dashboard-statistics.md, .planning/todos/pending/2026-08-28-update-available-badge-missing-at-stack-level.md, .planning/todos/pending/2026-08-28-update-badge-shown-with-null-latest-tag.md, .planning/todos/pending/2026-08-28-add-yaml-env-editor.md, .planning/todos/completed/2026-08-28-frontend-refactor-audit.md, .planning/todos/completed/2026-08-28-redesign-ui-ux-service-colors-mobile.md, .planning/todos/completed/2026-08-28-redesign-dashboard-statistics.md, .planning/todos/completed/2026-08-28-update-available-badge-missing-at-stack-level.md, .planning/todos/completed/2026-08-28-update-badge-shown-with-null-latest-tag.md, .planning/todos/completed/2026-08-28-add-yaml-env-editor.md</files>
  <read_first>
    - CLAUDE.md — §Page Composition (its example file list still names compose-tab/environment-tab), §Frontend UI, §Known Refactoring Targets
    - the SUMMARY files of plans 11-01 … 11-12 — residual targets they reported (notably 11-11's list of settings cards still using ad-hoc form state)
    - .planning/todos/completed/2026-09-16-windows-ci-check-fails-on-real-platform-bugs.md — frontmatter `completed:`/`status:` fields and `## Resolution` section convention
    - the six pending folded todo files
  </read_first>
  <action>
    1. Run the four gate commands in this task's `<verify>` block (hand-rolled pill markup outside components/ui — D-08; Card usage on the stack-management surfaces — D-03; page-file line budgets; full client unit suite + `yarn typecheck`) plus the full `yarn workspace @docktor/client test:integration`. Fix any hit inside this plan, or record in the SUMMARY why a remaining hit is legitimate.
    2. CLAUDE.md: remove the three closed rows from Known Refactoring Targets and add rows for residual targets reported by this phase's SUMMARYs (at minimum: the Settings cards' ad-hoc useState form state, which the CLAUDE.md forms rule says should be react-hook-form). Update the Page Composition example list to the new stack-detail components (overview-tab, config-tab, activity-timeline, services-section, stack-detail-header, stack-alerts, proxy-tab, backups-tab). Add to §Frontend UI, one bullet each: status/config pills via `ToneBadge` + `StatusDot` (never hand-rolled pill classes); flat sections via `Section` primitives, Card only for distinct groupings; code editing via `CodeEditor`/`ComposeEditor`; `EnvEditor` for .env content; every color utility needs a `dark:` counterpart (next-themes class strategy); `UnsavedChangesGuard` for pages with in-place editors (requires the data router); Playwright runs take `PLAYWRIGHT_PORT`.
    3. Move each of the six folded todos from `.planning/todos/pending/` to `.planning/todos/completed/` (git mv), set `completed:` to the execution date and `status: completed` in its frontmatter, and append a `## Resolution` section naming the decision(s) and plan(s) that closed it: frontend-refactor-audit → 11-08 (LogTerminal shared with backups) plus the extraction work in 11-01/11-02/11-06/11-07/11-11; redesign-ui-ux-service-colors-mobile → D-11 (11-02, 11-06, 11-08), D-01/D-02 (11-01), D-17 (11-13); redesign-dashboard-statistics → D-14 (11-04); update-available-badge-missing-at-stack-level → D-09 (11-02, 11-04, 11-05, 11-06); update-badge-shown-with-null-latest-tag → D-10 (11-02, 11-06); add-yaml-env-editor → D-18/D-19 (11-09), D-20/D-21/D-22 (11-12).
  </action>
  <verify>
    <automated>test -z "$(grep -rn 'rounded-full px-2 py-0.5' client/src --include=*.tsx | grep -v components/ui)" && test -z "$(grep -rln '<Card' 'client/src/routes/app/stacks/[id].tsx' client/src/routes/app/stacks/components client/src/routes/app/stacks/backups client/src/routes/app/dashboard.tsx)"</automated>
    <fails_when>non-zero exit — a hand-rolled pill or a Card remains on a stack-management surface</fails_when>
    <automated>test "$(wc -l < 'client/src/routes/app/stacks/[id].tsx')" -le 90 && test "$(wc -l < 'client/src/routes/app/stacks/backups/[backupId].tsx')" -le 90 && test "$(wc -l < client/src/routes/app/dashboard.tsx)" -le 80 && test "$(wc -l < client/src/routes/app/settings.tsx)" -le 80</automated>
    <fails_when>non-zero exit — a page file exceeds its line budget</fails_when>
    <automated>for t in frontend-refactor-audit redesign-ui-ux-service-colors-mobile redesign-dashboard-statistics update-available-badge-missing-at-stack-level update-badge-shown-with-null-latest-tag add-yaml-env-editor; do test -f ".planning/todos/completed/2026-08-28-$t.md" && test ! -f ".planning/todos/pending/2026-08-28-$t.md" && grep -q '^## Resolution' ".planning/todos/completed/2026-08-28-$t.md" || exit 1; done</automated>
    <fails_when>non-zero exit — a folded todo is still pending, missing, or has no Resolution section</fails_when>
    <automated>yarn workspace @docktor/client test && yarn typecheck</automated>
    <fails_when>non-zero exit, a "failed" count in the vitest summary, or any "error TS" line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'routes/app/stacks/\[id\].tsx.*587' CLAUDE.md` prints 0 and `grep -c 'SmtpCard.*NotificationTriggersCard' CLAUDE.md` prints 0 (closed rows removed)
    - `grep -c 'ToneBadge' CLAUDE.md` prints ≥ 1, `grep -c 'UnsavedChangesGuard' CLAUDE.md` prints ≥ 1, `grep -c 'PLAYWRIGHT_PORT' CLAUDE.md` prints ≥ 1
    - All four gate commands above exit 0
  </acceptance_criteria>
  <done>The phase-wide D-03/D-08/page-size gates pass, CLAUDE.md reflects the new architecture and remaining targets, and the six folded todos are closed with resolutions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → GitHub (gh CLI) | the audit files follow-up issues on the public repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-40 | Information Disclosure | GitHub issues filed from the audit | low | mitigate | Issue bodies describe layout findings only; screenshots or copied content must come from stubbed E2E data, never from a real stack's compose/env content or credentials |
| T-11-41 | Tampering | responsive fixes in shared layout components | low | mitigate | Both the desktop and the mobile Playwright projects must pass after the fixes, so no layout change can silently break a desktop flow |
| T-11-42 | Denial of Service | none new | low | accept | Documentation, test-config and CSS-utility changes only; no new runtime request paths |
</threat_model>

<verification>
- `PLAYWRIGHT_PORT=5182 yarn workspace @docktor/client test:integration` (both projects) green
- `yarn workspace @docktor/client test` and `yarn typecheck` green
- All Task 3 gates exit 0
</verification>

<success_criteria>
- Every component audited at phone width in both themes; core flows pass the mobile project; cosmetic findings are GitHub issues.
- Phase-wide pill/Card/page-size gates pass; CLAUDE.md and the folded todos reflect the finished rework.
</success_criteria>

<output>
Create `.planning/phases/11-ui-rework/11-13-SUMMARY.md` when done, including the audit table, the list of filed issues, and the gate command outputs.
</output>
