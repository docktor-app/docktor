---
phase: 11-ui-rework
plan: 04
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - client/src/components/common/stat-card.tsx
  - client/src/lib/dashboard-stats.ts
  - client/src/lib/stacks-api.ts
  - client/src/hooks/use-backup-defaults.ts
  - client/src/hooks/use-stacks.ts
  - client/src/routes/app/dashboard/components/dashboard-stat-cards.tsx
  - client/src/routes/app/dashboard.tsx
  - client/test/unit/components/common/stat-card.test.tsx
  - client/test/unit/lib/dashboard-stats.test.ts
  - client/test/unit/hooks/use-backup-defaults.test.ts
  - client/test/unit/hooks/use-stacks.test.ts
  - client/test/integration/stacks.spec.ts
  - client/test/integration/auth.spec.ts
  - client/test/integration/setup-wizard.spec.ts
autonomous: true
requirements:
  - "GH-15 (docktor-app/docktor#15) — this plan implements D-14 (reusable StatCard + two extra at-a-glance stats), the dashboard half of D-03, and the live-refresh half of D-09 on the list surface; closes CLAUDE.md's Known Refactoring Target for routes/app/dashboard.tsx."

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-14: The dashboard shows six stat cards — Total Stacks, Running, Stopped, Errors, Updates Available, Backups Configured — all rendered by one reusable StatCard component in components/common/."
    - "D-14 / UI-SPEC Discretion Decision 2: Updates Available counts stacks with at least one service whose updateAvailable is true; Backups Configured counts stacks whose effective schedule (per-stack backupSchedule, else the global default schedule) is set."
    - "UI-SPEC empty / zero-one-many rows: a fresh install with zero stacks renders all six cards with the value 0, not a hidden or broken layout."
    - "UI-SPEC loading row: while stacks load, every card (including the two new ones) shows the same h-8 w-12 Skeleton placeholder the original four used."
    - "UI-SPEC error row: if the stacks request fails every card shows an em dash (—) instead of a number; if only the backup-defaults request fails, Backups Configured alone shows —."
    - "UI-SPEC partial row: a stack with no update-check data or no backup schedule counts as 0 toward Updates Available / Backups Configured rather than being excluded or blocking the card."
    - { statement: "UI-SPEC long-text row: a stat value with 4+ digits never overflows the fixed StatCard layout — the value truncates and exposes the full number via its title attribute", verification: backstop }
    - "UI-SPEC overflow row: the stat grid wraps to 2 columns on phones, 3 at md and 6 at xl."
    - "D-09 (list liveness): the stack list and dashboard refetch when an update_available SSE event arrives, so the stack-level update pill and the Updates Available count refresh without a reload."
    - "D-03: 'Recent Stacks' renders as a flat Section, not a Card; StatCards remain Cards (a genuinely distinct data grouping)."
    - "UI-SPEC typography: StatCard values use text-2xl font-semibold (600), never font-bold."
    - "client/src/routes/app/dashboard.tsx is at most 80 lines."
  prohibitions:
    - "StatCard's props contain no domain types (Stack, Service, ...) — it passes CLAUDE.md's components/common checklist; all counting lives in lib/dashboard-stats.ts."
    - "No N+1 requests: Backups Configured uses the list response's backupSchedule field plus ONE GET /api/settings/backup-defaults call, never a per-stack backup-config fetch."
  artifacts:
    - "client/src/components/common/stat-card.tsx — StatCard"
    - "client/src/lib/dashboard-stats.ts — DashboardStats, computeDashboardStats()"
    - "client/src/hooks/use-backup-defaults.ts — useBackupDefaults()"
    - "client/src/routes/app/dashboard/components/dashboard-stat-cards.tsx — DashboardStatCards"
  key_links:
    - "GET /api/stacks already returns every Stack scalar (StackRepository.findAll uses findMany with include services), so `backupSchedule` is present in the list payload today; stacks-api.ts's Stack interface only needs to declare it."
    - "Updates Available reads services[].updateAvailable from the list response, which plan 11-05 adds server-side; before 11-05 lands the count is truthfully 0 (UI-SPEC partial row), not an error."
    - "Every E2E spec that lands on the dashboard must stub **/api/settings/backup-defaults — the fixtures.ts guard aborts and fails any unstubbed /api request."
---

**Implements:** [docktor-app/docktor#15](https://github.com/docktor-app/docktor/issues/15) — decisions D-14, D-03 (dashboard), D-09 (list liveness).

<objective>
Extract the dashboard's four copy-pasted stat Cards into one generic StatCard (CLAUDE.md Known Refactoring Target), add the two at-a-glance stats the UI-SPEC chose (Updates Available, Backups Configured), handle every UI-SPEC state for the card row, flatten "Recent Stacks", and make the list surface refresh on update_available events.

Purpose: D-14, the dashboard's share of D-03, the list-liveness share of D-09.
Output: StatCard, computeDashboardStats, useBackupDefaults, DashboardStatCards section, a ≤ 80-line dashboard page, E2E stubs for the new request.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-ui-rework/11-CONTEXT.md
@.planning/phases/11-ui-rework/11-UI-SPEC.md
@.planning/phases/11-ui-rework/11-PATTERNS.md
@.planning/phases/11-ui-rework/11-01-SUMMARY.md
@CLAUDE.md
@client/src/routes/app/dashboard.tsx
</context>

## Planning notes

- Spec-less probe fallback skipped (no REQUIREMENTS.md-backed IDs); truths derive from D-14 and the UI-SPEC Dashboard StatCards rows.
- "Backups Configured" data path (verified during planning): the server's backup scheduler registers a stack when `stack.backupSchedule ?? globalDefault` is set (server/src/jobs/backup-scheduler.ts:96), the list endpoint already returns `backupSchedule`, and the global default comes from the existing GET /api/settings/backup-defaults. No server change is needed for this stat.

## Artifacts this phase produces

| Artifact | Kind | Status |
|----------|------|--------|
| `StatCard` / `StatCardProps` | component in `client/src/components/common/stat-card.tsx` | new |
| `DashboardStats` | exported interface in `client/src/lib/dashboard-stats.ts` | new |
| `computeDashboardStats(stacks, globalDefaultSchedule)` | exported pure function | new |
| `useBackupDefaults()` | hook in `client/src/hooks/use-backup-defaults.ts` | new |
| `DashboardStatCards` / `DashboardStatCardsProps` | section component in `client/src/routes/app/dashboard/components/dashboard-stat-cards.tsx` | new |
| `Stack.backupSchedule` | new field on the `Stack` interface in `client/src/lib/stacks-api.ts` | new field |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Six-stat dashboard row — StatCard, stats computation, backup-defaults hook</name>
  <files>client/src/components/common/stat-card.tsx, client/src/lib/dashboard-stats.ts, client/src/lib/stacks-api.ts, client/src/hooks/use-backup-defaults.ts, client/src/routes/app/dashboard/components/dashboard-stat-cards.tsx, client/src/routes/app/dashboard.tsx, client/test/unit/components/common/stat-card.test.tsx, client/test/unit/lib/dashboard-stats.test.ts, client/test/unit/hooks/use-backup-defaults.test.ts</files>
  <read_first>
    - client/src/routes/app/dashboard.tsx — the four inline stat Cards (lines 50-111), the count computations (lines 14-21), the Recent Stacks Card (lines 113-129)
    - client/src/lib/stacks-api.ts — Stack / StackWithServices / Service interfaces
    - client/src/lib/backups-api.ts — getBackupDefaults() and BackupDefaults
    - client/src/hooks/use-stacks.ts — return shape {stacks, loading, error, refetch}
    - client/src/components/common/layout/section.tsx — Section primitives from plan 11-01
    - .planning/phases/11-ui-rework/11-UI-SPEC.md §UI Considerations "Dashboard StatCards" rows and §Discretion Decisions items 2-3
  </read_first>
  <behavior>
    - computeDashboardStats([], null) → all six counts 0
    - running counts RUNNING + HEALTHY; stopped counts STOPPED; errors counts ERROR + UNHEALTHY; total counts all
    - updatesAvailable counts a stack once even if three of its services have updates; services with updateAvailable undefined count as no update
    - backupsConfigured with globalDefaultSchedule "0 3 * * *" counts every stack; with null counts only stacks whose backupSchedule is a non-empty string
    - StatCard with loading renders a Skeleton and no value; with value 1234567 renders "1234567" in an element carrying truncate and title="1234567"; the value element uses font-semibold
    - useBackupDefaults resolves defaultSchedule from getBackupDefaults(); on rejection it reports error and defaultSchedule null
  </behavior>
  <action>
    Per D-14 and D-03:

    1. `stacks-api.ts`: add `backupSchedule: string | null` to the `Stack` interface (the list and detail endpoints already send it).
    2. `lib/dashboard-stats.ts`: `interface DashboardStats {total; running; stopped; errors; updatesAvailable; backupsConfigured}` (all numbers) and `computeDashboardStats(stacks: ReadonlyArray<StackWithServices>, globalDefaultSchedule: string | null): DashboardStats` per `<behavior>`, treating empty-string schedules as unset. Pure, explicit return type.
    3. `hooks/use-backup-defaults.ts`: fetch `getBackupDefaults()` once on mount; return `{defaultSchedule: string | null, loading: boolean, error: string | null}`; narrow errors with `instanceof Error` (no `any`); ignore the response after unmount.
    4. `components/common/stat-card.tsx` (generic): `StatCard({label, value, icon, loading = false, valueClassName})` with `value: number | string`, `icon: React.ReactNode`. Card → CardHeader (`flex flex-row items-center justify-between pb-2`) with the label as `text-sm font-normal text-muted-foreground` and the icon; CardContent shows `Skeleton className="h-8 w-12"` when loading, else a div `text-2xl font-semibold tabular-nums truncate` + `valueClassName` with `title={String(value)}`. `data-slot="stat-card"`.
    5. `routes/app/dashboard/components/dashboard-stat-cards.tsx`: `DashboardStatCards({stats, loading, stacksError, defaultsError})` rendering a `grid grid-cols-2 gap-4 md:grid-cols-3 xl:grid-cols-6` of six StatCards in order Total Stacks (Layers icon), Running (Play, `text-green-600 dark:text-green-400` value), Stopped (Square), Errors (AlertTriangle, `text-red-600 dark:text-red-400`), Updates Available (ArrowUpCircle, `text-blue-600 dark:text-blue-400` icon), Backups Configured (Archive). Value is "—" for every card when stacksError is set, and for Backups Configured alone when defaultsError is set. Icons carry `aria-hidden`.
    6. `dashboard.tsx`: compose only — `useStacks()`, `useBackupDefaults()`, `computeDashboardStats(stacks, defaultSchedule)`, PageHeader (unchanged "Create Stack" action), `DashboardStatCards`, and a `Section` "Recent Stacks" (SectionHeader with SectionTitle, SectionActions holding the "View all stacks" link button when there are more than 5 stacks) around `StackList` with the first five stacks. No Card in the page file. ≤ 80 lines.
    7. Tests per `<behavior>` in the three new test files.
  </action>
  <verify>
    <automated>yarn workspace @docktor/client test test/unit/components/common/stat-card.test.tsx test/unit/lib/dashboard-stats.test.ts test/unit/hooks/use-backup-defaults.test.ts</automated>
    <fails_when>non-zero exit, or the vitest "Test Files" summary reports a failed file</fails_when>
    <automated>test "$(wc -l < client/src/routes/app/dashboard.tsx)" -le 80 && yarn workspace @docktor/client exec tsc -b</automated>
    <fails_when>non-zero exit — the page exceeds 80 lines or tsc prints an "error TS" line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -vE '^\s*(//|\*)' client/src/routes/app/dashboard.tsx | grep -c '<Card'` prints 0
    - `grep -c '<StatCard' client/src/routes/app/dashboard/components/dashboard-stat-cards.tsx` prints 6
    - `grep -c 'font-bold' client/src/components/common/stat-card.tsx` prints 0
    - `grep -cE 'Stack|Service' client/src/components/common/stat-card.tsx` prints 0 (no domain vocabulary in the common component)
    - The stat-card test includes the 1234567 truncation/title case (UI-SPEC long-text backstop)
  </acceptance_criteria>
  <done>The dashboard shows six StatCards with correct loading, empty, error and partial behaviour; the page file is a ≤ 80-line composition with a flat Recent Stacks section.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Live list refresh on update events and E2E coverage for the new stats</name>
  <files>client/src/hooks/use-stacks.ts, client/test/unit/hooks/use-stacks.test.ts, client/test/integration/stacks.spec.ts, client/test/integration/auth.spec.ts, client/test/integration/setup-wizard.spec.ts</files>
  <read_first>
    - client/src/hooks/use-stacks.ts — useContainerEvents handler (config_changed/config_error refetch branch) and the any-typed catch block
    - client/src/hooks/use-container-events.ts — the event union (confirm the update_available member and its fields)
    - client/test/unit/hooks/use-stacks.test.ts — existing SSE mocking pattern
    - client/test/integration/stacks.spec.ts — "dashboard shows stack stats and recent stacks" test and mockStacksList helper
    - client/test/integration/auth.spec.ts and client/test/integration/setup-wizard.spec.ts — the tests that land on "/" (grep for `toHaveURL("/"` and `goto("/")`)
    - client/test/integration/fixtures.ts — the unstubbed-API guard
  </read_first>
  <behavior>
    - an update_available container event makes useStacks refetch the list exactly once (background mode — `loading` does not flip back to true for an already-loaded list)
    - a failed initial fetch sets error from the Error's message; a non-Error rejection sets "Failed to fetch stacks"
  </behavior>
  <action>
    Per D-09 (list liveness) and D-14:

    1. `use-stacks.ts`: add `update_available` to the event types that trigger a refetch; split the fetch into initial vs background modes mirroring use-stack.ts (background refreshes must not set `loading` true or clear already-rendered stacks); type the catch clause's error parameter as `unknown` (it is currently the any type) and narrow it via `instanceof Error`. Keep the return shape `{stacks, loading, error, refetch}`.
    2. `use-stacks.test.ts`: add the `<behavior>` cases.
    3. E2E: in stacks.spec.ts's dashboard test, stub `**/api/settings/backup-defaults` with `{defaultSchedule: null, defaultRetention: null}`, give one mock stack a service with `updateAvailable: true` and a `backupSchedule`, and assert the cards "Updates Available" and "Backups Configured" are visible with value 1. Add the same backup-defaults stub to every test in auth.spec.ts and setup-wizard.spec.ts that lands on the dashboard, so the fixtures guard never sees an unstubbed request. Do not change any other assertion in those two files.
  </action>
  <verify>
    <automated>yarn workspace @docktor/client test test/unit/hooks/use-stacks.test.ts</automated>
    <fails_when>non-zero exit, or the vitest "Test Files" summary reports a failed file</fails_when>
    <automated>PLAYWRIGHT_PORT=5175 yarn workspace @docktor/client test:integration stacks.spec.ts auth.spec.ts setup-wizard.spec.ts</automated>
    <fails_when>non-zero exit, or a "failed" count in the Playwright summary, or an "Unstubbed API request(s)" error in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'update_available' client/src/hooks/use-stacks.ts` prints ≥ 1
    - `grep -c 'err: any' client/src/hooks/use-stacks.ts` prints 0
    - `grep -c 'api/settings/backup-defaults' client/test/integration/auth.spec.ts client/test/integration/setup-wizard.spec.ts client/test/integration/stacks.spec.ts` shows ≥ 1 in each file
    - The dashboard E2E asserts both new card labels and their values
  </acceptance_criteria>
  <done>The dashboard and stack list refresh on update events; all E2E specs that reach the dashboard pass with the new request stubbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/settings/backup-defaults | existing authenticated endpoint, newly called from the dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-11 | Information Disclosure | useBackupDefaults | low | accept | Reads only the global cron schedule/retention the Settings page already shows to the same authenticated user; no credentials are in that response |
| T-11-12 | Denial of Service | dashboard request fan-out | low | mitigate | Exactly one extra request per dashboard mount; no per-stack fetch loop (prohibition above) |
| T-11-13 | Tampering (XSS) | StatCard value rendering | low | accept | Values are numbers or the em dash rendered as text |
</threat_model>

<verification>
- `yarn workspace @docktor/client test` green
- `PLAYWRIGHT_PORT=5175 yarn workspace @docktor/client test:integration stacks.spec.ts auth.spec.ts setup-wizard.spec.ts` green
- `yarn workspace @docktor/client exec tsc -b` clean
</verification>

<success_criteria>
- Six StatCards with correct counts and all UI-SPEC states; generic StatCard in components/common.
- dashboard.tsx ≤ 80 lines with no inline Cards; list refreshes on update events.
</success_criteria>

<output>
Create `.planning/phases/11-ui-rework/11-04-SUMMARY.md` when done.
</output>
