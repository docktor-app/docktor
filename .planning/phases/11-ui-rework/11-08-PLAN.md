---
phase: 11-ui-rework
plan: 08
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - client/src/components/domain/stack/log-terminal.tsx
  - client/src/components/domain/stack/log-connection-status.tsx
  - client/src/components/domain/stack/log-viewer.tsx
  - client/src/components/common/log-output.tsx
  - client/src/hooks/use-backup-detail.ts
  - client/src/lib/backup-format.ts
  - client/src/components/domain/backup/backup-trigger-badge.tsx
  - client/src/routes/app/stacks/backups/components/backup-detail-header.tsx
  - client/src/routes/app/stacks/backups/components/backup-metadata.tsx
  - client/src/routes/app/stacks/backups/[backupId].tsx
  - client/src/routes/app/stacks/components/backup-history.tsx
  - client/test/unit/components/domain/stack/log-terminal.test.tsx
  - client/test/unit/components/log-viewer.test.tsx
  - client/test/unit/lib/backup-format.test.ts
  - client/test/unit/routes/stacks/backup-detail-page.test.tsx
  - client/test/unit/routes/stacks/backup-history.test.tsx
autonomous: true
requirements:
  - "GH-15 (docktor-app/docktor#15) — this plan implements ROADMAP success criterion 3 (backup and backup-detail pages reuse the log-viewer component, folded todo 2026-08-28-frontend-refactor-audit), D-11's log-line attribution through the shared terminal, and the backup-detail half of D-03."

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ROADMAP SC3: The backup detail page renders restic output through the same LogTerminal component the stack Logs tab uses; the separate LogOutput component no longer exists."
    - "D-11: In the combined (all services) log view each line is prefixed with its [service] tag colored by getServiceColor; lines without a service (backup output) render with no prefix."
    - "The log viewer's Timestamps toggle actually prefixes HH:MM:SS timestamps, the service prefix appears only in the combined 'All services' view, and the Wrap toggle switches between wrapped and horizontally scrolling lines (all three toggles were previously no-ops or inverted)."
    - "UI-SPEC backups log viewer empty row: a completed backup with no captured lines shows the log viewer's 'no output' treatment ('No log output was captured for this backup.')."
    - "UI-SPEC backups log viewer loading row: an in-progress backup with no lines yet shows the muted line 'Waiting for output…'."
    - "UI-SPEC backups log viewer error row: while an in-progress backup's stream is disconnected, the page shows the same 'Disconnected' indicator the stack log viewer shows."
    - "UI-SPEC backups log viewer populated / overflow rows: restic output renders with ANSI colors and auto-scrolls while streaming, in the same fixed-height terminal as container logs."
    - "UI-SPEC backups log viewer long-text row: backup log lines wrap (whitespace-pre-wrap break-all) rather than scrolling horizontally."
    - "The backup detail page keeps every existing behaviour: one request on mount, one-shot resync on completed/failed, bounded disconnected poll (CR-01), non-blank title fallback (CR-03), terminal records render persisted lines."
    - "D-03: the backup detail page renders no Card; metadata is a flat row and Output is a Section."
    - "The Backups tab's history table no longer re-fetches in a loop: opening the tab issues one request, and polling runs only while an IN_PROGRESS backup exists (fixes Phase 10 UAT gap G-10-2)."
    - "The backup detail breadcrumb 'Backups' link goes to /stacks/:id/backups (the path form the router consumes)."
    - "client/src/routes/app/stacks/backups/[backupId].tsx is at most 90 lines."
  prohibitions:
    - "The backup detail page must not import useLogStream (backups are not containers — RESEARCH Pitfall 2)."
    - "No change to useBackupStream or useLogStream."
  artifacts:
    - "client/src/components/domain/stack/log-terminal.tsx — LogTerminal, LogTerminalLine, formatLogTimestamp()"
    - "client/src/components/domain/stack/log-connection-status.tsx — LogConnectionStatus"
    - "client/src/hooks/use-backup-detail.ts — useBackupDetail()"
    - "client/src/lib/backup-format.ts — formatDuration(), formatSize(), BACKUP_TRIGGER_LABELS"
    - "client/src/components/domain/backup/backup-trigger-badge.tsx — BackupTriggerBadge"
    - "client/src/routes/app/stacks/backups/components/backup-detail-header.tsx — BackupDetailHeader"
    - "client/src/routes/app/stacks/backups/components/backup-metadata.tsx — BackupMetadata"
  key_links:
    - "LogViewer keeps ownership of useLogStream and its toolbar; it maps LogLineEvent[] into LogTerminal lines. The backup page maps useBackupStream()'s string[] (or the record's logLines) to {line} objects — the two data sources meet only at LogTerminal's props (RESEARCH Pattern 2)."
    - "log-viewer.test.tsx asserts data-testid=\"log-viewer-terminal\" with a bg-black class and a combobox showing 'All services'; LogTerminal takes a testId prop so LogViewer keeps that id."
    - "backup-history.tsx previously listed `backups` in its polling effect's dependency array while setting it inside the effect — the loop Phase 10 UAT recorded as G-10-2. If Phase 10's gap closure also edits this file, expect a small merge conflict; the behaviour both fixes target is identical."
---

**Implements:** [docktor-app/docktor#15](https://github.com/docktor-app/docktor/issues/15) — ROADMAP success criterion 3; decisions D-11 (log attribution), D-03 (backup detail).

<objective>
Make the backup detail page reuse the stack log viewer's terminal instead of its own near-duplicate. Extract the presentational `LogTerminal` from `log-viewer.tsx` (RESEARCH Pattern 2 — the two data sources are structurally different, so reuse happens at the presentational layer), fix the log viewer's three broken toolbar toggles on the way, then rebuild the backup detail page as a ≤ 90-line composition around a `useBackupDetail` hook, flat metadata and the shared terminal. Deduplicate the backup formatting helpers and trigger pill it shares with the Backups-tab history table, and fix that table's runaway fetch loop.

Purpose: ROADMAP success criterion 3 / folded frontend-refactor-audit todo; D-11 attribution through the shared terminal; D-03 for the backup detail page.
Output: LogTerminal, LogConnectionStatus, useBackupDetail, backup-format lib, BackupTriggerBadge, BackupDetailHeader, BackupMetadata; LogOutput deleted.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-ui-rework/11-CONTEXT.md
@.planning/phases/11-ui-rework/11-RESEARCH.md
@.planning/phases/11-ui-rework/11-UI-SPEC.md
@.planning/phases/11-ui-rework/11-PATTERNS.md
@.planning/phases/11-ui-rework/11-02-SUMMARY.md
@CLAUDE.md
@client/src/components/domain/stack/log-viewer.tsx
@client/src/routes/app/stacks/backups/[backupId].tsx
</context>

## Planning notes

- Spec-less probe fallback skipped (no REQUIREMENTS.md-backed IDs); truths derive from ROADMAP SC3, D-03, D-11 and the UI-SPEC "Backups log viewer" rows (partial and zero-one-many are dismissed there).
- Bugs found while planning and fixed here because this plan rewrites the code that holds them: LogViewer computes `showServicePrefix` and has a `formatLine` helper but never uses either (Timestamps toggle is a no-op, prefix always shows), and its Wrap toggle maps both states to wrapping classes; the backup breadcrumb links to `?tab=backups`, which the `/stacks/:id/:tab?` route ignores; backup-history.tsx's effect re-triggers itself (Phase 10 UAT gap G-10-2, a client bug outside Phase 10's server scope).
- Copy change: an in-progress backup with no lines now reads "Waiting for output…" (UI-SPEC loading row) instead of "No output yet...".

## Artifacts this phase produces

| Artifact | Kind | Status |
|----------|------|--------|
| `LogTerminal` / `LogTerminalProps` | component in `client/src/components/domain/stack/log-terminal.tsx` | new |
| `LogTerminalLine` | exported interface `{line: string; service?: string; timestamp?: string}` | new |
| `formatLogTimestamp(iso)` | exported pure function → `HH:MM:SS` | new |
| `LogConnectionStatus` | component in `client/src/components/domain/stack/log-connection-status.tsx` | new |
| `useBackupDetail(backupId)` | hook in `client/src/hooks/use-backup-detail.ts` | new |
| `formatDuration`, `formatSize`, `BACKUP_TRIGGER_LABELS` | exports in `client/src/lib/backup-format.ts` (deduplicated from two files) | moved |
| `BackupTriggerBadge` | component in `client/src/components/domain/backup/backup-trigger-badge.tsx` | new |
| `BackupDetailHeader` | component in `client/src/routes/app/stacks/backups/components/backup-detail-header.tsx` | new |
| `BackupMetadata` | component in `client/src/routes/app/stacks/backups/components/backup-metadata.tsx` | new |
| `client/src/components/common/log-output.tsx` | file | deleted |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract LogTerminal from the log viewer and make its toolbar toggles work</name>
  <files>client/src/components/domain/stack/log-terminal.tsx, client/src/components/domain/stack/log-connection-status.tsx, client/src/components/domain/stack/log-viewer.tsx, client/test/unit/components/domain/stack/log-terminal.test.tsx, client/test/unit/components/log-viewer.test.tsx</files>
  <read_first>
    - client/src/components/domain/stack/log-viewer.tsx — toolbar, native select, auto-scroll effect (lines 67-77), the unused line-formatting helper and showServicePrefix flag, terminal block (lines 143-164)
    - client/src/lib/service-color.ts (11-02)
    - client/src/hooks/use-log-stream.ts — LogLineEvent
    - client/src/components/ui/select.tsx — shadcn Select exports
    - client/test/unit/components/log-viewer.test.tsx — the two live assertions and the three pending todo cases
  </read_first>
  <behavior>
    - LogTerminal with zero lines renders emptyMessage (default "No log output yet...") in muted text
    - a line with service "web" renders a "[web]" prefix whose class equals getServiceColor("web") when showServicePrefix is true, and no prefix when false or when the line has no service
    - showTimestamps with timestamp "2026-01-01T12:34:56Z" renders the local HH:MM:SS from formatLogTimestamp before the line; no timestamp renders nothing extra
    - lineWrap true → line element has whitespace-pre-wrap and break-all; false → whitespace-pre
    - ANSI escape sequences render as colored spans (ansi-to-react), never as raw innerHTML
    - LogViewer: combobox shows "All services"; the prefix shows only while "All services" is selected; clicking Timestamps adds timestamps; Auto-scroll/Timestamps/Wrap buttons expose aria-pressed
  </behavior>
  <action>
    Per ROADMAP SC3 and D-11:

    1. `log-terminal.tsx`: `LogTerminal({lines, autoScroll, lineWrap = false, showTimestamps = false, showServicePrefix = true, emptyMessage = "No log output yet...", testId = "log-terminal", className})`. Owns the scroll ref and the existing auto-scroll effect (scrollTo fallback kept). Container: `bg-black rounded font-mono text-sm text-white h-96 overflow-auto p-2 w-full` plus className, `data-testid={testId}`, `aria-live` polite while autoScroll else off. Each line: `div` with the wrap classes from `<behavior>`, optional timestamp span (`text-gray-400`), optional service prefix span colored by `getServiceColor`, then `<Ansi>{line.line}</Ansi>`. Keys: index is acceptable here because lines are append-only. Export `LogTerminalLine` and `formatLogTimestamp`.
    2. `log-connection-status.tsx`: `LogConnectionStatus({connected})` rendering the text "Connected" / "Disconnected" (`text-xs text-muted-foreground`, with a StatusDot green/neutral) — the indicator both pages share (UI-SPEC error row).
    3. `log-viewer.tsx`: keep `useLogStream`, the service selection state and the clear action; replace the native select with the shadcn Select (trigger `aria-label="Select service"`, items "All services" plus each service, each service item preceded by its color dot via getServiceColor); toolbar buttons get `aria-pressed`; replace the terminal block with `LogTerminal testId="log-viewer-terminal" lines={lines} autoScroll showTimestamps lineWrap showServicePrefix={selectedService === "all"}`; replace the connected span with `LogConnectionStatus`. Delete the unused line-formatting helper function. Toolbar stays `flex flex-wrap items-center gap-2`.
    4. Tests: new log-terminal.test.tsx per `<behavior>`; in log-viewer.test.tsx keep the two existing assertions (adjust the combobox query to the shadcn trigger) and implement the three pending todo cases (ANSI rendering, combined-view service prefix, toolbar buttons).
  </action>
  <verify>
    <automated>yarn workspace @docktor/client test test/unit/components/domain/stack/log-terminal.test.tsx test/unit/components/log-viewer.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest "Test Files" summary reports a failed file</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'formatLine' client/src/components/domain/stack/log-viewer.tsx` prints 0 and `grep -c '<LogTerminal' client/src/components/domain/stack/log-viewer.tsx` prints 1
    - `grep -c 'it.todo' client/test/unit/components/log-viewer.test.tsx` prints 0
    - `grep -c 'getServiceColor' client/src/components/domain/stack/log-terminal.tsx` prints ≥ 1
  </acceptance_criteria>
  <done>A presentational LogTerminal exists with working timestamp, prefix and wrap options; the stack log viewer uses it with a shadcn service select and a shared connection indicator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backup detail page streams restic output through LogTerminal via a useBackupDetail hook</name>
  <files>client/src/hooks/use-backup-detail.ts, client/src/lib/backup-format.ts, client/src/routes/app/stacks/backups/[backupId].tsx, client/src/components/common/log-output.tsx, client/test/unit/routes/stacks/backup-detail-page.test.tsx, client/test/unit/lib/backup-format.test.ts</files>
  <read_first>
    - client/src/routes/app/stacks/backups/[backupId].tsx — full file: loadBackup initial/resync modes, the resync effect, the bounded disconnected poll (BACKUP_RESYNC_* constants), displayLines/isStillStreaming/outputEmptyMessage, shortId/title fallback comment, three duplicated breadcrumb blocks, the metadata Card and Output Card
    - client/src/components/common/log-output.tsx — the component being retired
    - client/src/hooks/use-backup-stream.ts — {lines, status}
    - client/test/unit/routes/stacks/backup-detail-page.test.tsx — every case (initial load, resync paths, request counts, CR-03 title, CR-01 poll bounds, empty messages, autoScroll switch-off) and how it currently mocks the output component
  </read_first>
  <behavior>
    - formatDuration and formatSize return exactly what the two duplicated implementations return today (e.g. "< 1s", "1m 5s", "1.5 KB", "2.00 GB", "-" for null/NaN); BACKUP_TRIGGER_LABELS maps MANUAL/SCHEDULED/RESTORE to Manual/Scheduled/Restore
    - every existing backup-detail-page case keeps passing with the output assertions retargeted from LogOutput to LogTerminal (lines, autoScroll, emptyMessage)
    - an IN_PROGRESS record with no lines shows "Waiting for output…"; a FAILED record with no lines shows "No log output was captured for this backup."; a COMPLETED record with no lines shows the same captured-nothing message
    - while IN_PROGRESS and the stream status is "disconnected", "Disconnected" is shown next to the Output heading
  </behavior>
  <action>
    Per ROADMAP SC3 and D-03:

    1. `lib/backup-format.ts`: move `formatDuration`, `formatSize` and the trigger-label map (exported as `BACKUP_TRIGGER_LABELS: Record<BackupRecord["trigger"], string>`) out of `[backupId].tsx` unchanged in behaviour, with explicit return types.
    2. `hooks/use-backup-detail.ts`: move the page's record state, `loadBackup` (initial vs resync modes), the one-shot resync effect, the bounded disconnected poll with its two constants and CR-01 comment, and the `useBackupStream` call into `useBackupDetail(backupId)`. Return `{backup, loading, error, displayLines: string[], isStreaming, isStillStreaming, streamStatus}`. Behaviour and request counts must be identical — the page test is the regression net.
    3. `[backupId].tsx`: compose `useBackupDetail`, and render the Output area as a `Section` (SectionTitle "Output", SectionActions showing `LogConnectionStatus connected={false}` only while isStreaming and streamStatus is "disconnected") containing `LogTerminal lines={displayLines.map(line => ({line}))} autoScroll={isStillStreaming} lineWrap showServicePrefix={false} testId="backup-log-terminal" emptyMessage={...}` with the three empty messages from `<behavior>`. Keep the FAILED error Alert and its RESTORE-specific copy. Keep the shortId OR-fallback and its comment (CR-03). The header and metadata still render inline at this step; Task 3 extracts them. Delete `client/src/components/common/log-output.tsx`.
    4. Tests: retarget backup-detail-page.test.tsx's output-component mock to `@/components/domain/stack/log-terminal` (assert on its `lines`/`autoScroll`/`emptyMessage` props), update the IN_PROGRESS empty-message case to "Waiting for output…", add the disconnected-indicator case; add backup-format.test.ts.
  </action>
  <verify>
    <automated>yarn workspace @docktor/client test test/unit/routes/stacks/backup-detail-page.test.tsx test/unit/lib/backup-format.test.ts</automated>
    <fails_when>non-zero exit, or the vitest "Test Files" summary reports a failed file</fails_when>
    <automated>test ! -e client/src/components/common/log-output.tsx && yarn workspace @docktor/client exec tsc -b</automated>
    <fails_when>non-zero exit — log-output.tsx still exists or tsc prints an "error TS" line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'LogOutput' client/src` prints nothing
    - `grep -c 'useLogStream' 'client/src/routes/app/stacks/backups/[backupId].tsx'` prints 0 and `grep -c 'LogTerminal' 'client/src/routes/app/stacks/backups/[backupId].tsx'` prints ≥ 1
    - `grep -c 'Waiting for output' 'client/src/routes/app/stacks/backups/[backupId].tsx'` prints 1
    - All pre-existing backup-detail-page cases (request counts, CR-01, CR-03) pass
  </acceptance_criteria>
  <done>Backup output streams through the shared LogTerminal with the UI-SPEC loading/empty/disconnected states; page logic lives in useBackupDetail with unchanged request behaviour; LogOutput is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Flat backup detail header/metadata, shared trigger badge, and the Backups-tab history fetch-loop fix</name>
  <files>client/src/components/domain/backup/backup-trigger-badge.tsx, client/src/routes/app/stacks/backups/components/backup-detail-header.tsx, client/src/routes/app/stacks/backups/components/backup-metadata.tsx, client/src/routes/app/stacks/backups/[backupId].tsx, client/src/routes/app/stacks/components/backup-history.tsx, client/test/unit/routes/stacks/backup-history.test.tsx, client/test/unit/routes/stacks/backup-detail-page.test.tsx</files>
  <read_first>
    - client/src/routes/app/stacks/backups/[backupId].tsx — state after Task 2 (the three breadcrumb blocks and the metadata Card)
    - client/src/routes/app/stacks/components/backup-history.tsx — duplicated formatDuration/formatSize/TRIGGER_LABELS, the hand-rolled trigger pill, the polling useEffect (lines 44-107) whose dependency array includes the state it sets
    - client/src/components/domain/backup/backup-status-badge.tsx and client/src/components/common/tone-badge.tsx (11-02)
    - .planning/phases/11-ui-rework/11-UI-SPEC.md §Typography and §Spacing (metadata row sizing)
  </read_first>
  <behavior>
    - BackupTriggerBadge renders "Manual" / "Scheduled" / "Restore" in the neutral tone
    - BackupDetailHeader renders breadcrumbs Stacks › {stack} › Backups › {crumb} where the Backups link href is /stacks/{id}/backups, plus the page title; used for the loading, error and ready states
    - BackupMetadata renders the status badge, trigger badge, "Started: …", "Duration: …" and "Size: …" in one wrapping row with no Card
    - BackupHistory: mounting with only COMPLETED backups calls getBackups exactly once even after advancing fake timers by 60s; with an IN_PROGRESS backup it polls every 5s and stops polling once a poll returns no IN_PROGRESS backup
  </behavior>
  <action>
    Per D-03 and the G-10-2 fix:

    1. `backup-trigger-badge.tsx`: `BackupTriggerBadge({trigger})` → `ToneBadge tone="neutral"` with `BACKUP_TRIGGER_LABELS[trigger]`.
    2. `backup-detail-header.tsx`: `BackupDetailHeader({stackId, stackLabel, crumb, title})` — one PageHeader with the breadcrumb built once (Stacks → `/stacks`, stackLabel → `/stacks/${stackId}`, Backups → `/stacks/${stackId}/backups`, crumb as BreadcrumbPage) and PageTitle; replaces the three copies in the page.
    3. `backup-metadata.tsx`: `BackupMetadata({backup})` — `div className="flex flex-wrap items-center gap-x-4 gap-y-2 text-sm"` with BackupStatusBadge, BackupTriggerBadge, and the three muted text spans using the backup-format helpers. No Card.
    4. `[backupId].tsx`: use BackupDetailHeader for loading ("Loading..."), error ("Error") and ready (crumb = shortId, title = pageTitle) states and BackupMetadata in place of the metadata Card. The page must end at ≤ 90 lines with no Card.
    5. `backup-history.tsx`: import the helpers from `@/lib/backup-format` and use BackupTriggerBadge (delete the local copies and the hand-rolled pill); restructure the polling so the effect depends only on `stackId`: fetch once on mount, then while the latest response contains an IN_PROGRESS backup, schedule the next fetch 5s later via a timeout held in a ref-less closure variable; stop when none remain or on unmount; keep the existing 30s "recently triggered" grace behaviour only if it is reachable without the state dependency — otherwise drop it and note the removal in the SUMMARY.
    6. Tests: new backup-history.test.tsx per `<behavior>` (mock `@/lib/backups-api`, `vi.useFakeTimers()`); extend backup-detail-page.test.tsx with the breadcrumb href assertion.
  </action>
  <verify>
    <automated>yarn workspace @docktor/client test test/unit/routes/stacks/backup-history.test.tsx test/unit/routes/stacks/backup-detail-page.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest "Test Files" summary reports a failed file</fails_when>
    <automated>test "$(wc -l < 'client/src/routes/app/stacks/backups/[backupId].tsx')" -le 90 && yarn workspace @docktor/client exec tsc -b</automated>
    <fails_when>non-zero exit — the page exceeds 90 lines or tsc prints an "error TS" line</fails_when>
    <automated>PLAYWRIGHT_PORT=5177 yarn workspace @docktor/client test:integration backups.spec.ts</automated>
    <fails_when>non-zero exit, or a "failed" count in the Playwright summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -vE '^\s*(//|\*)' 'client/src/routes/app/stacks/backups/[backupId].tsx' client/src/routes/app/stacks/backups/components/backup-metadata.tsx | grep -c '<Card'` prints 0
    - `grep -c 'function formatDuration' client/src/routes/app/stacks/components/backup-history.tsx` prints 0 and `grep -vE '^\s*(//|\*)' client/src/routes/app/stacks/components/backup-history.tsx | grep -c 'rounded-full px-2 py-0.5'` prints 0
    - `grep -c 'tab=backups' client/src/routes/app/stacks/backups/components/backup-detail-header.tsx` prints 0
    - The backup-history test asserting a single getBackups call for an all-completed list passes
    - backups.spec.ts passes unchanged (the detail page still shows "Completed" and "Manual")
  </acceptance_criteria>
  <done>The backup detail page is a ≤ 90-line flat composition with one header implementation; trigger badges and format helpers are shared; the Backups tab no longer hammers the API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container / restic output → browser | log lines stream from Docker and restic via SSE and are rendered with ANSI styling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-24 | Tampering (XSS) | LogTerminal ANSI rendering | medium | mitigate | Lines render through ansi-to-react (React elements, no dangerouslySetInnerHTML) exactly as before; a log-terminal test asserts escape sequences become styled spans. Service names and timestamps render as text |
| T-11-25 | Denial of Service | backup-history polling | medium | mitigate | The self-retriggering effect (G-10-2) is replaced by mount-once + in-progress-only polling, covered by a fake-timer test asserting request counts |
| T-11-26 | Information Disclosure | backup log output | low | accept | Same output the page already streamed; no new data source |
</threat_model>

<verification>
- `yarn workspace @docktor/client test` green; `PLAYWRIGHT_PORT=5177 yarn workspace @docktor/client test:integration backups.spec.ts` green; `yarn workspace @docktor/client exec tsc -b` clean
</verification>

<success_criteria>
- Backup detail uses the shared LogTerminal with UI-SPEC states; LogOutput deleted; stack log viewer toggles work.
- Backup detail page ≤ 90 lines, no Cards; history table fetch loop fixed.
</success_criteria>

<output>
Create `.planning/phases/11-ui-rework/11-08-SUMMARY.md` when done; state explicitly that Phase 10 UAT gap G-10-2 is fixed here so Phase 10's gap closure can skip or reconcile it.
</output>
