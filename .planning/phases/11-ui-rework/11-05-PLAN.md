---
phase: 11-ui-rework
plan: 05
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - server/src/application/stack-service.ts
  - server/test/unit/application/stack-service.test.ts
  - server/test/integration/stacks.test.ts
autonomous: true
requirements:
  - "GH-15 (docktor-app/docktor#15) — this plan implements the server half of D-09: GET /api/stacks returns per-service updateAvailable/latestTag so the stack-level update badge (11-02) and the Updates Available stat (11-04) have data on the list surface."

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-09: GET /api/stacks returns, for every service of every listed stack, `updateAvailable` (boolean, false when no ImageUpdateCheck row exists) and `latestTag` (string or null) — the same fields and semantics GET /api/stacks/:id already returns."
    - "D-09: A service on an explicit tag is matched to its ImageUpdateCheck row via the tag-qualified image ref (buildImageRefFromService), exactly like the detail route, so explicit-tag services are never silently reported as up to date."
    - "listStacks() performs ONE batched ImageUpdateCheck lookup for all listed stacks' services — never one query per stack or per service."
    - "listStacks() still hides protected stacks unless proxy.showInDashboard is enabled (existing behaviour preserved)."
    - "GET /api/stacks/:id's response is unchanged (same shape, same values) after the enrichment is shared between the two methods."
  prohibitions:
    - "No Prisma schema change and no migration — this reads the existing ImageUpdateCheck table through the existing repository port."
    - "No route-level join: the enrichment lives in StackService (application layer); routes/stacks.ts keeps calling stackService.listStacks() only (CLAUDE.md layering)."
    - "No change to any client or shared file in this plan."
  artifacts:
    - "server/src/application/stack-service.ts — listStacks() enriched; private shared enrichment helper used by listStacks() and getStackWithUpdateInfo()"
    - "server/test/unit/application/stack-service.test.ts — listStacks enrichment + single-batch cases"
    - "server/test/integration/stacks.test.ts — GET /api/stacks carries updateAvailable/latestTag from a seeded ImageUpdateCheck row"
  key_links:
    - "Phase 10 (feature/phase-10-backend-refactoring) already moved the detail route's ImageUpdateCheck join into StackService.getStackWithUpdateInfo() behind an injected ImageUpdateCheckReadRepo port. This plan extends THAT code; implementing it against the pre-Phase-10 route-level join would duplicate Phase 10's refactor and conflict with it on merge — hence the task precondition."
    - "The client's Service interface already declares `updateAvailable?: boolean` and `latestTag?: string | null`; the list payload simply starts populating them — no client change is required for 11-02's StackUpdateBadge or 11-04's stat to light up."
---

**Implements:** [docktor-app/docktor#15](https://github.com/docktor-app/docktor/issues/15) — decision D-09 (list surface, server side).

<objective>
Close the data gap RESEARCH Pitfall 1 found: the stack list endpoint never computes `updateAvailable`/`latestTag` (only the detail route does), so a client-only D-09 badge on the stack list and dashboard would silently never render. Share the existing update-info enrichment between `getStackWithUpdateInfo()` and `listStacks()` in StackService, with one batched lookup.

Purpose: D-09's list/dashboard half needs real server data; this is the phase's only server change, additive and non-breaking (optional fields become populated).
Output: enriched `listStacks()`, unit + integration tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-ui-rework/11-CONTEXT.md
@.planning/phases/11-ui-rework/11-RESEARCH.md
@CLAUDE.md
</context>

## Planning notes

- Spec-less probe fallback skipped (no REQUIREMENTS.md-backed IDs); truths derive from D-09 and RESEARCH Pitfall 1.
- **Dependency conflict flagged at planning time:** on this branch (`feature/phase-11-ui-rework`, based on `main`) the join still lives inline in `server/src/routes/stacks.ts`. Phase 10 — complete and in UAT on `feature/phase-10-backend-refactoring`, not yet merged — moved it into `StackService.getStackWithUpdateInfo()` with an injected `ImageUpdateCheckReadRepo` port (`this.updateChecks`). Phase 10 touches no client/shared files, so merging it into this branch before executing this plan is conflict-free for every other Phase 11 plan. Task 1 carries a `<precondition>` so the executor halts with a blocking-human checkpoint instead of re-implementing Phase 10's move.
- Schema gate: no Prisma schema file is in scope; no schema push task is needed.

## Artifacts this phase produces

| Artifact | Kind | Status |
|----------|------|--------|
| `StackService.listStacks()` | method — now returns services augmented with `updateAvailable`/`latestTag` | modified |
| `StackService.withServiceUpdateInfo(stacks)` | private method (batched enrichment shared by `listStacks()` and `getStackWithUpdateInfo()`) | new |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enrich listStacks() with batched update info shared with the detail path</name>
  <precondition>`grep -c 'getStackWithUpdateInfo' server/src/application/stack-service.ts` prints ≥ 1 on the current branch — i.e. Phase 10 (feature/phase-10-backend-refactoring) has been merged into main and main merged into feature/phase-11-ui-rework</precondition>
  <files>server/src/application/stack-service.ts, server/test/unit/application/stack-service.test.ts</files>
  <read_first>
    - server/src/application/stack-service.ts — ImageUpdateCheckReadRepo port, constructor (`updateChecks`), listStacks(), getStack(), getStackWithUpdateInfo()
    - server/src/jobs/update-checker.ts — buildImageRefFromService (the tag-qualified key contract)
    - server/test/unit/application/stack-service.test.ts — createMockUpdateChecks and the existing listStacks / getStackWithUpdateInfo describe blocks
    - server/src/routes/stacks.ts — confirm GET /api/stacks calls stackService.listStacks() and nothing else
    - CLAUDE.md §Architecture (Server) — services never import route types; repositories are the only Prisma users
  </read_first>
  <behavior>
    - listStacks() with two stacks whose services map to refs A (hasUpdate true, latestTag "1.2.3") and B (no row) returns updateAvailable true / latestTag "1.2.3" for A's service and updateAvailable false / latestTag null for B's service
    - a service whose image ref cannot be built (buildImageRefFromService returns null) gets updateAvailable false / latestTag null
    - updateChecks.findByImageRefs is called exactly once per listStacks() call, with the de-duplicated union of refs across all stacks
    - with proxy.showInDashboard false a protected stack is still excluded, and its refs are not looked up
    - getStackWithUpdateInfo() keeps returning the identical shape and values its existing tests assert
  </behavior>
  <action>
    Per D-09 (server half):

    1. In StackService add a private method `withServiceUpdateInfo<T extends {services: Array<{image: string; imageTag: string | null}>}>(stacks: T[])` that builds each service's key with `buildImageRefFromService(svc.image, svc.imageTag)`, collects the non-null keys across ALL stacks into a de-duplicated array, calls `this.updateChecks.findByImageRefs(refs)` once (skip the call entirely when the array is empty), and returns the stacks with every service spread plus `updateAvailable` (`hasUpdate`, default false) and `latestTag` (default null). Explicit return type.
    2. Re-implement `getStackWithUpdateInfo(id)` as `getStack(id)` → null passthrough → `withServiceUpdateInfo([stack])[0]` so both paths share one implementation; keep its doc comment's key-matching rationale on the helper.
    3. `listStacks()`: keep the existing showInDashboard filtering FIRST, then return `this.withServiceUpdateInfo(filtered)`.
    4. Unit tests: add the `<behavior>` cases to the existing listStacks/getStackWithUpdateInfo describe blocks using the file's own mock factories; keep every existing assertion.
  </action>
  <verify>
    <automated>yarn workspace @docktor/server test:unit test/unit/application/stack-service.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary reports any failed test, or 0 tests collected for the file</fails_when>
    <automated>yarn typecheck</automated>
    <fails_when>non-zero exit, or any "error TS" line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'withServiceUpdateInfo' server/src/application/stack-service.ts` prints ≥ 3 (definition + two call sites)
    - `grep -c 'imageUpdateCheck\|ImageUpdateCheck' server/src/routes/stacks.ts` prints 0 (no route-level join)
    - The unit test asserting findByImageRefs is called exactly once for a two-stack list passes
  </acceptance_criteria>
  <done>listStacks() returns update info for every listed service via one batched lookup, sharing its implementation with the detail path; unit tests prove matching, defaults, batching and protected-stack filtering.</done>
</task>

<task type="auto">
  <name>Task 2: Integration test — GET /api/stacks carries update info from the real database</name>
  <files>server/test/integration/stacks.test.ts</files>
  <read_first>
    - server/test/integration/stacks.test.ts — the "GET /api/stacks → lists stacks" test, its setup/cleanup and auth helpers
    - server/test/integration/setup.ts — testcontainers database bootstrap
    - server/src/repositories/image-update-check-repository.ts — the persisted row shape (imageRef, hasUpdate, latestTag, availableTags)
    - CLAUDE.md §Testing — integration tests run against a real database and clean up their own data
  </read_first>
  <action>
    Per D-09: add a test that creates a stack with a service on an explicit tag through the API (or the repository the existing tests already use for seeding), inserts one ImageUpdateCheck row keyed by that service's tag-qualified ref with hasUpdate true and latestTag "9.9.9" via the image-update-check repository, calls GET /api/stacks as the authenticated test user, and asserts the listed service has `updateAvailable: true` and `latestTag: "9.9.9"` while a second service with no row has `updateAvailable: false` and `latestTag: null`. Clean up the stack and the ImageUpdateCheck row in the test's own teardown, matching the file's existing cleanup style.
  </action>
  <verify>
    <automated>yarn workspace @docktor/server test:integration test/integration/stacks.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary reports any failed test (a Docker/testcontainers startup error also counts as failure — report it rather than skipping)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'updateAvailable' server/test/integration/stacks.test.ts` prints ≥ 2
    - The new integration test passes against the real Postgres container and removes the rows it created
  </acceptance_criteria>
  <done>A real-database test proves the list endpoint returns update info end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → GET /api/stacks | existing route behind requireAuth; response gains two fields per service |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-14 | Information Disclosure | listStacks() enrichment | low | accept | updateAvailable/latestTag are already exposed per stack on GET /api/stacks/:id to the same authenticated user; the list route stays behind the unchanged requireAuth onRequest hook |
| T-11-15 | Denial of Service | ImageUpdateCheck lookup on every list call | low | mitigate | One batched `findByImageRefs` call per request with de-duplicated refs (no N+1); skipped entirely when there are no refs |
| T-11-16 | Elevation of Privilege | protected-stack visibility | low | mitigate | showInDashboard filtering runs before enrichment and is covered by a unit test, so protected stacks are not newly exposed |
</threat_model>

<verification>
- `yarn workspace @docktor/server test:unit` green; `yarn workspace @docktor/server test:integration test/integration/stacks.test.ts` green; `yarn typecheck` clean
</verification>

<success_criteria>
- GET /api/stacks returns updateAvailable/latestTag per service with detail-route semantics, via one batched query.
</success_criteria>

<output>
Create `.planning/phases/11-ui-rework/11-05-SUMMARY.md` when done; if the precondition halted execution, record that and the merge that unblocked it.
</output>
