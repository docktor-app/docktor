---
phase: 11-ui-rework
plan: 09
type: execute
wave: 3
depends_on: ["11-01", "11-03"]
files_modified:
  - client/package.json
  - yarn.lock
  - client/src/components/common/code-editor.tsx
  - client/src/lib/yaml-syntax-linter.ts
  - client/src/components/domain/stack/compose-editor.tsx
  - client/src/routes/app/stacks/components/config-tab.tsx
  - client/src/routes/app/stacks/create.tsx
  - client/test/setup.ts
  - client/test/unit/lib/yaml-syntax-linter.test.ts
  - client/test/unit/components/common/code-editor.test.tsx
  - client/test/unit/routes/stacks/config-tab.test.tsx
  - client/test/integration/stacks.spec.ts
autonomous: false
requirements:
  - "GH-15 (docktor-app/docktor#15) — this plan implements D-18 (CodeMirror 6 compose editor, not Monaco) and D-19 (basic YAML syntax validation only) on both compose editing surfaces (Config tab and Create Stack page, per folded todo 2026-08-28-add-yaml-env-editor)."

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-18: The Config tab's Compose File section and the Create Stack page's compose field are CodeMirror 6 editors (via @uiw/react-codemirror) with YAML syntax highlighting — no Monaco dependency is added."
    - "D-19: Typing invalid YAML (bad indentation, unclosed quote, duplicate key) shows an inline error diagnostic with the yaml parser's own message verbatim; valid YAML shows none; there is no docker-compose-schema linting."
    - "UI-SPEC compose error row: save failures still surface through the existing ApiError → toast path; YAML diagnostics are inline only (no toast)."
    - "UI-SPEC compose empty row: an empty compose document shows a muted 'services:' placeholder instead of a blank pane."
    - "UI-SPEC compose long-text / overflow rows: line wrapping stays off so indentation is never ambiguous; long lines scroll horizontally inside a fixed 400px-high editor with its own vertical scroll."
    - "D-15 integration: the editor switches between CodeMirror's light and dark themes from next-themes' resolvedTheme."
    - "RESEARCH Pitfall 3: the existing dirty guard still holds with CodeMirror — a background stack refresh never replaces the editor content while the compose file has unsaved edits, and a legitimate external change appears once the edit is saved."
    - "The compose editor is reachable by assistive tech and tests as a textbox named 'Docker Compose File' on both surfaces."
    - "Package legitimacy: @uiw/react-codemirror and yaml (flagged SUS by the automated gate on recency only) are confirmed by a human before `yarn add` runs."
  prohibitions:
    - "No Monaco (monaco-editor, @monaco-editor/*) dependency (D-18)."
    - "No docker-compose schema validation, service/key linting or warnings beyond YAML parse errors (D-19; tracked separately by the configurable-compose-linting todo)."
    - "No extra CodeMirror theme package — @uiw/react-codemirror's built-in \"light\"/\"dark\" theme strings are used (RESEARCH assumption A1, verified during planning: the ReactCodeMirrorProps theme type is 'light' | 'dark' | 'none' | Extension)."
  artifacts:
    - "client/src/components/common/code-editor.tsx — CodeEditor (generic themed CodeMirror wrapper)"
    - "client/src/lib/yaml-syntax-linter.ts — yamlSyntaxDiagnostics(), yamlSyntaxLinter"
    - "client/src/components/domain/stack/compose-editor.tsx — ComposeEditor"
    - "client/package.json — @uiw/react-codemirror, @codemirror/lang-yaml, @codemirror/lint, yaml"
  key_links:
    - "ConfigTab keeps its `files: StackConfigFiles` prop (11-01); ComposeEditor's value/onChange bind to files.composeContent / files.setComposeContent exactly like the text area did, so useStackConfigFiles' dirty guard is untouched."
    - "@uiw/react-codemirror re-exports @codemirror/view and @codemirror/state (verified in its 4.25.12 index.d.ts), so EditorView.contentAttributes is imported from it — no direct @codemirror/view dependency is needed."
    - "Plan 11-12 reuses CodeEditor (no language extension) for the env editor's raw mode — keep CodeEditor generic."
    - "stack-detail-page.test.tsx mocks the config-tab module (11-01), so CodeMirror never renders in that page test."
---

**Implements:** [docktor-app/docktor#15](https://github.com/docktor-app/docktor/issues/15) — decisions D-18, D-19.

<objective>
Replace the plain compose text areas with a CodeMirror 6 YAML editor that flags syntax errors inline, on both surfaces where users edit compose files (Config tab, Create Stack page), following the app theme.

Purpose: D-18/D-19 and the folded add-yaml-env-editor todo (which names both [id].tsx and create.tsx). Starts with the mandatory package-legitimacy gate because two of the four new packages were flagged SUS by the automated recency heuristic.
Output: CodeEditor (generic), yamlSyntaxLinter, ComposeEditor, wired into ConfigTab and create.tsx; 4 new client dependencies.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-ui-rework/11-CONTEXT.md
@.planning/phases/11-ui-rework/11-RESEARCH.md
@.planning/phases/11-ui-rework/11-UI-SPEC.md
@.planning/phases/11-ui-rework/11-01-SUMMARY.md
@.planning/phases/11-ui-rework/11-03-SUMMARY.md
@CLAUDE.md
</context>

## Planning notes

- Spec-less probe fallback skipped (no REQUIREMENTS.md-backed IDs); truths derive from D-18/D-19 and the UI-SPEC "Compose editor" rows (loading and partial are dismissed there).
- Package Legitimacy Audit (11-RESEARCH.md): `@codemirror/lang-yaml` OK, `@codemirror/lint` OK, `@uiw/react-codemirror` SUS (gate reason `too-new` — latest-version recency; first published 2018-07-02, ~3.2M weekly downloads), `yaml` SUS (same reason; first published 2011-04-15, ~149.5M weekly downloads, already a direct dependency of server/package.json). Task 1 is the blocking-human gate the protocol requires; it can never be auto-approved.
- `create.tsx` is included because the folded todo 2026-08-28-add-yaml-env-editor lists it and D-18 governs "the compose YAML editor" — leaving a plain text area on the create page would leave two inconsistent compose editors.

## Artifacts this phase produces

| Artifact | Kind | Status |
|----------|------|--------|
| `CodeEditor` / `CodeEditorProps` | component in `client/src/components/common/code-editor.tsx` | new |
| `yamlSyntaxDiagnostics(source)` | exported pure function → `Diagnostic[]` in `client/src/lib/yaml-syntax-linter.ts` | new |
| `yamlSyntaxLinter` | exported CodeMirror lint extension | new |
| `ComposeEditor` / `ComposeEditorProps` | component in `client/src/components/domain/stack/compose-editor.tsx` | new |
| `@uiw/react-codemirror` ^4.25.12, `@codemirror/lang-yaml` ^6.1.3, `@codemirror/lint` ^6.9.7, `yaml` ^2.9.1 | client dependencies | new |

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy before installing the CodeMirror/YAML dependencies</name>
  <files>n/a — verification gate before any install</files>
  <read_first>
    - .planning/phases/11-ui-rework/11-RESEARCH.md §Package Legitimacy Audit and its note on the two SUS verdicts
  </read_first>
  <what-built>Nothing installed yet. Four packages are about to be added to @docktor/client: @uiw/react-codemirror ^4.25.12 (SUS, recency-only), yaml ^2.9.1 (SUS, recency-only; already used by the server), @codemirror/lang-yaml ^6.1.3 (OK), @codemirror/lint ^6.9.7 (OK).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@uiw/react-codemirror — publisher uiwjs, repository github.com/uiwjs/react-codemirror, multi-million weekly downloads, long release history.
    2. Open https://www.npmjs.com/package/yaml — author eemeli, repository github.com/eemeli/yaml; confirm it is the same package as the `yaml` entry in server/package.json.
    3. Open https://www.npmjs.com/package/@codemirror/lang-yaml and https://www.npmjs.com/package/@codemirror/lint — publisher is the codemirror org (Marijn Haverbeke).
    4. Confirm none of the four names is a lookalike (exact scope and spelling).
  </how-to-verify>
  <action>Present the four packages and the verification steps above to the developer and wait for an explicit answer. Do not run any install command before approval. Record the answer and date in the SUMMARY.</action>
  <verify>
    <human-check>Developer confirms all four packages are the legitimate upstream packages (or names which one to replace).</human-check>
  </verify>
  <acceptance_criteria>
    - The developer's explicit approval (or rejection) is recorded in 11-09-SUMMARY.md
    - `git diff --name-only -- client/package.json yarn.lock` is empty until approval is recorded
  </acceptance_criteria>
  <resume-signal>Type "approved" or name the package(s) to replace</resume-signal>
  <done>All four packages are human-verified as legitimate, or the plan halts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install the editor packages and build CodeEditor, the YAML syntax linter and ComposeEditor</name>
  <files>client/package.json, yarn.lock, client/src/components/common/code-editor.tsx, client/src/lib/yaml-syntax-linter.ts, client/src/components/domain/stack/compose-editor.tsx, client/test/setup.ts, client/test/unit/lib/yaml-syntax-linter.test.ts, client/test/unit/components/common/code-editor.test.tsx</files>
  <read_first>
    - .planning/phases/11-ui-rework/11-RESEARCH.md §Code Examples (YAML linter, CodeMirror wired to dark mode) and §Standard Stack versions
    - client/src/components/common/theme-provider.tsx (11-03) — the provider useTheme() reads
    - client/test/setup.ts — existing global stubs (EventSource, matchMedia from 11-03)
    - node_modules/@uiw/react-codemirror/esm/index.d.ts (after install) — ReactCodeMirrorProps (value, height, theme, extensions, placeholder, basicSetup, onChange, readOnly)
    - node_modules/yaml/dist/errors.d.ts (after install) — YAMLParseError/YAMLError `pos: [number, number]` and `message`
  </read_first>
  <behavior>
    - yamlSyntaxDiagnostics("services:\n  web:\n    image: nginx\n") → []
    - yamlSyntaxDiagnostics of a document with an unclosed double quote → exactly one or more diagnostics, each severity "error", message equal to the yaml package's error message, from/to within [0, source.length] and to > from
    - duplicate mapping keys produce an error diagnostic
    - CodeEditor renders an element with role "textbox" named by its ariaLabel and containing the initial value text; under a ThemeProvider with resolved theme dark the editor root carries CodeMirror's dark theme class
  </behavior>
  <action>
    Per D-18 and D-19 (only after Task 1 approval):

    1. Run `yarn workspace @docktor/client add @uiw/react-codemirror@^4.25.12 @codemirror/lang-yaml@^6.1.3 @codemirror/lint@^6.9.7 yaml@^2.9.1`.
    2. `lib/yaml-syntax-linter.ts`: `yamlSyntaxDiagnostics(source: string): Diagnostic[]` — `parseDocument(source)` from `yaml`; map each entry of `doc.errors` to `{from, to, severity: "error", message: error.message}` with from/to taken from `error.pos` and clamped so `0 ≤ from < to ≤ max(source.length, 1)`. Export `yamlSyntaxLinter = linter((view) => yamlSyntaxDiagnostics(view.state.doc.toString()))`. Syntax only — no compose-specific rules (D-19).
    3. `components/common/code-editor.tsx` (generic, no domain vocabulary): `CodeEditor({value, onChange, ariaLabel, height = "400px", extensions = [], placeholder, readOnly = false, className})`. Renders `@uiw/react-codemirror` with `theme={resolvedTheme === "dark" ? "dark" : "light"}` from next-themes' `useTheme()`, `basicSetup={{lineNumbers: true, foldGutter: true, highlightActiveLine: true}}`, no line-wrapping extension, and a memoised extensions array that appends `EditorView.contentAttributes.of({"aria-label": ariaLabel})` (EditorView imported from `@uiw/react-codemirror`). Wrap in `div className={cn("overflow-hidden rounded-md border", className)}`.
    4. `components/domain/stack/compose-editor.tsx`: `ComposeEditor({value, onChange, ariaLabel = "Docker Compose File", height = "400px"})` → CodeEditor with `extensions=[yaml(), yamlSyntaxLinter, lintGutter()]` (memoised) and `placeholder="services:"`.
    5. `client/test/setup.ts`: add jsdom shims CodeMirror needs only if missing — `Range.prototype.getClientRects` returning an empty list and `Range.prototype.getBoundingClientRect` returning a zero DOMRect — guarded by existence checks.
    6. Tests per `<behavior>`: yaml-syntax-linter.test.ts (pure function) and code-editor.test.tsx (render smoke test, textbox name, dark theme class under a ThemeProvider with localStorage theme "dark").
  </action>
  <verify>
    <automated>yarn workspace @docktor/client test test/unit/lib/yaml-syntax-linter.test.ts test/unit/components/common/code-editor.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest "Test Files" summary reports a failed file</fails_when>
    <automated>grep -c '"@uiw/react-codemirror"' client/package.json && ! grep -q 'monaco' client/package.json && yarn workspace @docktor/client exec tsc -b</automated>
    <fails_when>non-zero exit — the dependency is missing, a monaco package is present, or tsc prints an "error TS" line</fails_when>
  </verify>
  <acceptance_criteria>
    - client/package.json lists all four packages at the researched major versions
    - `grep -c 'parseDocument' client/src/lib/yaml-syntax-linter.ts` prints ≥ 1
    - `grep -c 'resolvedTheme' client/src/components/common/code-editor.tsx` prints ≥ 1
    - `grep -cE 'Stack|Compose' client/src/components/common/code-editor.tsx` prints 0 (CodeEditor stays generic)
  </acceptance_criteria>
  <done>The four packages are installed; a generic themed CodeEditor, a pure YAML syntax linter and a ComposeEditor exist with passing tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Use ComposeEditor on the Config tab and the Create Stack page</name>
  <files>client/src/routes/app/stacks/components/config-tab.tsx, client/src/routes/app/stacks/create.tsx, client/test/unit/routes/stacks/config-tab.test.tsx, client/test/integration/stacks.spec.ts</files>
  <read_first>
    - client/src/routes/app/stacks/components/config-tab.tsx (11-01) — the Compose File section and its text area bound to `files`
    - client/src/routes/app/stacks/create.tsx — the composeContent FormField (lines ~125-148) and its FormControl/FormDescription/FormMessage
    - client/test/unit/routes/stacks/config-tab.test.tsx (11-01)
    - client/test/integration/stacks.spec.ts — the config-tab save test (11-01) and the create-stack test using `getByLabel(/docker compose file/i).fill(...)`
  </read_first>
  <behavior>
    - ConfigTab renders ComposeEditor with value files.composeContent; an editor change calls files.setComposeContent; the Compose Save button stays disabled until composeDirty
    - Create Stack: typing into the "Docker Compose File" editor updates the form's composeContent; submitting an empty editor still shows the createStackSchema validation message under the field
    - E2E: on /stacks/my-app/config, typing an unclosed quote into the editor shows a CodeMirror lint error marker; fixing it removes the marker; editing and clicking "Save compose file" sends the new composeContent in the PUT body
  </behavior>
  <action>
    Per D-18, D-19 and Pitfall 3:

    1. `config-tab.tsx`: replace the compose text area with `ComposeEditor value={files.composeContent} onChange={files.setComposeContent}`; leave the Environment section untouched (plan 11-12 replaces it). Remove the text-area import if it is no longer used by the env section.
    2. `create.tsx`: in the composeContent FormField render `ComposeEditor value={field.value} onChange={field.onChange} height="300px"` directly inside FormItem (not wrapped in FormControl, whose Slot would forward ids to CodeMirror's wrapper instead of the textbox — the accessible name comes from ComposeEditor's ariaLabel "Docker Compose File"); keep FormLabel, FormDescription and FormMessage.
    3. config-tab.test.tsx: mock `@/components/domain/stack/compose-editor` with a minimal controlled textarea exposing the same aria-label, and assert the wiring in `<behavior>` (CodeMirror itself is covered by 11-09 Task 2's tests).
    4. stacks.spec.ts: update the create-stack test to type into the editor (`getByRole("textbox", {name: /docker compose file/i})` then `click()` + `keyboard.type(...)`, or `fill()` if Playwright accepts it on the contenteditable); update the config-tab save test to edit via the editor; add the lint marker test (`.cm-lintRange-error` visible after typing an unclosed quote, absent after fixing).
  </action>
  <verify>
    <automated>yarn workspace @docktor/client test test/unit/routes/stacks/config-tab.test.tsx test/unit/routes/stacks/stack-detail-page.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest "Test Files" summary reports a failed file</fails_when>
    <automated>PLAYWRIGHT_PORT=5178 yarn workspace @docktor/client test:integration stacks.spec.ts</automated>
    <fails_when>non-zero exit, or a "failed" count in the Playwright summary, or an "Unstubbed API request(s)" error</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ComposeEditor' client/src/routes/app/stacks/components/config-tab.tsx` prints ≥ 1 and `grep -c 'ComposeEditor' client/src/routes/app/stacks/create.tsx` prints ≥ 1
    - `grep -c 'cm-lintRange-error' client/test/integration/stacks.spec.ts` prints ≥ 1
    - The E2E config save test observes the edited composeContent in the PUT body
  </acceptance_criteria>
  <done>Both compose editing surfaces use the themed CodeMirror YAML editor with inline syntax diagnostics, proven end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → client bundle | four new third-party packages enter the shipped client |
| compose file content → editor | server-stored compose text is rendered and parsed in the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-SC | Tampering | npm installs (@uiw/react-codemirror, yaml, @codemirror/lang-yaml, @codemirror/lint) | high | mitigate | Package-legitimacy gate: RESEARCH audit table + Task 1 blocking-human checkpoint verifying publisher/repository before `yarn add`; exact scoped names pinned with caret ranges; yarn.lock committed so CI installs the audited versions |
| T-11-27 | Tampering (XSS) | CodeMirror rendering of compose content | low | accept | CodeMirror renders document text as DOM text nodes (no innerHTML of user content); it replaces a text area with the same content — no new injection surface (RESEARCH Security Domain) |
| T-11-28 | Denial of Service | yaml parse on every change in the linter | low | accept | CodeMirror's linter debounces by default; compose files are small; parse errors are caught and returned as diagnostics |
| T-11-29 | Information Disclosure | compose content | low | accept | Same content already shown by the text area; nothing new is fetched |
</threat_model>

<verification>
- `yarn workspace @docktor/client test` green; `PLAYWRIGHT_PORT=5178 yarn workspace @docktor/client test:integration stacks.spec.ts` green; `yarn workspace @docktor/client exec tsc -b` clean
</verification>

<success_criteria>
- Human-verified packages installed; CodeMirror YAML editor with inline syntax errors on the Config tab and the Create page; dirty guard intact.
</success_criteria>

<output>
Create `.planning/phases/11-ui-rework/11-09-SUMMARY.md` when done, including the package-verification answer and the CodeEditor props (plan 11-12 reuses it).
</output>
