---
phase: 11-ui-rework
plan: 11
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - client/src/routes/app/settings.tsx
  - client/src/routes/app/settings/components/timezone-combobox.tsx
  - client/src/routes/app/settings/components/general-settings-card.tsx
  - client/src/routes/app/settings/components/smtp-card.tsx
  - client/src/routes/app/settings/components/notification-triggers-card.tsx
  - client/src/routes/app/settings/components/notification-log-card.tsx
  - client/src/routes/app/settings/components/backup-repository-card.tsx
  - client/src/routes/app/settings/components/backup-defaults-card.tsx
  - client/src/components/domain/notification/notification-type-badge.tsx
  - client/test/unit/routes/settings/settings-page.test.tsx
  - client/test/unit/components/domain/notification/notification-type-badge.test.tsx
autonomous: true
requirements:
  - "GH-15 (docktor-app/docktor#15) — this plan closes CLAUDE.md's Known Refactoring Target for routes/app/settings.tsx (11-CONTEXT.md: 'this phase should close all three'), applies the phase's clean-component-architecture goal (SRP, one section per file) to Settings, and brings the notification-type pill onto the D-08 single badge scheme."

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "client/src/routes/app/settings.tsx is a composition-only page of at most 80 lines: it resolves the tab, renders the Page shell and the four TabsContent sections, and defines no component, helper or form state of its own."
    - "Every Settings card lives in its own file under client/src/routes/app/settings/components/ — General, Timezone combobox, SMTP, Notification Triggers, Notification Log, Backup Repository, Backup Defaults — next to the existing ProxySettingsCard and CertificatesCard."
    - "The extraction is behaviour-preserving: every Settings tab shows the same headings, fields, labels, save/test actions, toasts and field-error messages as before."
    - "D-08: the Notification Log's type column renders through NotificationTypeBadge on ToneBadge (Error red, Unhealthy yellow, Disk orange, unknown types neutral with their raw value) — no hand-rolled pill classes remain in Settings."
    - "The Settings tab list scrolls horizontally inside its own container on narrow screens instead of widening the page (D-17 baseline)."
  prohibitions:
    - "No change to settings API clients, request payloads or server endpoints."
    - "Settings cards stay Cards — each is a genuinely distinct settings group (D-03 reserves Card for exactly this)."
    - "No conversion of the settings forms to react-hook-form in this plan (a behaviour-preserving move only); the residual ad-hoc form state is recorded as a Known Refactoring Target by plan 11-13."
  artifacts:
    - "client/src/routes/app/settings/components/timezone-combobox.tsx — TimezoneCombobox"
    - "client/src/routes/app/settings/components/general-settings-card.tsx — GeneralSettingsCard"
    - "client/src/routes/app/settings/components/smtp-card.tsx — SmtpCard"
    - "client/src/routes/app/settings/components/notification-triggers-card.tsx — NotificationTriggersCard"
    - "client/src/routes/app/settings/components/notification-log-card.tsx — NotificationLogCard"
    - "client/src/routes/app/settings/components/backup-repository-card.tsx — BackupRepositoryCard"
    - "client/src/routes/app/settings/components/backup-defaults-card.tsx — BackupDefaultsCard"
    - "client/src/components/domain/notification/notification-type-badge.tsx — NotificationTypeBadge, describeNotificationType()"
  key_links:
    - "backups.spec.ts's three Settings Backup-tab tests (repository fields, SFTP and S3 conditional fields) exercise BackupRepositoryCard by label — they must pass unmodified; this plan does not edit that spec (plan 11-07 owns it in this wave)."
    - "NotificationLogCard keeps its useContainerEvents subscription so new notifications still appear live."
---

**Implements:** [docktor-app/docktor#15](https://github.com/docktor-app/docktor/issues/15) — CLAUDE.md Known Refactoring Target (settings.tsx), clean component architecture, D-08 (notification pill).

<objective>
Split the 1125-line `settings.tsx` — five inline card components, a combobox, two helpers and the General form's state — into one file per section under `routes/app/settings/components/`, following the ProxySettingsCard/CertificatesCard precedent, leaving a ≤ 80-line page orchestrator. Move the notification-type pill onto ToneBadge.

Purpose: CLAUDE.md's Known Refactoring Target (RESEARCH Pitfall 5 sized it at ~980 lines of inline components); 11-CONTEXT.md asks this phase to close all three targets.
Output: seven extracted section files, NotificationTypeBadge, a thin settings.tsx, a settings page smoke test.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-ui-rework/11-CONTEXT.md
@.planning/phases/11-ui-rework/11-RESEARCH.md
@.planning/phases/11-ui-rework/11-02-SUMMARY.md
@CLAUDE.md
@client/src/routes/app/settings/components/proxy-settings-card.tsx
</context>

## Planning notes

- Spec-less probe fallback skipped (no REQUIREMENTS.md-backed IDs); truths derive from CLAUDE.md's Known Refactoring Targets, the phase goal's clean-architecture clause, and D-08.
- Section map (verified during planning): TimezoneCombobox lines 42-93, getTypeBadgeClass/formatType 95-111, SmtpCard 113-330, NotificationTriggersCard 332-520, NotificationLogCard 522-604, BackupRepositoryCard 606-832, BackupDefaultsCard 834-952, SettingsPage (with inline General card and its form state) 954-1125.

## Artifacts this phase produces

| Artifact | Kind | Status |
|----------|------|--------|
| `TimezoneCombobox` | component in `settings/components/timezone-combobox.tsx` | moved |
| `GeneralSettingsCard` | component in `settings/components/general-settings-card.tsx` (owns the General form state previously in SettingsPage) | new (extracted) |
| `SmtpCard`, `NotificationTriggersCard`, `NotificationLogCard`, `BackupRepositoryCard`, `BackupDefaultsCard` | components, one file each in `settings/components/` | moved |
| `NotificationTypeBadge` | component in `client/src/components/domain/notification/notification-type-badge.tsx` | new |
| `describeNotificationType(type)` | exported pure function → `{label, tone}` (replaces getTypeBadgeClass/formatType) | new |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the Notifications tab sections and the notification-type badge</name>
  <files>client/src/routes/app/settings.tsx, client/src/routes/app/settings/components/smtp-card.tsx, client/src/routes/app/settings/components/notification-triggers-card.tsx, client/src/routes/app/settings/components/notification-log-card.tsx, client/src/components/domain/notification/notification-type-badge.tsx, client/test/unit/components/domain/notification/notification-type-badge.test.tsx, client/test/unit/routes/settings/settings-page.test.tsx</files>
  <read_first>
    - client/src/routes/app/settings.tsx lines 1-37 (imports), 95-111 (type helpers), 113-604 (SmtpCard, NotificationTriggersCard, NotificationLogCard), 1106-1112 (Notifications TabsContent)
    - client/src/routes/app/settings/components/proxy-settings-card.tsx — the extracted-card file shape to copy (named export, Readonly props, imports grouped external → workspace → local)
    - client/src/components/common/tone-badge.tsx (11-02)
    - client/src/lib/notifications-api.ts — NotificationEntry and the API functions the cards call
    - client/test/unit/routes/settings/certificates-card.test.tsx — mocking style for settings cards
  </read_first>
  <behavior>
    - describeNotificationType("stack_error") → {label: "Error", tone: "red"}; "stack_unhealthy" → {"Unhealthy", "yellow"}; "disk_warning" → {"Disk", "orange"}; "something_new" → {label: "something_new", tone: "neutral"}
    - SettingsPage at /settings/notifications renders headings SMTP, Notification Triggers and Notification Log (APIs mocked)
  </behavior>
  <action>
    Behaviour-preserving move (CLAUDE.md Known Refactoring Target):

    1. Create `smtp-card.tsx`, `notification-triggers-card.tsx`, `notification-log-card.tsx` in `routes/app/settings/components/`, each containing exactly one exported component moved verbatim from settings.tsx with only the imports it uses. Where a moved component has an untyped or `any`-typed catch parameter or missing `Readonly<>` props, tighten it in place (CLAUDE.md TypeScript standards) without changing behaviour.
    2. `components/domain/notification/notification-type-badge.tsx`: `describeNotificationType(type: string): {label: string; tone: Tone}` per `<behavior>` and `NotificationTypeBadge({type})` rendering ToneBadge. NotificationLogCard uses it in place of the old class-helper + label-helper pair, which are deleted.
    3. settings.tsx imports the three cards; delete the moved code and the now-unused imports.
    4. Tests: notification-type-badge.test.tsx and the first cases of settings-page.test.tsx (MemoryRouter at /settings/notifications with `@/lib/notifications-api`, `@/lib/settings-api`, `@/lib/backups-api`, `@/lib/proxy-api`, `@/lib/certificates-api` and use-container-events mocked).
  </action>
  <verify>
    <automated>yarn workspace @docktor/client test test/unit/components/domain/notification/notification-type-badge.test.tsx test/unit/routes/settings</automated>
    <fails_when>non-zero exit, or the vitest "Test Files" summary reports a failed file</fails_when>
    <automated>yarn workspace @docktor/client exec tsc -b</automated>
    <fails_when>non-zero exit, or any "error TS" line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'function SmtpCard\|function NotificationTriggersCard\|function NotificationLogCard' client/src/routes/app/settings.tsx` prints 0
    - `grep -c 'getTypeBadgeClass' client/src/routes/app/settings/components/notification-log-card.tsx` prints 0 and `grep -c 'NotificationTypeBadge' client/src/routes/app/settings/components/notification-log-card.tsx` prints ≥ 1
    - Each of the three new card files exports exactly one component
  </acceptance_criteria>
  <done>The Notifications tab's three sections live in their own files and the type pill uses the shared badge scheme; behaviour is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract General, Timezone and Backup sections and reduce settings.tsx to a ≤ 80-line orchestrator</name>
  <files>client/src/routes/app/settings.tsx, client/src/routes/app/settings/components/timezone-combobox.tsx, client/src/routes/app/settings/components/general-settings-card.tsx, client/src/routes/app/settings/components/backup-repository-card.tsx, client/src/routes/app/settings/components/backup-defaults-card.tsx, client/test/unit/routes/settings/settings-page.test.tsx</files>
  <read_first>
    - client/src/routes/app/settings.tsx (after Task 1) — TimezoneCombobox and TIMEZONES, BackupRepositoryCard, BackupDefaultsCard, SettingsPage with its General form state, load effect, handleSaveGeneral error mapping and the General TabsContent
    - client/test/integration/backups.spec.ts lines 369-420 — Settings Backup-tab assertions (labels) that must keep passing
  </read_first>
  <behavior>
    - SettingsPage at /settings/general renders the General card with Instance name / Base URL / Timezone fields populated from getGeneralSettings; a 400 ApiError mentioning "instance name" shows under the Instance name field
    - /settings/backup renders Backup Repository and Default Backup Settings; /settings/proxy renders the proxy settings and certificates cards; an unknown tab falls back to General
  </behavior>
  <action>
    1. `timezone-combobox.tsx`: move `TimezoneCombobox` (and the `TIMEZONES` constant it needs) verbatim; props interface renamed `TimezoneComboboxProps` with `Readonly<>`.
    2. `general-settings-card.tsx`: `GeneralSettingsCard()` owns the state, load effect, `handleSaveGeneral` (including its ApiError → field-error mapping and toasts) and the General Card markup moved from SettingsPage.
    3. `backup-repository-card.tsx`, `backup-defaults-card.tsx`: move each component verbatim with only its imports.
    4. `settings.tsx`: only `VALID_TABS`, tab resolution, navigate-on-change, PageHeader, and Tabs with four TabsContent blocks rendering the section components. Wrap TabsList in `div className="-mx-1 overflow-x-auto px-1"` with `className="w-max"` on the list. ≤ 80 lines; remove every unused import.
    5. Extend settings-page.test.tsx per `<behavior>`.
  </action>
  <verify>
    <automated>yarn workspace @docktor/client test test/unit/routes/settings</automated>
    <fails_when>non-zero exit, or the vitest "Test Files" summary reports a failed file</fails_when>
    <automated>test "$(wc -l < client/src/routes/app/settings.tsx)" -le 80 && yarn workspace @docktor/client exec tsc -b</automated>
    <fails_when>non-zero exit — settings.tsx exceeds 80 lines or tsc prints an "error TS" line</fails_when>
    <automated>PLAYWRIGHT_PORT=5180 yarn workspace @docktor/client test:integration backups.spec.ts</automated>
    <fails_when>non-zero exit, or a "failed" count in the Playwright summary (the Settings Backup-tab tests are the regression net)</fails_when>
  </verify>
  <acceptance_criteria>
    - `wc -l < client/src/routes/app/settings.tsx` prints a number ≤ 80
    - `grep -cE '^(function|const [A-Za-z]+ = \()' client/src/routes/app/settings.tsx` prints 0 (no inline components or helpers; the page itself is `export default function`)
    - `ls client/src/routes/app/settings/components/` lists all nine card/section files (7 new + proxy-settings-card + certificates-card)
    - The three Settings Backup-tab E2E tests pass unmodified
  </acceptance_criteria>
  <done>settings.tsx is a ≤ 80-line orchestrator; every Settings section lives in its own file with unchanged behaviour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings forms → settings APIs | SMTP credentials, backup repository credentials and general settings are submitted to existing authenticated endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-33 | Information Disclosure | SMTP / backup repository secret fields | medium | mitigate | Verbatim move preserves today's handling (write-only secrets surfaced only as hasPassword/hasSftpKey/hasS3SecretKey flags); the settings-page test asserts no secret value is pre-filled from the API response |
| T-11-34 | Tampering (XSS) | notification log messages | low | accept | Rendered as React text, unchanged |
| T-11-35 | Repudiation | behaviour drift during extraction | low | mitigate | Unit smoke tests per tab plus the unmodified backups.spec.ts Settings tests serve as the behaviour-preservation net |
</threat_model>

<verification>
- `yarn workspace @docktor/client test` green; `PLAYWRIGHT_PORT=5180 yarn workspace @docktor/client test:integration backups.spec.ts` green; `yarn workspace @docktor/client exec tsc -b` clean
</verification>

<success_criteria>
- settings.tsx ≤ 80 lines with no inline components; seven sections extracted; notification pill on ToneBadge; behaviour unchanged.
</success_criteria>

<output>
Create `.planning/phases/11-ui-rework/11-11-SUMMARY.md` when done, listing any ad-hoc (non-react-hook-form) form state that remains in the extracted cards for 11-13's CLAUDE.md update.
</output>
