---
phase: 11-ui-rework
plan: 06
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - client/src/lib/stack-event-description.ts
  - client/src/hooks/use-stack-timeline.ts
  - client/src/routes/app/stacks/components/activity-timeline.tsx
  - client/src/routes/app/stacks/components/overview-tab.tsx
  - client/src/routes/app/stacks/components/event-log-card.tsx
  - client/src/routes/app/stacks/components/status-log-card.tsx
  - client/src/routes/app/stacks/components/services-tab.tsx
  - client/src/routes/app/stacks/components/services-section.tsx
  - client/src/routes/app/stacks/components/stack-detail-header.tsx
  - client/test/unit/lib/stack-event-description.test.ts
  - client/test/unit/hooks/use-stack-timeline.test.ts
  - client/test/unit/routes/stacks/activity-timeline.test.tsx
  - client/test/unit/routes/stacks/event-log-card.test.tsx
  - client/test/unit/routes/stacks/services-section.test.tsx
  - client/test/unit/routes/stacks/stack-detail-header.test.tsx
autonomous: true
requirements:
  - "GH-15 (docktor-app/docktor#15) — this plan implements D-07 (one unified, type-filterable activity timeline replacing the Deployments/Status/Event cards), the Overview half of D-03, the detail-header half of D-09, D-10's per-service placement, and D-11's service-color reuse in the services table. ROADMAP success criterion 2."

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-07: The Overview tab shows one 'Activity' section merging deployments, status transitions and configuration/update events into a single list sorted newest first, replacing the three stacked Recent Deployments / Status Log / Event Log cards."
    - "D-07: A Select labelled 'Filter activity by type' narrows the timeline to All activity, Deployments, Status changes, or Events; a filter with no matches shows 'No matching activity.'"
    - "UI-SPEC empty row: with no deployments, status logs or events the timeline shows the heading 'No activity yet' and the body 'Deploys, status changes, and events for this stack will appear here.'"
    - "UI-SPEC loading row: while the events request is in its initial load the timeline shows Skeleton rows instead of the list."
    - "UI-SPEC error row: if the events request fails, the timeline still shows deployment and status entries, plus a destructive Alert with the error and a Retry button that re-runs the events request; a successful retry clears the alert."
    - "UI-SPEC populated / overflow rows: the list sits inside a ScrollArea capped at h-96, newest first."
    - "UI-SPEC long-text row: long deployment errors, status messages and event descriptions wrap in place (break-words) and are never truncated."
    - "D-07 liveness: timeline entries update without a reload when useStack's SSE-driven deployments/statusLogs change or useStackEvents refetches — no new endpoint and no new SSE event type."
    - "D-03: The Overview tab renders no Card — Services and Activity are flat Section elements."
    - "D-09: The stack detail header shows the blue 'update available' pill next to the status badge whenever any service has updateAvailable true, in the same flex-wrap container."
    - "D-10: Each service row shows 'Update available → {latestTag}' or 'Content updated' via ServiceUpdateBadge instead of the old hand-rolled pill."
    - "D-11: Each service row shows a small dot in that service's deterministic color (getServiceColor), matching the color its log lines use in the log viewer."
    - "UI-SPEC spacing exception: per-service icon buttons (upgrade, view logs) have accessible names and are at least 44px below the md breakpoint."
  prohibitions:
    - "No new server endpoint (e.g. /api/stacks/:id/timeline) and no new SSE event type — the merge is a pure client-side useMemo over data the page already has (RESEARCH anti-pattern)."
    - "useStack and useStackEvents are not modified."
  artifacts:
    - "client/src/lib/stack-event-description.ts — describeStackEvent() + event tone/label mapping (moved from event-log-card.tsx)"
    - "client/src/hooks/use-stack-timeline.ts — TimelineEntry, TimelineFilter, buildTimeline(), filterTimeline(), useStackTimeline()"
    - "client/src/routes/app/stacks/components/activity-timeline.tsx — ActivityTimeline"
    - "client/src/routes/app/stacks/components/services-section.tsx — ServicesSection (replaces services-tab.tsx)"
    - "client/src/routes/app/stacks/components/overview-tab.tsx — composes ServicesSection + ActivityTimeline"
  key_links:
    - "OverviewTab keeps its 11-01 props {stack, onViewLogs, onUpgraded}; it now calls useStackEvents(stack.id) and useStackTimeline(stack.deployments, stack.statusLogs, events) itself, so [id].tsx is untouched."
    - "stack-detail-page.test.tsx mocks the overview-tab module (11-01), so deleting event-log-card.tsx / status-log-card.tsx and renaming services-tab.tsx cannot break it."
    - "describeStackEvent's payload-parsing behaviour (malformed / empty / absent payload never throws; hash pair appended as 7-char short forms) is a Phase 2 contract carried over unchanged into lib/stack-event-description.ts with its existing test cases."
---

**Implements:** [docktor-app/docktor#15](https://github.com/docktor-app/docktor/issues/15) — decisions D-07, D-03 (Overview), D-09 (header), D-10, D-11.

<objective>
Replace the Overview tab's three stacked log Cards with one clear, filterable activity timeline, flatten the Services card into a section with per-service color, D-10 update copy and accessible actions, and put the stack-level update pill in the detail header.

Purpose: D-07 is the phase's headline consolidation (ROADMAP success criterion 2); D-03/D-09/D-10/D-11 land on the same screen.
Output: stack-event-description lib, useStackTimeline hook, ActivityTimeline, ServicesSection, updated OverviewTab and StackDetailHeader; EventLogCard/StatusLogCard deleted.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-ui-rework/11-CONTEXT.md
@.planning/phases/11-ui-rework/11-RESEARCH.md
@.planning/phases/11-ui-rework/11-UI-SPEC.md
@.planning/phases/11-ui-rework/11-01-SUMMARY.md
@.planning/phases/11-ui-rework/11-02-SUMMARY.md
@CLAUDE.md
</context>

## Planning notes

- Spec-less probe fallback skipped (no REQUIREMENTS.md-backed IDs); truths derive from D-07/D-03/D-09/D-10/D-11 and the UI-SPEC "Unified timeline" rows (partial and zero-one-many are dismissed in the UI-SPEC and not planned).
- The timeline is co-located under `routes/app/stacks/components/` (not `components/domain/stack/` as RESEARCH sketched) because it is used by exactly one page — CLAUDE.md's co-location rule. `services-tab.tsx` is renamed `services-section.tsx` because it is a section of the Overview tab, not a tab.

## Artifacts this phase produces

| Artifact | Kind | Status |
|----------|------|--------|
| `describeStackEvent(entry)` | exported function in `client/src/lib/stack-event-description.ts` (moved from event-log-card.tsx; now returns `{label, description, tone}`) | moved/modified |
| `TimelineEntryType` | exported type `"deployment" \| "status" \| "event"` in `client/src/hooks/use-stack-timeline.ts` | new |
| `TimelineEntry` | exported discriminated union | new |
| `TimelineFilter` | exported type `"all" \| TimelineEntryType` | new |
| `buildTimeline(deployments, statusLogs, events)` | exported pure function | new |
| `filterTimeline(entries, filter)` | exported pure function | new |
| `useStackTimeline(deployments, statusLogs, events)` | exported hook | new |
| `ActivityTimeline` / `ActivityTimelineProps` | component in `routes/app/stacks/components/activity-timeline.tsx` | new |
| `ServicesSection` / `ServicesSectionProps` | component in `routes/app/stacks/components/services-section.tsx` | new (replaces `ServicesTab`) |
| `event-log-card.tsx`, `status-log-card.tsx`, `services-tab.tsx`, `event-log-card.test.tsx` | files | deleted |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unified, filterable activity timeline replacing the three Overview log cards</name>
  <files>client/src/lib/stack-event-description.ts, client/src/hooks/use-stack-timeline.ts, client/src/routes/app/stacks/components/activity-timeline.tsx, client/src/routes/app/stacks/components/overview-tab.tsx, client/src/routes/app/stacks/components/event-log-card.tsx, client/src/routes/app/stacks/components/status-log-card.tsx, client/test/unit/lib/stack-event-description.test.ts, client/test/unit/hooks/use-stack-timeline.test.ts, client/test/unit/routes/stacks/activity-timeline.test.tsx, client/test/unit/routes/stacks/event-log-card.test.tsx</files>
  <read_first>
    - client/src/routes/app/stacks/components/overview-tab.tsx — content moved there by 11-01 (Recent Deployments block, StatusLogCard, EventLogCard)
    - client/src/routes/app/stacks/components/event-log-card.tsx — describeStackEvent, LABELS, loading/error/retry gate (lines 78-117)
    - client/src/routes/app/stacks/components/status-log-card.tsx — status transition rendering (from → to, message)
    - client/test/unit/routes/stacks/event-log-card.test.tsx — describeStackEvent cases (lines 162-199) to carry over, and the retry case pattern
    - client/src/hooks/use-stack-events.ts — {events, loading, error, refetch}; events is null after a failed initial load
    - client/src/lib/stacks-api.ts — StackDetail["deployments"], StackDetail["statusLogs"], StackEvent shapes
    - client/src/components/common/layout/section.tsx, client/src/components/common/tone-badge.tsx, client/src/components/domain/stack/stack-status-badge.tsx (display="compact")
    - .planning/phases/11-ui-rework/11-UI-SPEC.md §Copywriting (timeline empty state) and §UI Considerations "Unified timeline" rows
  </read_first>
  <behavior>
    - buildTimeline merges one deployment (deployedAt T1), one status log (createdAt T3) and one event (createdAt T2) into [status, event, deployment] (newest first); equal timestamps keep source order deployments → status logs → events; a null events argument contributes nothing
    - filterTimeline(entries, "deployment") keeps only deployment entries; "all" returns every entry
    - describeStackEvent keeps its four existing cases and returns tones: config_changed yellow, config_error red, update_available blue
    - ActivityTimeline: empty → "No activity yet" + body copy; eventsLoading → skeleton rows (role="status", aria-label "Loading activity"); eventsError → destructive Alert + "Retry" button calling onRetryEvents while deployment/status entries still render; choosing "Deployments" in the "Filter activity by type" select hides status/event entries; a filter with no matches renders "No matching activity."
    - a deployment entry renders "Deploy succeeded" (green) or "Deploy failed" (red) plus its errorMessage; a status entry renders "{from} → {to}" with a compact status badge for the target status and its message; entry text containers carry break-words
  </behavior>
  <action>
    Per D-07 and D-03 (Overview):

    1. `lib/stack-event-description.ts`: move `describeStackEvent` and its label map out of event-log-card.tsx unchanged in behaviour, replacing the Badge-variant map with a `Tone` map (config_changed "yellow", config_error "red", update_available "blue"); return type `{label: string; description: string; tone: Tone}`.
    2. `hooks/use-stack-timeline.ts`: export the types from the Artifacts table. `TimelineEntry` = `{type: "deployment"; id; timestamp; success; errorMessage}` | `{type: "status"; id; timestamp; fromStatus; toStatus; message}` | `{type: "event"; id; timestamp; event: StackEvent}` — each variant also keeps a `key` of `${type}:${id}` for React keys (ids from different tables can collide). `buildTimeline` maps deployments (timestamp = deployedAt), statusLogs (createdAt) and events (createdAt), then stable-sorts by `Date.parse(timestamp)` descending. `useStackTimeline` wraps it in `useMemo` keyed on the three inputs. `filterTimeline` is pure.
    3. `activity-timeline.tsx`: `ActivityTimeline({entries, eventsLoading, eventsError, onRetryEvents})`. `Section` with `SectionHeader` → `SectionTitle` "Activity" and `SectionActions` holding a shadcn `Select` (trigger `aria-label="Filter activity by type"`, options All activity / Deployments / Status changes / Events; filter state local via useState). Body: the `<behavior>` states in this order — error Alert (above the list, when eventsError is non-null), skeleton when eventsLoading, empty state when there are no entries at all, "No matching activity." when the filter empties the list, else a `ScrollArea className="h-96"` list. Each row: timestamp (`text-xs text-muted-foreground whitespace-nowrap`, `toLocaleString()`), a ToneBadge type label, and a `min-w-0 break-words text-sm` description. Status rows use `StackStatusBadge display="compact"` for the target status. Rows stack vertically below `sm` (`flex flex-col gap-1 sm:flex-row sm:items-start sm:gap-3`) so timestamps never force horizontal scroll on phones.
    4. `overview-tab.tsx`: keep props `{stack, onViewLogs, onUpgraded}`; call `useStackEvents(stack.id)` and `useStackTimeline(stack.deployments, stack.statusLogs, events)`; render the existing services component (Task 2 replaces it) and `ActivityTimeline` with `eventsLoading={loading}`, `eventsError={events === null ? error : null}`, `onRetryEvents={refetch}` inside `div className="space-y-8"`. Remove the Recent Deployments block and the two card imports.
    5. Delete `event-log-card.tsx`, `status-log-card.tsx` and `client/test/unit/routes/stacks/event-log-card.test.tsx`; move its describeStackEvent cases into `stack-event-description.test.ts` and its loading/empty/error/retry intent into `activity-timeline.test.tsx`. Add `use-stack-timeline.test.ts`.
  </action>
  <verify>
    <automated>yarn workspace @docktor/client test test/unit/lib/stack-event-description.test.ts test/unit/hooks/use-stack-timeline.test.ts test/unit/routes/stacks/activity-timeline.test.tsx test/unit/routes/stacks/stack-detail-page.test.tsx</automated>
    <fails_when>non-zero exit, or the vitest "Test Files" summary reports a failed file, or "No test files found"</fails_when>
    <automated>test ! -e client/src/routes/app/stacks/components/event-log-card.tsx && test ! -e client/src/routes/app/stacks/components/status-log-card.tsx && yarn workspace @docktor/client exec tsc -b</automated>
    <fails_when>non-zero exit — one of the old card files still exists or tsc prints an "error TS" line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'EventLogCard\|StatusLogCard' client/src` prints nothing
    - `grep -vE '^\s*(//|\*)' client/src/routes/app/stacks/components/overview-tab.tsx client/src/routes/app/stacks/components/activity-timeline.tsx | grep -c '<Card'` prints 0
    - `grep -c 'No activity yet' client/src/routes/app/stacks/components/activity-timeline.tsx` prints 1 and `grep -c 'Filter activity by type' client/src/routes/app/stacks/components/activity-timeline.tsx` prints 1
    - `grep -c 'break-words' client/src/routes/app/stacks/components/activity-timeline.tsx` prints ≥ 1 and `grep -c 'h-96' client/src/routes/app/stacks/components/activity-timeline.tsx` prints ≥ 1
    - `grep -c 'stacks/:id/activity' server/src/routes/stacks.ts` prints 0 and `git status --porcelain server/` prints nothing after this plan's commits (no server change, no new endpoint)
  </acceptance_criteria>
  <done>The Overview tab shows one filterable, live-updating Activity timeline with loading, empty, error/retry and overflow handling; the three old cards and their test are gone, their behaviour carried into the new tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flat Services section with service colors and D-10 update copy, plus the stack-level update pill in the header</name>
  <files>client/src/routes/app/stacks/components/services-tab.tsx, client/src/routes/app/stacks/components/services-section.tsx, client/src/routes/app/stacks/components/overview-tab.tsx, client/src/routes/app/stacks/components/stack-detail-header.tsx, client/test/unit/routes/stacks/services-section.test.tsx, client/test/unit/routes/stacks/stack-detail-header.test.tsx</files>
  <read_first>
    - client/src/routes/app/stacks/components/services-tab.tsx — full file: UPGRADE_BLOCKED_STATES and its comment, table columns, the hand-rolled update pill, inline JSON.parse ports, title-only icon buttons, ServiceUpgradeDialog wiring
    - client/src/routes/app/stacks/components/stack-detail-header.tsx — the flex-wrap badge container 11-01 prepared
    - client/src/lib/service-color.ts, client/src/lib/service-ports.ts, client/src/components/domain/stack/service-update-badge.tsx, client/src/components/domain/stack/stack-update-badge.tsx, client/src/components/domain/stack/service-status-badge.tsx (from 11-02)
    - client/src/components/ui/tooltip.tsx — Tooltip exports for icon-button labels
    - client/test/unit/routes/stacks/service-upgrade-dialog.test.tsx — how the dialog is mocked/driven
  </read_first>
  <behavior>
    - ServicesSection renders heading "Services"; each row shows a color dot whose class equals getServiceColor(serviceName), the service name, a compact status indicator, the image, and "Update available → 1.2.3" or "Content updated" when updateAvailable
    - Ports render via formatPorts; a malformed ports string renders "-" instead of throwing
    - The upgrade button (named "Upgrade {service}") appears only when updateAvailable, is disabled with an explanatory label while the stack is DEPLOYING/UPDATING/BACKING_UP/RESTORING/MIGRATING, and opens ServiceUpgradeDialog; "View logs for {service}" calls onViewLogs(serviceName)
    - An empty services list renders "No services defined"
    - StackDetailHeader shows "update available" next to the status badge when a service has updateAvailable true, and not otherwise
  </behavior>
  <action>
    Per D-03, D-09, D-10, D-11:

    1. Create `services-section.tsx` from services-tab.tsx: `ServicesSection` with the same props (`services, stackId, stackStatus, onViewLogs, onUpgraded`), rendered as `Section` → `SectionHeader`/`SectionTitle` "Services" → the existing Table (no Card). Keep `UPGRADE_BLOCKED_STATES` and its explanatory comment verbatim. Name cell: `span className={cn("size-2 shrink-0 rounded-full bg-current", getServiceColor(svc.serviceName))} aria-hidden="true"` before the name (D-11). Status cell: `ServiceStatusBadge ... display="compact"`. Image cell: image text plus `ServiceUpdateBadge updateAvailable latestTag` (D-10). Ports cell: `formatPorts(svc.ports)`. Actions: icon Buttons with `aria-label` (`Upgrade ${name}` / `Cannot upgrade while the stack is ${status}` when blocked, and `View logs for ${name}`), wrapped in Tooltips showing the same text, sized `min-h-11 min-w-11 md:min-h-9 md:min-w-9`.
    2. Delete `services-tab.tsx`; point overview-tab.tsx at `ServicesSection`.
    3. `stack-detail-header.tsx`: render `StackUpdateBadge services={stack.services}` immediately after `StackStatusBadge` inside the existing `flex flex-wrap items-center gap-1` container (D-09 — same placement relationship as the list's config-changed pill).
    4. Tests per `<behavior>`: new services-section.test.tsx (mock ServiceUpgradeDialog as the existing dialog test does) and stack-detail-header.test.tsx (MemoryRouter + SidebarProvider; mock StackActions).
  </action>
  <verify>
    <automated>yarn workspace @docktor/client test test/unit/routes/stacks</automated>
    <fails_when>non-zero exit, or the vitest "Test Files" summary reports a failed file</fails_when>
    <automated>test ! -e client/src/routes/app/stacks/components/services-tab.tsx && yarn workspace @docktor/client exec tsc -b</automated>
    <fails_when>non-zero exit — services-tab.tsx still exists or tsc prints an "error TS" line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -vE '^\s*(//|\*)' client/src/routes/app/stacks/components/services-section.tsx | grep -c 'rounded-full px-2 py-0.5'` prints 0
    - `grep -c 'getServiceColor' client/src/routes/app/stacks/components/services-section.tsx` prints ≥ 1 and `grep -c 'JSON.parse' client/src/routes/app/stacks/components/services-section.tsx` prints 0
    - `grep -c 'StackUpdateBadge' client/src/routes/app/stacks/components/stack-detail-header.tsx` prints ≥ 1
    - `grep -vE '^\s*(//|\*)' client/src/routes/app/stacks/components/services-section.tsx | grep -c '<Card'` prints 0
  </acceptance_criteria>
  <done>The Overview tab is two flat sections; service rows carry their log color, D-10 update copy and accessible 44px actions; the header shows the stack-level update pill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server data → timeline rendering | deployment error messages, status messages, event messages and payload JSON originate from the server/Docker and are rendered to the user |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-17 | Tampering (XSS) | ActivityTimeline entry text | low | mitigate | All messages render as React text children (no dangerouslySetInnerHTML); describeStackEvent parses payload JSON only to read two hash strings, sliced to 7 chars, inside try/catch |
| T-11-18 | Denial of Service | describeStackEvent on malformed payloads | low | mitigate | The never-throws contract and its malformed/empty/absent payload tests are carried over unchanged |
| T-11-19 | Information Disclosure | timeline | low | accept | Shows the same deployment/status/event records the three cards already showed |
</threat_model>

<verification>
- `yarn workspace @docktor/client test` green; `yarn workspace @docktor/client exec tsc -b` clean
</verification>

<success_criteria>
- One Activity timeline with type filter and all UI-SPEC states replaces three Cards; no new endpoint.
- Flat Services section with service color dots, D-10 copy and accessible actions; header shows the stack-level update pill.
</success_criteria>

<output>
Create `.planning/phases/11-ui-rework/11-06-SUMMARY.md` when done.
</output>
