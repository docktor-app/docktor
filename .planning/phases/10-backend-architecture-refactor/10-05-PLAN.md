---
phase: 10-backend-architecture-refactor
plan: 05
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - server/src/application/ports/certificate-filesystem-port.ts
  - server/src/application/ports/registry-client-port.ts
  - server/src/application/ports/brownfield-scanner-port.ts
  - server/src/application/ports/compose-analyzer-port.ts
  - server/src/application/ports/compose-rewriter-port.ts
  - server/src/application/ports/volume-migrator-port.ts
  - server/src/infrastructure/certificate-filesystem.ts
  - server/src/infrastructure/registry-client.ts
  - server/src/infrastructure/brownfield-scanner.ts
  - server/src/infrastructure/compose-analyzer.ts
  - server/src/infrastructure/compose-rewriter.ts
  - server/src/infrastructure/volume-migrator.ts
  - server/test/unit/architecture/layering.test.ts
autonomous: true
requirements: ["D-01", "D-06", "D-07"]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every class under server/src/infrastructure/ has a named port interface in server/src/application/ports/ and declares `implements` against it (D-07 read broadly per PD-2)"
    - "An automated test fails if a new class is added under server/src/infrastructure/ without a matching `implements <Name>Port` clause, so the convention holds without review vigilance"
    - "No runtime behaviour changes: no method body, argument list, spawn invocation, HTTP call or return shape is edited by this plan"
  artifacts:
    - server/src/application/ports/certificate-filesystem-port.ts
    - server/src/application/ports/registry-client-port.ts
    - server/src/application/ports/brownfield-scanner-port.ts
    - server/src/application/ports/compose-analyzer-port.ts
    - server/src/application/ports/compose-rewriter-port.ts
    - server/src/application/ports/volume-migrator-port.ts
  key_links:
    - "server/test/unit/architecture/layering.test.ts -> the infrastructure-port rule (every infrastructure class implements a port)"
  prohibitions:
    - "A port is trimmed to only the members one caller happens to use today, so the next consumer has to widen it or reach for the concrete class again"
    - "A port interface duplicates a data shape instead of importing its single existing declaration, creating two definitions that can drift"
    - "An error class exported alongside a concrete class (for example the registry-unavailable error) is turned into a port member or relocated while the class is being interfaced"
    - "A method body, HTTP call or path-resolution routine is 'tidied' while being interfaced"
---

<objective>
Complete D-07 across the whole infrastructure directory: the six classes plan 10-02 did not cover each get a named port and an `implements` clause, and the architecture fitness test gains a rule that keeps the convention true for every class added later.

This is deliberate scope, not scope creep. PD-2 (recorded in `10-01-PLAN.md`) reads D-07's "Interface **every** infrastructure dependency, even single-implementation ones" as the governing sentence and its four named classes as examples, backed by D-01's "stricter DI conventions across the board" and D-06's commitment to full scope. Stopping at the four named classes would be a descope decision this phase forbids.

Like 10-02, this plan is declaration-only. Consumers switch to the port types in plans 10-06 (application services) and 10-07 (jobs).

Purpose: without the fitness rule, the port convention survives exactly as long as the next author remembers it.
Output: six port files, six `implements` clauses, and one new rule in the layering test.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-backend-architecture-refactor/10-CONTEXT.md
@.planning/phases/10-backend-architecture-refactor/10-PATTERNS.md
@.planning/phases/10-backend-architecture-refactor/10-02-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
Created by this plan: `CertificateFilesystemPort`, `RegistryClientPort`, `BrownfieldScannerPort`, `ComposeAnalyzerPort`, `ComposeRewriterPort`, `VolumeMigratorPort` and their six files under `server/src/application/ports/`; the infrastructure-port rule inside `server/test/unit/architecture/layering.test.ts`. The phase-wide inventory lives in `10-01-PLAN.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Ports for CertificateFilesystem and RegistryClient</name>
  <files>server/src/application/ports/certificate-filesystem-port.ts, server/src/application/ports/registry-client-port.ts, server/src/infrastructure/certificate-filesystem.ts, server/src/infrastructure/registry-client.ts</files>
  <read_first>
    - server/src/infrastructure/certificate-filesystem.ts (the class being interfaced — enumerate every public member and exact signature; note its consumer is `application/certificate-service.ts`, which already imports it as a type)
    - server/src/infrastructure/registry-client.ts (the class being interfaced — note `RegistryUnavailableError` is exported from this file as an `AppError` subclass and is **not** part of the port)
    - server/src/application/ports/docker-executor-port.ts (written in 10-02 — the exact file shape, doc-comment style and naming convention to match)
  </read_first>
  <action>
Create `server/src/application/ports/certificate-filesystem-port.ts` exporting `CertificateFilesystemPort`, listing every public member of `CertificateFilesystem` with its exact current signature. Import any result or argument shape from its existing declaration site rather than restating it. Carry the same doc-comment rationale 10-02's ports carry.

Create `server/src/application/ports/registry-client-port.ts` exporting `RegistryClientPort`, listing every public member of `RegistryClient`. `RegistryUnavailableError` stays exactly where it is, exported from the infrastructure module as today — it is a typed error in the `lib/errors.ts` hierarchy that callers catch by identity, not a port member, and relocating it would change what `update-checker.ts`'s catch block matches.

Add `implements CertificateFilesystemPort` and `implements RegistryClientPort` to the two class declarations, importing each port with a type-only import.

Change nothing else in either infrastructure file. If the compiler reports a mismatch, the port is corrected to match the class, never the reverse.
  </action>
  <acceptance_criteria>
    - Both port files exist, each exporting one interface that covers the full public surface of its class.
    - `grep -cE '^export class CertificateFilesystem implements CertificateFilesystemPort' server/src/infrastructure/certificate-filesystem.ts` prints `1`.
    - `grep -cE '^export class RegistryClient implements RegistryClientPort' server/src/infrastructure/registry-client.ts` prints `1`.
    - `grep -cE '^export class RegistryUnavailableError' server/src/infrastructure/registry-client.ts` prints `1` — the error class is still exported from the same module.
    - `git diff` on both infrastructure files shows no change inside any method body.
  </acceptance_criteria>
  <reversibility rating="reversible">Type-level only.</reversibility>
  <verify>
    <automated>yarn typecheck</automated>
    <fails_when>Non-zero exit, or any line containing `error TS` appears in the output.</fails_when>
    <automated>yarn workspace @docktor/server test:unit test/unit/infrastructure/registry-client.test.ts</automated>
    <fails_when>Non-zero exit, or the run summary reports a non-zero failed count — the registry client's behaviour or error identity changed.</fails_when>
  </verify>
  <done>Both classes are compiler-bound to a published port; the registry-unavailable error keeps its current identity and export site.</done>
</task>

<task type="auto">
  <name>Task 2: Ports for BrownfieldScanner and ComposeAnalyzer</name>
  <files>server/src/application/ports/brownfield-scanner-port.ts, server/src/application/ports/compose-analyzer-port.ts, server/src/infrastructure/brownfield-scanner.ts, server/src/infrastructure/compose-analyzer.ts</files>
  <read_first>
    - server/src/infrastructure/brownfield-scanner.ts (the class being interfaced — note its consumers are two route files today, a layering violation plan 10-09 fixes; the port is what that fix will depend on)
    - server/src/infrastructure/compose-analyzer.ts (the class being interfaced)
    - server/src/application/ports/certificate-filesystem-port.ts (written in Task 1 — the convention to match)
  </read_first>
  <action>
Create `server/src/application/ports/brownfield-scanner-port.ts` exporting `BrownfieldScannerPort` and `server/src/application/ports/compose-analyzer-port.ts` exporting `ComposeAnalyzerPort`, each listing the full public surface of its class with exact signatures, importing rather than restating every referenced result shape.

Add the matching `implements` clause to each class declaration.

The scanner's path-handling code — the separator normalisation and deduplication fixed in plan 05.1-12, and the Windows-path behaviour its two dedicated test files pin — must not be touched. This plan adds a type and one clause; nothing inside a method moves.
  </action>
  <acceptance_criteria>
    - Both port files exist, each exporting one interface covering the full public surface of its class.
    - `grep -cE '^export class BrownfieldScanner implements BrownfieldScannerPort' server/src/infrastructure/brownfield-scanner.ts` prints `1`.
    - `grep -cE '^export class ComposeAnalyzer implements ComposeAnalyzerPort' server/src/infrastructure/compose-analyzer.ts` prints `1`.
    - `git diff` on both infrastructure files shows no change inside any method body.
  </acceptance_criteria>
  <reversibility rating="reversible">Type-level only.</reversibility>
  <verify>
    <automated>yarn typecheck</automated>
    <fails_when>Non-zero exit, or any line containing `error TS` appears in the output.</fails_when>
    <automated>yarn workspace @docktor/server test:unit test/unit/infrastructure/brownfield-scanner.test.ts test/unit/infrastructure/brownfield-scanner-windows-paths.test.ts test/unit/infrastructure/compose-analyzer.test.ts</automated>
    <fails_when>Non-zero exit, or the run summary reports a non-zero failed count — path handling or analysis behaviour changed.</fails_when>
  </verify>
  <done>Both classes are compiler-bound to a published port with their path-handling behaviour provably unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Ports for ComposeRewriter and VolumeMigrator, plus the infrastructure-port fitness rule</name>
  <files>server/src/application/ports/compose-rewriter-port.ts, server/src/application/ports/volume-migrator-port.ts, server/src/infrastructure/compose-rewriter.ts, server/src/infrastructure/volume-migrator.ts, server/test/unit/architecture/layering.test.ts</files>
  <read_first>
    - server/src/infrastructure/compose-rewriter.ts (the class being interfaced — note `VolumeSelection` is declared here and imported by `application/migration-service.ts`)
    - server/src/infrastructure/volume-migrator.ts (the class being interfaced)
    - server/test/unit/architecture/layering.test.ts (created by 10-01 — the rule table this task appends a case to; match its file-reading approach and its self-diagnosing failure messages)
    - server/src/application/ports/docker-executor-port.ts (the naming convention the new rule encodes)
  </read_first>
  <action>
Create `server/src/application/ports/compose-rewriter-port.ts` exporting `ComposeRewriterPort` and `server/src/application/ports/volume-migrator-port.ts` exporting `VolumeMigratorPort`, each covering the full public surface of its class. `VolumeSelection` keeps its current declaration site and is imported by the port, not restated.

Add the matching `implements` clause to each class declaration.

Then append a new case to `server/test/unit/architecture/layering.test.ts`: **every `*.ts` file directly under `server/src/infrastructure/` declares at least one exported class, and every exported class declaration in that directory carries an `implements <Something>Port` clause.** Read the source text and match the class declaration with a line-anchored regular expression so a mention in a comment cannot satisfy the rule. Derive the expected port name from the class name by convention and assert the referenced port file exists under `server/src/application/ports/`, so a clause naming a port that was never written fails too.

Report failures by listing the offending file and class names in the assertion message.

Apply the same care as Tasks 1 and 2: the rewriter's YAML-document editing and the migrator's file-move logic are not touched.
  </action>
  <acceptance_criteria>
    - Both port files exist; `ComposeRewriterPort` imports `VolumeSelection` rather than redeclaring it.
    - `grep -cE '^export class ComposeRewriter implements ComposeRewriterPort' server/src/infrastructure/compose-rewriter.ts` prints `1`.
    - `grep -cE '^export class VolumeMigrator implements VolumeMigratorPort' server/src/infrastructure/volume-migrator.ts` prints `1`.
    - `grep -cE '^export class [A-Za-z]+ implements [A-Za-z]+Port' server/src/infrastructure/*.ts | grep -vc ':0$'` accounts for every infrastructure module that exports a class — the fitness rule and this count agree.
    - Temporarily adding a class with no `implements` clause to a file in `server/src/infrastructure/` makes `layering.test.ts` fail (confirmed once locally by the executor, then reverted).
  </acceptance_criteria>
  <reversibility rating="reversible">Type-level plus one test rule.</reversibility>
  <verify>
    <automated>yarn workspace @docktor/server test:unit test/unit/architecture/layering.test.ts</automated>
    <fails_when>Non-zero exit, or the run summary reports a non-zero failed count — an infrastructure class has no port, or a named port file does not exist.</fails_when>
    <automated>yarn typecheck</automated>
    <fails_when>Non-zero exit, or any line containing `error TS` appears in the output.</fails_when>
    <automated>yarn workspace @docktor/server test:unit</automated>
    <fails_when>Non-zero exit, or the run summary reports a non-zero failed count.</fails_when>
  </verify>
  <done>Every class in the infrastructure directory implements a named port, and a test — not a reviewer — is what keeps it that way.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application -> host filesystem | `CertificateFilesystem` writes certificate and key material; `BrownfieldScanner` walks operator-supplied directories; `VolumeMigrator` moves data between paths |
| application -> container registry (network) | `RegistryClient` makes outbound HTTP calls to arbitrary, operator-configurable registries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-15 | Tampering | `CertificateFilesystem`, `VolumeMigrator`, `ComposeRewriter` file writes | high | mitigate | Declaration-only plan: every task forbids editing any method body, and acceptance criteria require `git diff` on the infrastructure files to show only import and class-declaration lines. The existing unit suites for these modules run as verify gates |
| T-10-16 | Tampering | `BrownfieldScanner` path traversal | high | mitigate | The separator-normalisation and deduplication behaviour fixed in 05.1-12, plus the two dedicated Windows-path suites, run as an explicit verify gate on Task 2 so a regression in path handling fails the task |
| T-10-17 | Information Disclosure | `RegistryUnavailableError` identity | medium | mitigate | The error class keeps its declaration site and export, asserted by grep, so `update-checker.ts`'s catch-by-identity keeps matching and a registry failure is still classified rather than surfacing as an opaque error |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed |
</threat_model>

<verification>
- `yarn typecheck` exits zero — the compiler has verified all six classes against their ports.
- `yarn workspace @docktor/server test:unit` exits zero with the same pass count as before the plan.
- `server/test/unit/architecture/layering.test.ts` now enforces the infrastructure-port rule and passes.
</verification>

<success_criteria>
1. All six remaining infrastructure classes have a named port under `server/src/application/ports/` and declare `implements` against it (D-07 per PD-2, D-01).
2. No infrastructure method body changed (`git diff` evidence recorded in the summary).
3. The architecture fitness test fails if an infrastructure class has no port or names a port file that does not exist.
4. `yarn typecheck` and `yarn workspace @docktor/server test:unit` both exit zero.
</success_criteria>

<output>
Create `.planning/phases/10-backend-architecture-refactor/10-05-SUMMARY.md` when done.
</output>
