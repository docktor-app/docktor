---
phase: 10-backend-architecture-refactor
plan: 15
type: execute
wave: 12
depends_on: ["10-14"]
files_modified: []
autonomous: true
requirements: ["D-02", "D-06", "D-13", "D-14", "D-15", "D-17", "D-18"]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every decision D-01 through D-18 is traceable to a plan, an artifact and a passing check, recorded in one table (D-06)"
    - "The five existing integration test files are byte-identical to their state on the base branch — the phase's hard constraint, proven by a diff rather than asserted"
    - "The full unit suite of every workspace and the project typecheck are green against the finished phase"
    - "The integration suite passes unmodified against a live database, including the first-run concurrency guard this environment cannot exercise"
    - "A human has confirmed, against a running server, that the job lifecycle, the live-state stream, notification isolation and the audit log all behave as they did before the refactor"
  artifacts:
    - .planning/phases/10-backend-architecture-refactor/10-15-SUMMARY.md
  key_links:
    - "the four checks deferred by plans 10-07, 10-09, 10-11 and 10-12, plus the one deferred by 10-13 -> this plan's human-check blocks -> 10-UAT.md"
  prohibitions:
    - "A deferred check is marked satisfied on the strength of unit-test evidence the deferring plan already declared insufficient"
    - "An integration test is edited to make it pass, breaking the phase's stated hard constraint"
    - "The coverage table is filled in from the plan files alone, without confirming each artifact actually exists in the tree and each named test actually passes"
    - "A red automated gate is recorded as an environmental issue without the evidence that distinguishes the two"
---

<objective>
Close Phase 10: prove the whole refactor green in one place, and collect the five checks earlier plans deliberately deferred to a phase-level gate rather than running five times.

This is the phase's last plan. It writes no production code. Its deliverables are evidence: a decision-coverage table spanning D-01 through D-18, a green automated gate against the finished tree, an integration run against a live database, and a set of live-environment observations a human performs once.

The deferred checks and their origins:
- **Job lifecycle against a real environment** — deferred by plan 10-07, which migrated all seven jobs onto the registry and declined to run the live check seven times.
- **The first-run concurrency guard's integration test** — deferred by plan 10-09; STATE.md records a long-standing host-level block on this environment's database port, so the run needs an unrestricted host.
- **The live-state stream's event sequences** — deferred by plan 10-11; no automated integration test exercises that stream, so D-18's "exact same observable events" constraint rests on unit-level shape assertions until a human watches the real thing.
- **Notification isolation under a real mail outage** — deferred by plan 10-12, which made the emitting operation stop awaiting the send.
- **Audit-log freshness after an external edit** — deferred by plan 10-13, which accepted a named ordering window (PD-11) when the audit write moved behind a bus that does not await handlers.

Purpose: five separate live checks scattered across five plans would each cost a full environment setup and would each be verified against a partially-refactored tree. Run once, at the end, against the finished thing.
Output: a summary carrying the coverage table, the gate results, and the answers to five live checks.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-backend-architecture-refactor/10-CONTEXT.md
@.planning/phases/10-backend-architecture-refactor/10-VALIDATION.md
@.planning/phases/10-backend-architecture-refactor/10-13-SUMMARY.md
@.planning/phases/10-backend-architecture-refactor/10-14-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
This plan creates no source artifact. It produces `10-15-SUMMARY.md`, carrying the phase's decision-coverage table, its automated gate results, and the recorded outcome of the five live checks.

The phase-wide symbol inventory lives in `10-01-PLAN.md`; this plan verifies that inventory against the tree rather than extending it.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: The automated phase gate and the decision-coverage table</name>
  <files>.planning/phases/10-backend-architecture-refactor/10-15-SUMMARY.md</files>
  <read_first>
    - 10-CONTEXT.md `<decisions>` section in full — the eighteen decisions this table must account for, read from the source rather than from any plan's paraphrase
    - 10-01-PLAN.md `<artifacts_this_phase_produces>` (the phase-wide symbol inventory, with the plan that owns each row) and `<plan_decisions>` (PD-1 through PD-5, the planning decisions the table must show were honoured)
    - every `10-*-SUMMARY.md` written so far in this phase — specifically each one's recorded evidence, so the table cites what was actually built rather than what was planned
    - 10-VALIDATION.md "Per-Task Verification Map" (the per-decision verification the phase committed to; its rows map onto this table's rows)
  </read_first>
  <action>
Run the automated gate against the finished tree and record every result, then build the coverage table.

The gate, in order, with the exact output of each recorded in the summary:
1. `yarn typecheck` — zero type errors, the standing CLAUDE.md mandate.
2. `yarn workspace @docktor/server test:unit` — the server's unit project.
3. `yarn test:unit` — every other workspace's unit suite, to catch a client or shared regression caused by a server-side type change.
4. `yarn build:server` — the server compiles to its output directory, which a passing typecheck alone does not prove.
5. `OUT=$(git diff --name-only main...HEAD -- server/test/integration/); echo "status=$? len=${#OUT}"` — must print `status=0 len=0`. This is the phase's hard constraint made checkable: the boundary states the existing integration tests pass **unmodified**, and a diff is the only way to prove the second half of that sentence.

Then build the decision-coverage table. One row per decision, D-01 through D-18, each carrying: the plan or plans that implemented it, the concrete artifact in the tree that embodies it, the automated check that covers it, and its status. Confirm each artifact exists on disk and each named check passes — do not fill the table from the plan files, which describe intent rather than outcome. The mapping the phase planned is below; treat it as the expected shape and record any divergence you find rather than forcing the tree to match it:

| Decision | Implemented by | Where to look |
|---|---|---|
| D-01 | 10-01, 10-02, 10-05, 10-06, 10-08, 10-09, 10-10 | `application/ports/`, the layering fitness test's rule table |
| D-02 | 10-04, 10-07 | `jobs/job.ts`, `jobs/job-registry.ts`, `jobs/index.ts` |
| D-03 | 10-08 (call sites removed in passing), 10-14 (the dedicated pass) | 10-14's findings table |
| D-04 | 10-03, 10-11, 10-12, 10-13 | `infrastructure/event-bus.ts`, `application/subscribers/` |
| D-05 | the phase's own wave structure | the `wave` field across 10-01 to 10-15 |
| D-06 | 10-01 (PD-5) | this table's completeness — a decision with no row is the descope D-06 forbids |
| D-07 | 10-01, 10-02, 10-05, 10-06, 10-07 | one port file per class under `infrastructure/`, each with an `implements` clause |
| D-08 | 10-06 | `domain/backup-retention-policy.ts`, `domain/proxy-idempotency.ts` |
| D-09 | 10-01 | `repositories/index.ts` and its reference-identity test |
| D-10 | 10-01, and the route-layer siblings in 10-08, 10-09, 10-10 | the layering test's data-client rule |
| D-11 | 10-14 | the absence of `server/src/services/` and the fitness rule guarding it |
| D-12 | 10-04, 10-07 | the cron package imported only by the job base classes |
| D-13 | 10-04, 10-07 | the two base classes and the seven jobs' `extends` clauses |
| D-14 | 10-04, 10-07 | the registry's per-job health state and its tests |
| D-15 | item 1 → 10-12, item 2 → 10-13, item 3 → 10-11 | the three subscriber modules and their registration |
| D-16 | 10-03 | the bus's synchronous `emit` returning `void` |
| D-17 | 10-03, 10-11, 10-12, 10-13 | the per-listener isolation test, and each subscriber suite's rejecting-handler case |
| D-18 | 10-11, plus Task 3's live check here | the bridge's deep-equality tests, and the human check below |

A decision whose row has no artifact or no passing check is a finding, not a formatting problem — record it as such and say so plainly in the summary rather than marking the row green.
  </action>
  <acceptance_criteria>
    - The summary contains the five gate results with their recorded output, in the order run.
    - The integration-diff check printed `status=0 len=0`, and the summary records that.
    - The coverage table has exactly eighteen rows, D-01 through D-18, none blank.
    - Every artifact cited in the table was confirmed to exist on disk, and every check cited was confirmed to pass, by running or listing it — not by reading a plan file.
    - Any divergence between the expected mapping above and what the tree actually contains is recorded as a finding with its own paragraph.
  </acceptance_criteria>
  <reversibility rating="reversible">Evidence gathering only; this task writes no source file.</reversibility>
  <verify>
    <automated>yarn typecheck</automated>
    <fails_when>Non-zero exit, or any line containing `error TS` appears in the output.</fails_when>
    <automated>yarn workspace @docktor/server test:unit</automated>
    <fails_when>Non-zero exit, or the run summary reports a non-zero failed count.</fails_when>
    <automated>yarn test:unit</automated>
    <fails_when>Non-zero exit, or the run summary reports a non-zero failed count in any workspace.</fails_when>
    <automated>yarn build:server</automated>
    <fails_when>Non-zero exit, or any line containing `error TS` appears in the output.</fails_when>
    <automated>OUT=$(git diff --name-only main...HEAD -- server/test/integration/); echo "status=$? len=${#OUT}"</automated>
    <fails_when>Prints anything other than `status=0 len=0` — a non-zero length means an existing integration test was modified, breaking the phase's stated hard constraint that they pass unmodified; a non-zero status means the diff itself did not run.</fails_when>
  </verify>
  <done>The finished phase compiles, builds, passes every unit suite, has not touched an integration test, and every one of the eighteen decisions is traceable to an artifact and a passing check.</done>
</task>

<task type="auto">
  <name>Task 2: The integration suite against a live database</name>
  <files>.planning/phases/10-backend-architecture-refactor/10-15-SUMMARY.md</files>
  <precondition>A PostgreSQL instance matching DATABASE_URL in .env.development accepts Prisma connections from the executing host; STATE.md records a long-standing host-level block on this environment's database port, so an unrestricted host is required.</precondition>
  <read_first>
    - server/test/integration/ — the five files, listed and left alone; note that `setup-concurrency.test.ts` is the one plan 10-09 named specifically, because the first-run guard it exercises depends on a uniqueness violation propagating to the caller and no unit test can prove that against a real database
    - 10-09-SUMMARY.md (what the concurrency guard's unit assertions do and do not cover, so a pass here is recorded as closing a named gap rather than as a generic green tick)
    - server/vitest.config.ts (the two named projects and the integration project's raised timeouts)
    - STATE.md entries naming the host-level database-port block (05.1-01, 05.1-05, 06-01, 09-03, 09-05) — the established evidence that a failure here may be environmental, and the shape of the evidence that distinguishes environmental from real
  </read_first>
  <action>
Run the five existing integration test files, unmodified, against a live database, and record the result per file.

If the run fails, distinguish the two possible causes before recording anything: a refactor regression, or the environmental block this project has hit repeatedly. The distinguishing evidence is the failure's shape — a connection or protocol-level failure before any test body executes points at the environment; an assertion failure inside a test body points at the phase. Record which, with the output that shows it. Do not record an assertion failure as environmental.

Under no circumstance edit an integration test to make it pass. The phase boundary states these five files pass **unmodified**; a change there does not fix the gate, it removes it. If a test genuinely needs to change because the phase changed behaviour it asserts, that is a phase-boundary violation and must be reported, not absorbed.

If the precondition cannot be met on the executing host, record the block with its evidence and leave the check to the human block below — that is the path STATE.md documents for this project, and plan 10-09 deferred here precisely for it.
  </action>
  <acceptance_criteria>
    - The five integration files are listed with a per-file result.
    - No file under `server/test/integration/` differs from the base branch.
    - If the suite did not run, the summary records the blocking evidence and classifies it as environmental with the output that supports the classification.
    - The first-run concurrency guard's result is called out by name, since plan 10-09 deferred it here specifically.
  </acceptance_criteria>
  <reversibility rating="reversible">A test run; nothing is modified.</reversibility>
  <verify>
    <automated>yarn workspace @docktor/server test:integration</automated>
    <fails_when>Non-zero exit, or the run summary reports a non-zero failed count, or fewer than five test files are collected.</fails_when>
    <automated>OUT=$(git diff --name-only main...HEAD -- server/test/integration/); echo "status=$? len=${#OUT}"</automated>
    <fails_when>Prints anything other than `status=0 len=0` — a non-zero length means an integration test was edited during the gate; a non-zero status means the diff itself did not run.</fails_when>
    <human-check>
On a host with a reachable PostgreSQL instance (this project's environment has a documented block on the database port), run `yarn workspace @docktor/server test:integration` from the repository root and confirm all five files pass with no edit to any of them. Confirm specifically that `server/test/integration/setup-concurrency.test.ts` passes: it is the only check that exercises the first-run lock's exclusive insert against a real database, and plan 10-09 restructured the route that owns it.
    </human-check>
  </verify>
  <done>The five integration files pass unmodified against a live database, or the block preventing that is recorded with evidence and routed to a human.</done>
</task>

<task type="auto">
  <name>Task 3: The live-environment behavioural gate</name>
  <files>.planning/phases/10-backend-architecture-refactor/10-15-SUMMARY.md</files>
  <read_first>
    - 10-07-PLAN.md `<verification>` (the job startup and shutdown check it deferred, and what a leak looks like: an orphaned container-event stream or file watcher after a shutdown signal)
    - 10-11-PLAN.md `<verification>` (the live-state stream check it deferred, and the four operations whose event sequences D-18 freezes)
    - 10-12-PLAN.md `<verification>` (the mail-outage check it deferred, and the awaited-to-fire-and-forget change that made it necessary)
    - 10-13-PLAN.md `<verification>` (the audit-log freshness check it deferred, and PD-11's named ordering window)
    - server/src/jobs/index.ts in its finished state (the registry's startup log lines, which are what a human reads to confirm all seven jobs registered)
    - docker-compose.dev.yml and the repository root scripts (how this project is brought up, so the human check names real commands rather than generic ones)
  </read_first>
  <action>
Automate everything automatable about the five deferred checks, then record what is left for a human.

Automatable now, and to be run and recorded: the job registry's unit suite, which proves every one of the seven jobs is registered and that a throwing stop does not abort the remaining stops; and the server build, which produces the artifact a human will actually start. Record both results.

Then write the human blocks below into the summary verbatim alongside their outcomes once answered. Each block names the exact commands to bring the environment up and the exact observation to make, because the phase's verification is harvested into the phase's UAT document and read later, away from this context.

What must not happen here: none of the four originally deferred checks may be marked satisfied on unit-test evidence. Each deferring plan stated in writing why its unit assertions were insufficient — plan 10-11 because no integration test exercises the live-state stream at all, plan 10-12 because a mail outage cannot be simulated end to end in a unit test, plan 10-07 because a leaked watcher is invisible until a real shutdown, plan 10-13 because the ordering window it accepted is between two processes' clocks rather than inside one function. Restating the unit evidence is not an answer to any of them.
  </action>
  <acceptance_criteria>
    - The job registry's unit suite result and the server build result are recorded.
    - The summary carries five human blocks — job lifecycle, live-state sequences, mail outage, audit-log freshness, and the integration run from Task 2 — each with a place for its outcome.
    - No deferred check is marked satisfied by citing a unit test.
    - Each block names the commands to bring up the environment and the exact observation to make, with no step left as "check it works".
  </acceptance_criteria>
  <reversibility rating="reversible">Evidence gathering only.</reversibility>
  <verify>
    <automated>yarn workspace @docktor/server test:unit test/unit/jobs/index.test.ts</automated>
    <fails_when>Non-zero exit, or the run summary reports a non-zero failed count — the job registry's own suite is red, so no live observation of job startup would be meaningful.</fails_when>
    <automated>yarn build:server</automated>
    <fails_when>Non-zero exit, or any line containing `error TS` appears in the output — there is no artifact for a human to start.</fails_when>
    <human-check>
**Job startup and shutdown (deferred by plan 10-07 — D-02, D-13, D-14).**
Start the server against a real environment. Confirm the startup log names every one of the seven jobs — the state poller, file watcher, update checker, disk checker, notification watcher, backup scheduler and proxy certificate poller — and that none is missing from the sequence. Then send the process a shutdown signal and confirm it exits without leaving an orphaned container-event stream or a file watcher still holding the stacks directory. Report the startup log's job list and whether the process exited cleanly.
    </human-check>
    <human-check>
**Live-state stream sequences (deferred by plan 10-11 — D-18).**
With the server running, connect a browser (or an events client) to the live-state endpoint and perform four operations, recording the event sequence each produces: deploy a stack, stop a stack, edit a stack's compose file directly on disk outside the app, and run a backup. For each, confirm the events arrive with the same types, the same fields and in the same order as before the refactor. The configuration-changed events are the ones to watch most closely: their origin discriminator distinguishes an in-app save from an outside edit, and the outside edit above must arrive tagged as external.
    </human-check>
    <human-check>
**Notification isolation under a mail outage (deferred by plan 10-12 — D-17).**
Configure a mail server in settings, then make it unreachable (point it at a closed port, or block it). Run a backup that fails. Confirm three things: the backup's own outcome and status transition are what they would be with a working mail server, its log stream still terminates normally, and the notification row is still written to the notification log. The point of the check is that a mail outage is invisible to the backup — before this phase the backup awaited the send.
    </human-check>
    <human-check>
**Audit-log freshness after an external edit (deferred by plan 10-13 — D-15 item 2, PD-11).**
With a stack's detail page open in a browser, edit that stack's compose file directly on disk. Confirm the event log section shows the new entry without a manual refresh, and that its rendered label and detail match an entry written before this phase. Then repeat with an invalid compose file and confirm the configuration-error entry appears the same way. This is the observable symptom of the ordering window plan 10-13 accepted: the audit row is now written by a subscriber the emitting job does not await, so the browser can be told about the change before the row commits.
    </human-check>
  </verify>
  <done>Every check this phase deferred has been asked of a human once, in one place, against the finished server, with the commands and observations spelled out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase -> main branch | This gate is the last thing standing between an eighteen-decision internal refactor and a merge |
| server -> browser (live state) | The stream Phase 08 validated is verified here against a real client for the first time since the rewiring |
| server -> SMTP server | A deliberately unreachable mail server is used to prove the isolation the phase introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-50 | Repudiation | a gate marked green on incomplete evidence | high | mitigate | Every gate step records its actual output; the coverage table is built by confirming artifacts on disk and running the cited checks, and a row without both is recorded as a finding rather than marked green |
| T-10-51 | Tampering | an integration test edited to pass | high | mitigate | A diff against the base branch is run twice — once in Task 1 and once in Task 2 — and the action states plainly that editing one removes the gate rather than fixing it |
| T-10-52 | Repudiation | an environmental failure recorded as a pass, or a real regression recorded as environmental | high | mitigate | Task 2 requires the failure's shape to be classified with the output that supports it: a pre-test-body connection failure versus an in-test assertion failure, the distinction STATE.md's prior entries already establish for this project |
| T-10-53 | Information Disclosure | credentials during the live checks | medium | mitigate | The mail-outage check configures a mail server through the app's own settings, where the password is encrypted at rest by the existing settings path; the summary records the observation, never the configuration |
| T-10-06 | Denial of Service | mail outage stalling a backup | high | mitigate | This is the threat the live mail-outage check exists to confirm closed; plan 10-12 mitigated it in code and deferred the real-world confirmation here |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan or this phase |
</threat_model>

<verification>
- `yarn typecheck`, `yarn workspace @docktor/server test:unit`, `yarn test:unit` and `yarn build:server` all exit zero against the finished phase.
- The integration-diff check prints `status=0 len=0` — no existing integration test was touched.
- The summary carries the eighteen-row decision-coverage table with no blank cell, the five gate results with their output, the per-file integration result, and the five human blocks with their outcomes.
- The four checks deferred by plans 10-07, 10-09, 10-11 and 10-12, plus the one deferred by plan 10-13, appear here and nowhere else in the phase.
</verification>

<success_criteria>
1. Every decision D-01 through D-18 is traceable to a plan, an artifact confirmed on disk and a check confirmed passing (D-06 — a missing row is the descope D-06 forbids).
2. The five existing integration test files are unmodified, proven by diff, and pass against a live database.
3. The project typecheck, every workspace's unit suite and the server build are green against the finished phase.
4. The job lifecycle, the live-state stream's event sequences, notification isolation under a real mail outage and audit-log freshness after an external edit have each been put to a human once, against a running server (D-02, D-13, D-14, D-17, D-18, D-15).
5. No deferred check is closed by restating the unit-level evidence its deferring plan already declared insufficient.
</success_criteria>

<output>
Create `.planning/phases/10-backend-architecture-refactor/10-15-SUMMARY.md` when done.
</output>
